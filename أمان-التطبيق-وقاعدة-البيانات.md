# أمان التطبيق وقاعدة البيانات

> **الفرق عن `تقرير-أمان-RLS.md`:** ذاك الملف سجل تاريخي (قصة كل حادثة أمنية وإصلاحها بترتيب زمني). هذا الملف **الوضع الحالي + قائمة حية**.
>
> **يُسجَّل هنا دائماً:**
> 1. **كل شي يرفع نسبة الأمان** (بالكود أو بقاعدة البيانات) — قسم "طبقات الحماية المطبَّقة"
> 2. **كل نقطة/طريقة يحاول منها المخترقون الدخول** (IDOR، XSS، تجاوز Rate Limiting، سرقة الجلسة، إساءة استخدام منطق العمل، تسريب أسرار، وغيرها) — قسم "دليل الفحص الذاتي" + جدول حالة التنفيذ لكل واحدة (نُفِّذت/اختُبرت أو لسا)
>
> يُحدَّث باستمرار — أي نقطة جديدة بأي من النوعين (حماية جديدة، أو طريقة اختراق جديدة نكتشفها أو نفكر فيها) تُضاف فوراً، سواء نُفِّذت مباشرة أو انتظرت التنفيذ لاحقاً.
>
> آخر تحديث: 2026-07-14

---

## 📊 التقييم الحالي (تقديري، مو رقم علمي دقيق)

| الطبقة | النسبة التقديرية | آخر تحديث |
|---|---|---|
| سيبابيس (RLS + GRANT) | ~75-80% | 2026-07-07 |
| الكود (تطبيق + إدارة) | ~55-65% | 2026-07-14 |

**⚠️ لا شي يحمي 100% — هذا مبدأ ثابت بالأمن السيبراني (Defense in Depth)، مو تشاؤم. الهدف: طبقات كافية تخلي كلفة الاختراق أعلى بكثير من فائدته.**

---

## ✅ طبقات الحماية المطبَّقة فعلياً

### بالكود
- تشفير (hash) رمز دخول الصالون بالسيرفر — `api/_lib/owner-session.js` (`hashOwnerPin`)
- تحقق حقيقي بالسيرفر لرمز العميل (خطوة أولى) — `api/customer-auth.js`
- Rate limiting على محاولات تسجيل الدخول — `api/_lib/rate-limit.js`، مُفعَّل بـ`owner-auth.js`, `register-salon.js`
- قفل مؤقت بعد محاولات فاشلة متكررة (5 محاولات → قفل 10 دقائق) + رسالة توضح الوقت المتبقي
- حماية من الضغط المزدوج بنماذج التسجيل (بعد إصلاح 2026-07-14)
- فصل صلاحيات الحذف الذاتي (قابل للاسترجاع) عن الحذف النهائي بلوحة الإدارة
- منع اختيار نفس الرمز السري القديم عند استعادة رمز دخول الصالون (2026-07-15) — `api/salon-reset-pin.js`
- SMTP خارجي (Resend) مفعّل لبريد استعادة الرمز السري — يحل مشكلة قوالب البريد المقفولة بالخدمة الافتراضية (2026-07-15)

### بقاعدة البيانات (سيبابيس)
- RLS مفعّل على كل الجداول + "Enable automatic RLS" على DAWRAK-TEST (يمنع نسيان تفعيله بجدول جديد)
- GRANT مطابق تماماً لكل RLS policy (بعد تدقيق شامل 2026-07-07)
- `ALTER DEFAULT PRIVILEGES` دائم لصلاحيات الـsequences — يمنع تكرار فخّ "sequence بدون GRANT" لأي جدول مستقبلي
- إخفاء أعمدة حساسة (كلمة مرور الصالون، owner_pin_hash) عن anon/authenticated عبر GRANT على مستوى العمود (column-level)
- DAWRAK وDAWRAK-TEST متطابقان 100% هيكلةً وصلاحياتٍ (يسمح باختبار آمن قبل أي تغيير خطير على الإنتاج)
- إخفاء `pin_hash`/`pin_fails`/`pin_locked_until` لجدول `customers` عن anon/authenticated (2026-07-14، **مؤكد فعلياً بالمساء** بعد اكتشاف إن إصلاح الصباح كان بلا أثر حقيقي بسبب منح جدول كامل يتجاوزه — راجع `تقرير-أمان-RLS.md`) — يمنع قراءة/تعديل الرمز السري المُشفّر مباشرة عبر API خارج التطبيق. ✅ مطبَّق ومؤكد على DAWRAK وDAWRAK-TEST معاً

---

## 🔴 نقاط أمان عاجلة (فجوات حقيقية مفتوحة الآن)

| النقطة | الخطورة | التفاصيل |
|---|---|---|
| **RLS مفتوحة بالكامل (`USING (true)`) على `customers`/`bookings`/`reviews`/`promo_codes`** للقراءة والتعديل + `loadData()` تجيب كل العملاء/الحجوزات بلا فلتر صف لأي زائر (`App.jsx:1844`) | 🟡 متوسطة (كانت مصنّفة عالية) | ما فيه نظام هوية حقيقي (`auth.uid()`) مربوط بكل طلب — الثغرة حقيقية بالكود، لكن **تحقّق حي 2026-07-16 (F12/Network) أكّد الضرر الفعلي صفر الآن** لأنه لا يوجد عميل حقيقي ثانٍ غير أحمد بقاعدة البيانات. **يجب يُصلح قبل انضمام أي عميل حقيقي ثانٍ** — مو طارئ بالوقت الحالي. التفاصيل الكاملة بـ`تقرير-أمان-RLS.md` (قسم 2026-07-16) ونقطة 86 بـ`الفحص-الشامل.md` |
| **🟡 لوحة الإدارة: بقايا ثغرات `next`/`firebase` (بعد الإصلاح الجزئي)** | 🟡 متوسطة (كانت عالية جداً) | **✅ الثغرة الحرجة (تجاوز صلاحيات) اتصلحت** — تحديث `next@14.2.5→14.2.35` (نفس الإصدار الرئيسي، آمن، مبني ومختبر بنجاح، مرفوع على `main`). **باقٍ:** عدة ثغرات DoS/XSS/تسميم Cache بـNext.js **ما تنصلح إلا بقفزة إصدار رئيسي كامل** (next@16، مو مجرد patch كما افترضنا أول مرة) + ثغرات `firebase`/`undici` (تحتاج `firebase@12`). كلاهما تغييرات كاسرة (breaking) تحتاج اختبار توافق كامل منفصل — أُجِّلت عمداً. التفاصيل الكاملة بـ`تقرير-أمان-RLS.md` (قسم 2026-07-14) |
| **🟡 تسريب اسم+جوال عملاء الصالون — مخفَّف جزئياً** | 🟡 متوسطة (كانت عالية جداً) | اكتُشف أثناء جرد Tier 2 (2026-07-14): `refreshSalonBookings()` بـ`App.jsx`. **✅ الإصلاح الفوري مطبَّق (L125):** تطبيقنا نفسه ما عاد يطلب إلا حجوزات العميل نفسه بجهة العميل. **⚠️ الثغرة الجذرية لسه مفتوحة:** لأن سياسة RLS بجدول `bookings` لسا `USING(true)`، أي طلب مباشر لقاعدة البيانات (خارج تطبيقنا، بأداة زي Postman) بمعرفة `salon_id` بس يقدر لسا يجيب بيانات كل العملاء — الإصلاح الجذري الوحيد هو سياسة RLS حقيقية (مرحلة 2 من Tier 2). تفاصيل كاملة بـ`تقرير-أمان-RLS.md` (قسم 2026-07-14 الأخير) |
| **✅ دخول عميل بمجرد رقم الجوال بدون أي تحقق PIN** | كانت 🔴 حرجة جداً — **مُصلحة الآن** | اكتُشف 2026-07-15 (أحمد لاحظه أثناء مراجعة الشاشات): تبويب "رقم الجوال" بدخول العميل كان يسجّل الدخول لأي حساب بمجرد كتابة جواله، بدون أي رمز سري — استيلاء كامل محتمل على أي حساب عميل بمعرفة رقمه بس. **الإصلاح (L127):** التبويب صار يطلب PIN إجبارياً، ويتحقق منه بالسيرفر عبر `/api/customer-auth` (محمي بـrate limiting وقفل بعد 5 محاولات — نفس آلية دخول الصالون). مبني ومختبر (build نظيف)، بانتظار اختبار حي. تفاصيل كاملة بـ`تقرير-أمان-RLS.md` (قسم 2026-07-15) |

**✅ قرار محسوم ومُحدَّث (أحمد، 2026-07-14 — بخصوص Tier 2):** سُجّلت كنقطة رسمية (#93 بـ`الفحص-الشامل.md`). **تحديث لاحق نفس اليوم:** بعد نقاش مطوّل عن مستوى الحماية الحالي، قرر أحمد رفعها لأعلى الأولويات — **تُنفَّذ أولاً قبل أي نقطة متبقية أخرى** (بما فيها #28/#29 وباقي الأخطاء الصغيرة)، يلغي قرار "الأسهل أولاً" لخصوص هذي النقطة. مُسجَّلة رسمياً أيضاً بـ`ملف_الاتفاقيات/ملف_الاتفاقيات.md` (بند 25).

---

## ⏳ نقاط أمان معلّقة (لسه ناقصة، أقل إلحاحاً)

| النقطة | السبب/الشرط |
|---|---|
| **مراجعة أمنية مستقلة (Pentest)** من مختص خارجي | إلزامي قبل أي إطلاق تجاري حقيقي — تفاصيل بـ`تقرير-أمان-RLS.md` (قسم 2026-07-14). **مو نفس الشي مع الاختبارات الذاتية أدناه** — الاستقلالية تحديداً تعني شخص ما شارك ببناء التطبيق، بدون التحيز/عمى النقاط اللي عندنا احنا الاثنين |
| **فحص آلي بأداة OWASP ZAP** (حل وسط بانتظار Pentest بشري) | ⏳ لم يُنفَّذ بعد. **قرار أحمد (2026-07-14):** أقرب لمستوى احترافي من الاختبارات اليدوية الست، وما يحتاج طرف ثالث بشري. يُنفَّذ بجلسة لاحقة — كلود يوجّه أحمد خطوة بخطوة (تثبيت الأداة، تشغيلها على رابط التطبيق المنشور، قراءة النتائج) |
| ضعف معماري بتسجيل دخول **العميل** | التحقق أساساً محلي (localStorage)، السيرفر يتحقق بالخلفية بصمت بدون ما يمنع الدخول لو فشل — يحتاج تصميم أقوى مستقبلاً |
| **"دخول بالبصمة" مضلّل — لا يستخدم WebAuthn حقيقي** | اكتُشف 2026-07-15: الزر يتحقق بس من دعم المتصفح نظرياً (`window.PublicKeyCredential`) ثم يقرأ `dork_biometric_id` من localStorage ويُدخل مباشرة، بدون أي طلب بصمة/Face ID فعلي من نظام الجهاز. تسمية مضلّلة — خطورته أقل من ثغرات بعيدة المدى (يحتاج وصول فعلي للجهاز)، لكنه خداع للمستخدم بخصوص مستوى الحماية الفعلي. **قرار أحمد: تسجيلها، غير عاجلة** — إما تطبيق WebAuthn حقيقي أو تصحيح التسمية لـ"دخول سريع (هذا الجهاز)" |

> باقي أنواع الاختراق المحتملة (IDOR، XSS، تجاوز Rate Limiting، أمان الكوكيز/CSRF، تسريب الأسرار، إساءة استخدام منطق العمل، ثغرات المكتبات الجاهزة) متابَعة بجدول واحد أدناه — تفادياً لتكرار نفس النقطة بجدولين مختلفين.

---

## 🔍 دليل الفحص الذاتي (لأحمد — بأدوات المتصفح، بدون برامج) + جميع نقاط الاختراق المحتملة

النص الكامل بالخطوات موجود بـ`تقرير-أمان-RLS.md` (قسم 2026-07-14). ملخص الاختبارات:

1. **XSS** — إدخال `<img src=x onerror=alert('XSS')>` بحقل نصي يظهر لطرف ثاني، وتأكيد إنه ما يُنفَّذ
2. **IDOR** — تعديل رقم ID بطلب شبكي (Network tab) والتأكد ما يرجّع بيانات حساب ثاني
3. **تجاوز Rate Limiting** — محاولات دخول خاطئة متكررة، تأكيد القفل يشتغل
4. **أمان الكوكيز + CSRF** — **لصاحب الصالون:** F12 → Application → Cookies → `dork_owner_session`، تأكيد HttpOnly + Secure + SameSite (✅ تحقّق كودي 2026-07-14 أكّد الثلاثة صحيحة). **للعميل:** ⚠️ ما فيه كوكي أصلاً — الجلسة بـlocalStorage (مفتاح `sb-...-auth-token`) عبر مكتبة Supabase، بدون حماية HttpOnly — ضعف معماري معروف مسبقاً، يخلي اختبار XSS (بند 1) حرج بشكل خاص لجهة العميل
5. **تسريب الأسرار** — بحث بكود المصدر عن `service_role`, `SECRET`, `PRIVATE_KEY`
6. **إساءة استخدام منطق العمل** — تجاوز حد استخدام كود خصم، أو حجز متعارض
7. **ثغرات مكتبات جاهزة قديمة (`npm audit`)** — ✅ **نُفِّذ فعلياً 2026-07-14** (مو اختبار متصفح، شغّله كلود مباشرة): `dork-app` صفر ثغرات، `admin/` 14 ثغرة (تفاصيل بجدول "نقاط أمان عاجلة" أعلاه وبـ`تقرير-أمان-RLS.md`)

⚠️ اختبارات الكتابة (XSS، استغلال الأكواد) على صالون تجريبي فقط، مو "الوادي" الحقيقي.

**✅ قرار أحمد (2026-07-14):** تُنفَّذ اختبارات المتصفح (1-6) بجلسة لاحقة (بعدين) — يبدأ بتسجيل صالون تجريبي مخصّص للاختبار (زي "لقمان" سابقاً)، ثم يجرَّب كل اختبار بالترتيب واحد واحد بالتنسيق المباشر مع كلود، ويُسجَّل نتيجة كل واحد هنا فور تنفيذه.

**حالة التنفيذ (تُحدَّث أول بأول):**

| # | الاختبار | الحالة |
|---|---|---|
| 1 | XSS | ⏳ لم يُنفَّذ بعد |
| 2 | IDOR | ⏳ لم يُنفَّذ بعد |
| 3 | تجاوز Rate Limiting | ⏳ لم يُنفَّذ بعد (ملاحظة: قفل الدخول بعد 5 محاولات تأكد شغّال فعلاً بنقطة #19 — لكن هذا اختبار محاولة تجاوزه فعلياً، مو نفس الشي) |
| 4 | أمان الكوكيز + CSRF (HttpOnly/Secure/SameSite) | 🟡 **جزئي 2026-07-14** — تحقّق كودي (مو اختبار متصفح حي بعد): صاحب الصالون ✅ محمي بالكامل (HttpOnly+Secure+SameSite). العميل ❌ بدون حماية HttpOnly (localStorage) — ضعف معماري معروف |
| 5 | تسريب الأسرار بكود المتصفح | 🟡 **جزئي 2026-07-14** — تحقّق كودي أوّلي: صفر نتائج لـ`service_role` بكود الواجهة (App.jsx وصفحات admin)، ومفتاح service_role بلوحة الإدارة مقروء فقط من كود سيرفر (بدون `NEXT_PUBLIC_`) ولا يستورده أي مكوّن client — لسه ناقص فحص حي كامل بالمتصفح المنشور فعلياً |
| 6 | إساءة استخدام منطق العمل (كود خصم/تعارض حجز) | ⏳ لم يُنفَّذ بعد |
| 7 | ثغرات مكتبات جاهزة (`npm audit`) | ✅ **نُفِّذ 2026-07-14** — `dork-app`: صفر؛ `admin/`: 14 ثغرة (إصلاح Next.js متاح وآمن، إصلاح firebase يحتاج قفزة إصدار رئيسي) |

**تدقيق كودي إضافي 2026-07-14 (بطلب أحمد "لازم نطبق النقاط هذي كلها" — مراجعة الكود الفعلي مقابل قائمة الكلمات/الرموز اللي ترفع الأمان):**

| الممارسة | مطبَّقة فعلياً؟ |
|---|---|
| تشفير الرموز السرية (hash) | ✅ نعم — `hashOwnerPin`, `hashCustomerPin` |
| React auto-escape ضد XSS | ✅ نعم — استخدام وحيد لـ`dangerouslySetInnerHTML` بـ`admin/src/app/layout.tsx` وهو نص ثابت (theme init)، مو محتوى مستخدم |
| HttpOnly + Secure + SameSite على كوكي الجلسة | ✅ لصاحب الصالون فقط — ❌ ما يوجد أصلاً لجهة العميل (localStorage بدل كوكي) |
| أسرار السيرفر (`service_role`) غير مسرَّبة للمتصفح | ✅ نعم — صفر استخدام بكود العميل/admin المُقدَّم للمتصفح، ومفتاح الإدارة `SUPABASE_SERVICE_ROLE_KEY` (بدون `NEXT_PUBLIC_`) لا يُستورَد من أي client component |
| `auth.uid()` بسياسات RLS بدل `true` | ❌ لا — هذا بالضبط فجوة Tier 2 المفتوحة |
| GRANT محدّد بأعمدة (column-level) للحساس | ✅ نعم — مطبَّق على `salons` و`customers` |

**الخلاصة:** أغلب "الكلمات/الرموز" المذكورة مطبَّقة فعلاً بالكود، ما عدا فجوتين معروفتين مسبقاً وما تغيّرتا: جلسة العميل (localStorage) وTier 2 (`auth.uid()`).

---

## 📝 سجل الإضافات (كل شي يرفع نسبة الأمان يُسجَّل هنا بتاريخه)

- **2026-07-14** — إنشاء هذا الملف + التقييم الأولي + دليل الفحص الذاتي
- **2026-07-14 (صباحاً)** — اكتشاف وإصلاح أوّلي: `customers.pin_hash` كان قابل للقراءة والتعديل من anon/authenticated — أُصلح ظاهرياً (REVOKE عمودي) على DAWRAK وDAWRAK-TEST
- **2026-07-14 (مساءً)** — 🔴 اكتشاف: الإصلاح الصباحي **لم يُغلق الثغرة فعلياً** — `anon` كان عنده منح جدول كامل (`GRANT SELECT, UPDATE ON customers`) يتجاوز أي REVOKE عمودي بصمت. أُصلح جذرياً هذي المرة (REVOKE جدول كامل + GRANT عمودي صريح للأعمدة الآمنة فقط) ومؤكد بثلاث طبقات تحقق. أضاف أيضاً درس جديد لـCLAUDE.md (بند 11). ⏳ التطبيق على DAWRAK-TEST لسا معلّق
- **2026-07-14** — توثيق فجوة Tier 2 القديمة (RLS مفتوحة على customers/bookings/reviews/promo_codes) كنقطة عاجلة تحتاج مراجعة، بعد ما كانت مؤجَّلة بهدوء من 2026-07-02
- **2026-07-14** — إضافة 3 دروس أمان ناقصة من `تقرير-أمان-RLS.md` لقائمة الفحص الإلزامية بـ`CLAUDE.md` (بند 7، 8، 9): RLS للأدوار كلها معاً، فحص column-level grants قبل REVOKE، فحص منهجي شامل للأعمدة الحساسة
- **2026-07-14** — تسجيل مشروع Tier 2 كنقطة رسمية (#93 بـ`الفحص-الشامل.md`) — يُؤخَذ بالترتيب الطبيعي مع باقي النقاط المتبقية، مو مؤجَّل لأجل غير مسمى
- **2026-07-14** — تأكيد أحمد تنفيذ الست اختبارات الذاتية (دليل الفحص الذاتي) كاملة بجلسة لاحقة على صالون تجريبي مخصّص — أُضيف جدول تتبّع حالة كل اختبار
- **2026-07-14** — تنفيذ فعلي أول مرة لـ`npm audit`: `dork-app` صفر ثغرات ✅، `admin/` 14 ثغرة (1 حرجة) بسبب `next@14.2.5` القديم + تبعية `firebase`/`undici` — لم يُصلح بعد، يحتاج قرار أحمد
- **2026-07-14** — دمج CSRF ضمن اختبار أمان الكوكيز (فحص SameSite)، وتوحيد جدول تتبّع "نقاط أمان معلّقة" مع جدول "دليل الفحص الذاتي" لتفادي التكرار
- **2026-07-14** — تدقيق كودي شامل بطلب أحمد ("لازم نطبق النقاط هذي كلها"): تأكيد تطبيق hash/XSS-escape/أسرار السيرفر فعلياً بالكود ✅. اكتُشف وصُحِّح خطأ بدليل الفحص الذاتي: كوكي `dork_customer_session` غير موجود أصلاً — جلسة العميل بـlocalStorage بدون HttpOnly (يربط مباشرة بضعف تسجيل دخول العميل المعروف مسبقاً)
- **2026-07-14** — تسجيل قرار أحمد: فحص آلي بأداة OWASP ZAP كحل وسط عملي قبل توفر Pentest بشري مستقل — يُنفَّذ بجلسة لاحقة بتوجيه كلود خطوة بخطوة
- **2026-07-14** — قرار أحمد النهائي: مشروع Tier 2 يُنفَّذ **أولاً** قبل أي نقطة متبقية أخرى (يلغي "الأسهل أولاً" لخصوصه) — مُسجَّل أيضاً بـ`ملف_الاتفاقيات/ملف_الاتفاقيات.md` (بند 25)
- **2026-07-14** — Tier 2 مرحلة 1 (تعبئة `salons.auth_uid` تلقائياً) مطبَّقة ومؤكدة حياً على صالون "الوادي" + إصلاح خلل عرض `owner_email` بعد الحفظ (L124)
- **2026-07-14** — جرد Tier 2 مرحلة 2: اكتشاف ثغرة خصوصية حية (`refreshSalonBookings` تسرّب اسم+جوال كل عملاء صالون لأي زائر) + نمطين إضافيين (استعلامات قبل الهوية، استعلام customers غير مفلتر) يعقّدان قفل RLS
- **2026-07-14** — إصلاح فوري (L125) لتسريب `refreshSalonBookings`: تطبيقنا نفسه صار يطلب حجوزات العميل نفسه بس بجهة العميل — يقلل التسريب العملي كثيراً، لكن **الثغرة الجذرية بمستوى RLS لسا مفتوحة** (طلب مباشر خارج التطبيق لسا يقدر يجيب بيانات الكل)
- **2026-07-14** — 🔴→✅ اكتشاف حرج وإصلاح جذري: إصلاح `customers.pin_hash` الصباحي كان بلا أثر فعلي (منح جدول كامل لـ`anon` يتجاوز REVOKE عمودي) — أُصلح جذرياً (REVOKE جدول + GRANT عمودي صريح) ومؤكد بثلاث طبقات تحقق حي، مع إصلاح عطل مصاحب بتسجيل دخول العميل. أضاف بند 11 جديد لـCLAUDE.md
- **2026-07-14** — فحص شامل نهائي: كل الأعمدة الحساسة بكل الجداول (`admin_users`, `customers`, `salons`, `fcm_tokens`, `notification_logs`, `user_sessions`) فُحصت دفعة وحدة مقابل صلاحيات الجدول الكامل — النتيجة: فخّ "منح جدول كامل" محصور بـ`customers.pin_hash` بس، بقية الجداول نظيفة ومؤكدة (مو افتراض)
- **2026-07-14** — إصلاح `customers.pin_hash` (REVOKE جدول + GRANT عمودي) طُبِّق ومؤكد على **DAWRAK-TEST** أيضاً — بعد مقارنة أعمدة مطابقة 100% بين المشروعين. DAWRAK وDAWRAK-TEST متطابقان تماماً على هذي النقطة الآن
- **2026-07-14** — اكتمل جرد Tier 2 لكل الجداول الأربعة (`customers`, `bookings`, `reviews`, `promotions`/`promo_codes`): الاثنين الأولين خطورتهما بيانات شخصية (عولجت جزئياً)، والاثنين الأخيرين خطورتهما مالية/تشغيلية (`owner_reply` تلاعب، إساءة استخدام أكواد خصم) — لا بيانات شخصية بهما. الجرد الكامل جاهز لجلسة Tier 2 المخصّصة القادمة
- **2026-07-14** — تحديث `next@14.2.5→14.2.35` بلوحة الإدارة — يغلق الثغرة الحرجة (تجاوز صلاحيات)، مبني ومختبر بنجاح (build + type-check نظيفين)، مرفوع على `main`. اكتُشف أثناء التطبيق إن بقية ثغرات Next.js (DoS/XSS) تحتاج قفزة إصدار رئيسي (next@16) لا مجرد patch كما افترضنا — أُجِّلت لجلسة منفصلة مع firebase@12
- **2026-07-15** — أول اختبار حي لنقطة #28 (استعادة رمز الصالون) كشف 3 فجوات بنية تحتية: قوالب بريد Supepbase مقفولة بدون SMTP خارجي، طول OTP غير متطابق (8 مقابل 6)، وقبول نفس الرمز القديم كـ"جديد". الثلاثة أُصلحت (SMTP خارجي عبر Resend + تعديل قالب البريد + تصحيح طول OTP + فحص كودي يمنع تكرار نفس الرمز) — الميزة تعمل حياً بنجاح كامل الآن. ⏳ الرسائل لسا تصل Spam (يحتاج توثيق نطاق بريد خاص، مؤجَّل)
- **2026-07-15** — إضافة مؤقت انتظار (دقيقتين) قبل السماح بإعادة إرسال كود التحقق لاستعادة الرمز السري (L126) — لشاشتي الصالون والعميل، يمنع تضارب أكواد متعددة
- **2026-07-15** — 🔴→✅ اكتشاف حرج وإصلاح فوري: تبويب "رقم الجوال" بدخول العميل كان يسجّل الدخول بمجرد معرفة الجوال، بدون أي PIN — استيلاء كامل محتمل على أي حساب عميل. أُصلح (L127): يطلب PIN إجبارياً الآن، ويتحقق منه بالسيرفر عبر `/api/customer-auth` (rate limiting + قفل بعد 5 محاولات)
- **2026-07-15** — اكتشاف إضافي: "دخول بالبصمة" لا يستخدم WebAuthn حقيقي، فقط يتذكر آخر مستخدم بهذا الجهاز — تسمية مضلّلة، مسجّلة كنقطة معلّقة غير عاجلة
- **2026-07-15** — تحسينات مرافقة (L128): حذف تبويب "دخول سريع" (كان يتحقق محلياً بدون rate limiting — أضعف من التبويب الجديد)، نقل Google لحساب جديد فقط، تفعيل التعبئة التلقائية (نفس مكوّن `OtpInput`) لاستعادة رمز الصالون — الثلاث شاشات (تسجيل، استعادة عميل، استعادة صالون) توحّدت على نفس المكوّن التقني الآن
- **2026-07-15** — 🔴→✅ اكتشاف وإصلاح جذري (L129): نقطة 29 (استعادة رمز العميل) كانت ترفض الكود الصحيح باستمرار بسبب تصادم صامت — `api/customer-auth.js`/`api/owner-auth.js` يولّدان بصمت كود `magiclink` بكل دخول جوال+PIN ناجح لإصدار جلسة Supabase (Tier 2)، وهو **نفس خانة التوكن** التي يستخدمها `signInWithOtp` بشاشة "نسيت الرمز السري" لنفس البريد — أي دخول عادي كان يُبطل صامتاً كود الاستعادة قيد الانتظار. أُصلح بتغيير نوع التوليد الصامت لـ`recovery` (خانة منفصلة تماماً). أضاف بند 12 جديد لـCLAUDE.md. ⏳ بانتظار إعادة اختبار حي نظيفة
- **2026-07-15** — 🔴→✅ إصلاح ثانٍ (L130): بعد نجاح L129 (الكود اتحقق بنجاح فعلياً)، ظهر خطأ جديد "لا يوجد حساب بهذا الرقم" — استعلام `customers` بحث بالبريد بمطابقة حساسة لحالة الأحرف (`eq.`) بينما Supabase Auth غير حساس لحالة الأحرف، والبريد يُخزَّن كما كُتب حرفياً وقت التسجيل بدون تطبيع. أُصلح باستخدام `ilike.` بدل `eq.`
- **2026-07-15** — ✅ نقطة 29 (استعادة رمز العميل) **مختبرة حياً بنجاح كامل** — الفشل الثالث (بعد L129+L130) تبيّن إنه بيانات اختبار خاطئة (حساب تجريبي مسجَّل ببريد وهمي `test-customer@dork.internal` بدل بريد أحمد الحقيقي)، مو خللاً بالكود — صُحِّح البريد يدوياً ونجحت العملية كاملة
- **2026-07-15/16** — إعادة تسمية العلامة التجارية الكاملة DORK ← DAWRAK (شعار، هيدر، لوحة إدارة، توثيق، مشروعَي Supabase نفسهم) — راجع بند 26 بملف الاتفاقيات للتفاصيل
- **2026-07-16** — بدء نقاش Tier 2: اكتشاف `loadData()` (`App.jsx:1844`) تجيب كل العملاء/الحجوزات بلا فلتر لأي زائر — تحقّق حي (F12/Network) أكّد الثغرة حقيقية بالكود لكن **الضرر الفعلي صفر حالياً** (لا يوجد عميل حقيقي ثانٍ بعد). أُعيد تصنيف الأولوية لـ"قبل انضمام عميل حقيقي ثانٍ" بدل "طارئ الآن"
- **2026-07-16** — 🔴→✅ اكتشاف وإصلاح واسع منفصل: `authenticated` role كان بلا أي GRANT على 5 جداول (`promotions`, `messages`, `notifications`, `promo_codes`, `waiting_list`) رغم إن `anon` عنده وصول كامل — أي مستخدم مسجّل دخول (بعد تفعيل الجلسات الحقيقية بـTier 2 مرحلة 1) يواجه 403 صامت على هذي الميزات. اكتُشف بالصدفة (عطل "العروض الترويجية")، ثم فحص استباقي شامل لكل الجداول كشف الباقي دفعة وحدة. أُصلح ومؤكَّد على DAWRAK وDAWRAK-TEST معاً (صفر فجوات متبقية بالمشروعين). أضاف بند 13 جديد لـCLAUDE.md. ⏳ متبقٍ: اختبار حي للميزات الأربع
- **2026-07-16** — حذف كود قديم غير آمن (L134): `savePin()` كان يحاول PATCH ميت لعمودي `pin`/`pin_length` عبر anon (يفشل 403 دائماً بصمت) — المسار الآمن الحقيقي عبر `/api/customer-auth` كان يعمل فعلاً، فحُذف السطر الميت
