# تقرير أمان RLS — Dawrak App

> **الغرض:** تتبّع ثغرات RLS المكتشفة على مشروع Sydney (الإنتاج الحالي) وحالة إصلاح كل واحدة.
> **كل نقطة تبدأ بـ 🔲 لم تُصلح بعد — تتحول لـ ✅ بعد تشغيل SQL الإصلاح والتأكد.**
> **تاريخ الاكتشاف:** 2026-07-02 — أثناء تحضير نقل RLS لمشروع Mumbai.

---

## كيف اكتُشفت المشكلة

أثناء محاولة تطبيق ملفات RLS (`production-rls-v2.sql`, `secure-rls-policies.sql`, `fix-bookings-rls.sql`) على مشروع Mumbai، تبيّن أنها مبنية على متغيرات جلسة (`current_setting('app.session_token'...)`) غير مستخدمة أبداً بالتطبيق الفعلي. هذا قاد لفحص شامل لحالة RLS الحقيقية على **Sydney (الإنتاج)** عبر:

```sql
SELECT tablename, policyname, cmd, roles, qual FROM pg_policies WHERE schemaname='public' ORDER BY tablename, cmd;
```

النتيجة: عدة جداول فيها سياسات باسم `allow_all`/`public_all`/`admin_full_access` مُعطاة لدور `{public}` بشرط `true` — أي قراءة/تعديل/حذف مفتوح للجميع بدون أي هوية.

---

## جدول الحالة

| # | الجدول | المشكلة | الخطورة | الحالة |
|---|--------|---------|---------|--------|
| 1 | `customers` | سياسة `public_all` (ALL/public/true) — أي شخص يقرأ/يعدّل/**يحذف** أي عميل | 🔴 حرجة | ✅ أُصلح 2026-07-02 |
| 2 | `bookings` | سياسة `allow_all` (ALL/public/true) — أي شخص يقرأ/يعدّل/**يحذف** أي حجز | 🔴 حرجة | ✅ أُصلح 2026-07-02 |
| 3 | `reviews` | سياسة `allow_all` (ALL/public/true) — أي شخص يقرأ/يعدّل/**يحذف** أي تقييم | 🔴 حرجة | ✅ أُصلح 2026-07-02 |
| 4 | `promo_codes` | سياسة `admin_full_access` مُعطاة لـ `{public}` — أي شخص ينشئ/يحذف أكواد خصم | 🔴 حرجة (مالية) | ✅ أُصلح 2026-07-02 |
| 5 | `notifications` | `notifications_public_delete` + `notifications_public_update` مفتوحة للجميع — أي شخص يحذف/يعدّل إشعارات كل المستخدمين | 🟠 متوسطة | ✅ أُصلح 2026-07-02 |
| 6 | `customer_messages` / `messages` | قراءة عامة (`SELECT true`) لمحادثات خاصة بين عميل وصالون / صالون وأدمن | 🟠 متوسطة | 🟡 مؤجل — Tier 2 |
| 7 | `promotions` | `update own promotion` غير مقيّدة فعلياً (`true`) — أي صالون يقدر يعدّل عرض صالون ثاني | 🟡 منخفضة | 🟡 مؤجل — Tier 2 |
| 8 | `waiting_list` | `waiting_list_public_delete` مفتوحة، لكنها ميزة حقيقية مستخدمة بالتطبيق | 🟡 منخفضة | 🟡 مؤجل — Tier 2 |
| 9 | `salons` | محمية بشكل صحيح (`SELECT` مقيّد بـ `status='approved'` + `service_role` للباقي) | ✅ آمنة | ✅ نموذج يُحتذى به |

---

## Tier 1 — إصلاح طارئ آمن (جاهز للتشغيل)

تم فحص كل استخدام فعلي لـ `sb(...)` بـ `App.jsx` قبل كتابة هذا السكربت، للتأكد إنه ما يكسر أي ميزة شغالة:

```sql
-- customers
DROP POLICY IF EXISTS "public_all" ON customers;
CREATE POLICY "customers_public_select" ON customers FOR SELECT USING (true);
CREATE POLICY "customers_public_update" ON customers FOR UPDATE USING (true);

-- bookings
DROP POLICY IF EXISTS "allow_all" ON bookings;
CREATE POLICY "bookings_public_update" ON bookings FOR UPDATE USING (true);

-- reviews
DROP POLICY IF EXISTS "allow_all" ON reviews;
CREATE POLICY "reviews_public_update" ON reviews FOR UPDATE USING (true);

-- notifications
DROP POLICY IF EXISTS "notifications_public_delete" ON notifications;
DROP POLICY IF EXISTS "notifications_public_update" ON notifications;

-- promo_codes
DROP POLICY IF EXISTS "admin_full_access" ON promo_codes;
CREATE POLICY "promo_codes_public_update" ON promo_codes FOR UPDATE USING (true);
```

**آثار جانبية مقبولة (مفحوصة ومقصودة):**
- زر "حذف حسابي" للعميل (`App.jsx` تقريباً سطر 2749) سيتوقف عن العمل مؤقتاً — يحتاج مسار API آمن بديل لاحقاً.
- "تعليم الكل كمقروء" و"مسح الكل" بالإشعارات لن يُحفظا بقاعدة البيانات مؤقتاً (يبقيان شغالين محلياً بالمتصفح فقط، والإشعارات القديمة قد ترجع تظهر بعد تحديث الصفحة).

**لن تتأثر:** إنشاء حجز، تحديث حالة حجز، تسجيل عميل جديد، كتابة/قراءة تقييم، رد الصالون على تقييم، استخدام كود خصم.

---

## Tier 2 — يحتاج نظام هوية حقيقي (لاحقاً)

المشكلة المتبقية بعد Tier 1: **القراءة والتعديل لسا مفتوحين للجميع** على `customers`/`bookings`/`reviews`/`promo_codes` (بس مو الحذف). ما نقدر نقيّدهم بـ "كل مستخدم يشوف بياناته بس" لأن ما فيه هوية حقيقية مربوطة بكل طلب — التطبيق يستخدم anon key عام بدون توكن جلسة فعلي.

**الحل الحقيقي مرتبط بنقطة 86 الموجودة أصلاً بـ `الفحص-الشامل.md`:**
- تشغيل `node scripts/migrate-salon-auth.js` لربط الصالونات بـ Supabase Auth الحقيقي (`auth.uid()`)
- تمديد نفس المبدأ للعملاء
- ربط `sb()` بإرسال `authToken` فعلي (حالياً معامل ميت — موجود بالكود لكن ما يُمرَّر له أي قيمة أبداً)
- إعادة كتابة سياسات RLS لتعتمد على `auth.uid()` بدل `true`

هذا مشروع كامل، مو ترقيع SQL — يُنفَّذ بجلسة منفصلة بعد التنسيق معك.

**قرار أحمد (2026-07-02):** تأجيل Tier 2 عن قصد — لا يوجد عملاء حقيقيون حالياً على Sydney (كل الحسابات التجريبية أرقام أحمد الشخصية)، فخطورة القراءة المفتوحة منخفضة عملياً بالوقت الحالي. يُنفَّذ لاحقاً، والأنسب دمجه ضمن إعداد Mumbai (بيئة بدون مستخدمين حقيقيين أصلاً، مكان آمن لبنائه واختباره) قبل أي تحويل نهائي لـ Vercel.

---

## سجل التنفيذ

### 2026-07-02 — Tier 1 نُفّذ على Sydney ✅

شُغّل SQL الإصلاح الطارئ كامل (customers/bookings/reviews/notifications/promo_codes) بنجاح ("Success. No rows returned"). تحقق مباشر بعدها عبر `SET ROLE anon` أكّد قراءة سليمة على الثلاثة جداول (customers=3, bookings=55, reviews=8).

**اكتشاف جانبي غير متوقع أثناء الاختبار:** بعد الإصلاح، ظهر خطأ "قاعدة بيانات" بالتطبيق الحي — التحقيق كشف مشكلة **منفصلة تماماً وسابقة لهذا الإصلاح**: ملف `20260621_lockdown_salons_rls.sql` (بتاريخ 21 يونيو) منح `anon` صلاحية `SELECT` على أعمدة محددة فقط بجدول `salons`، ولم يشمل عمود `owner_email` الذي أُضيف لاحقاً لاستعلام `App.jsx` (commit `d4bbe0d`). أي عمود غير مصرَّح به ضمن استعلام SELECT يُسقط الاستعلام كاملاً في PostgreSQL — هذا عطّل قراءة الصالونات بالكامل لدور `anon` منذ نزول ذاك الـcommit، بشكل مستقل تماماً عن ثغرات RLS المذكورة أعلاه.

**الإصلاح:** `GRANT SELECT (owner_email) ON salons TO anon;` — نُفّذ ونجح.

**التحقق النهائي (اختبار حي كامل من أحمد):** حجز تجريبي من صفحة العميل → ظهر بلوحة الصالون → ظهر بصفحة "حجوزاتي" للعميل. كل شي يعمل بدون أخطاء.

**الحالة النهائية:** Tier 1 مكتمل بالكامل + إصلاح إضافي لمشكلة salons غير مرتبطة. Tier 2 (نظام auth.uid() الحقيقي) لسا مؤجل، مرتبط بنقطة 86.

### 2026-07-03 — اكتشاف وإصلاح: ميزة قائمة الانتظار معطوبة بالكامل على Sydney ✅

أثناء بدء العمل على RLS مشروع Mumbai، طلب أحمد نتحقق من ميزة "قائمة الانتظار" لأنه يتذكرها شغالة سابقاً. الاختبار الحي كشف خطأ حقيقي (`حدث خطأ، حاول مرة أخرى`) عند محاولة الانضمام. فحص Console + Network أظهر:

```
Proxy-Status: PostgREST; error=42501
```

نفس نمط مشكلة `salons` (عمود `owner_email`) لكن أشمل: فحص `information_schema.column_privileges` أظهر أن دور `anon` عنده على جدول `waiting_list` صلاحية **`REFERENCES` فقط على كل الأعمدة — صفر SELECT/INSERT/UPDATE/DELETE**. يعني سياسات RLS (`waiting_list_public_read/insert/delete`) كانت موجودة **لكن بلا أي صلاحية أساسية (GRANT) تُفعّلها** — الميزة كانت معطوبة بالكامل (قراءة، إضافة، حذف) من الأساس، وليس فقط "قبول/رفض" كما افترضنا أول مرة.

**الإصلاح:**
```sql
GRANT SELECT, INSERT, UPDATE, DELETE ON waiting_list TO anon;
CREATE POLICY "waiting_list_public_update" ON waiting_list FOR UPDATE USING (true);
```

**تحقق حي كامل:** انضمام لقائمة الانتظار نجح ✅ → قبول من لوحة الصالون نجح ✅ → إشعار "تم قبولك في الموعد" وصل فوراً للعميل ✅ → القائمة تفرّغت والحجز انتقل لتبويب "مقبول" تلقائياً ✅.

**درس منهجي مهم لبقية العمل:** *وجود سياسة RLS لا يعني وجود GRANT الأساسي.* هذا ثاني اكتشاف من هذا النوع بنفس اليوم (بعد salons) — أي عمل RLS مستقبلي (خصوصاً على Mumbai) يجب يتحقق من `information_schema.column_privileges` أو `role_table_grants` **بالإضافة إلى** `pg_policies`، لا يكفي فحص السياسات وحدها.

### 2026-07-03 — 🔴 عطل مؤقت بالإنتاج (Sydney): سياسة salons مقيّدة بدور `anon` فقط، سببه تعديل اليوم على `sb()`

بعد نشر تعديل `sb()` (استخدام جلسة Supabase الحقيقية تلقائياً — راجع الخطوة 3 بـ`النقاط-المتبقية.md`)، اختفت **كل** الصالونات من التطبيق الحي فجأة. التشخيص:

- سياسة `salons_public_select_approved` (من `20260621_lockdown_salons_rls.sql`) مقيّدة صراحة **`TO anon`** فقط — لا يوجد أي سياسة لدور `authenticated`.
- بمجرد ما صار عند أي متصفح جلسة Supabase حقيقية (بعد اختبار دخول عميل بـ PIN)، `sb()` الجديدة بدأت تبعث توكن الجلسة الحقيقي بدل مفتاح anon **حتى لتصفح الصالونات العام**.
- الدور `authenticated` ما عنده أي سياسة RLS تسمح برؤية `salons` → صفر نتائج (200 OK لكن `[]`) — مش خطأ صريح، فصعب اكتشافه بسرعة.

**الإصلاح (نُفّذ على Sydney، ونفس الشي لازم يُنفَّذ على Mumbai قبل أي تحويل Vercel):**
```sql
GRANT SELECT (
  id, name, owner, owner_phone, owner_email, region, gov, center, village,
  phone, address, location_url, services, prices,
  shift_enabled, shift1_start, shift1_end, shift2_start, shift2_end,
  work_start, work_end, barbers, tone, rating, status, paused,
  frozen, banned, welcome_msg, closed_days, slot_min,
  cancellation_window, total_paid, social, lang, created_at
) ON salons TO authenticated;

CREATE POLICY "salons_authenticated_select_approved" ON salons
  FOR SELECT TO authenticated USING (status = 'approved');
```

**تحقق حي:** تحديث الصفحة بعد التنفيذ أرجع كل الصالونات للظهور فوراً.

**✅ تم تطبيق نفس الإصلاح على Mumbai (2026-07-03) وتحقّق مزدوج:**
- `pg_policies` أكّدت وجود `salons_authenticated_select_approved` (`{authenticated}`, SELECT, `status='approved'`) بجانب `salons_public_select_approved` (`{anon}`) و`salons_service_role_all` (`{service_role}`, ALL)
- `information_schema.column_privileges` أكّدت GRANT SELECT لدور `authenticated` على كل الأعمدة الـ35 المطلوبة بدون نقص
- ما عاد فيه فرق بين Sydney وMumbai بخصوص هذي الثغرة — الطريق مفتوح لمتابعة تحويل Vercel

**درس إضافي:** أي سياسة RLS مستقبلية يجب تُكتب `USING (...)` بدون `TO anon` تحديداً (أو تُضاف نسخة مطابقة لـ`authenticated`) إلا إذا كان القصد صراحة تقييدها بدور واحد — القراءة العامة (كالصالونات المعتمدة) يجب تشتغل بغض النظر عن وجود جلسة مصادقة أو لا.

---

### 2026-07-03 — 🔴 تحويل Vercel لـ Mumbai: سلسلة أعطال إضافية واكتشاف GRANT ناقص لـ anon

بعد تحديث متغيرات Vercel (`VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON`, `SUPABASE_URL`, `SUPABASE_ANON`, `SUPABASE_SERVICE_ROLE_KEY`) لتشير لـ Mumbai وعمل Redeploy، ظهر خطأ `401 Invalid API key` بدل نجاح فوري. التشخيص مرّ بعدة مراحل:

1. **خطأ بشري:** قيمة `VITE_SUPABASE_ANON` انلصقت بخانة **Note** بالغلط بدل خانة **Value** بواجهة Vercel — الحل: نقلها للخانة الصحيحة.
2. **صيغة مفتاح خاطئة:** استُخدم مفتاح anon من تبويب **"Legacy anon, service_role API keys"** (صيغة JWT قديمة)، بينما Sydney (والمفروض Mumbai كمان) يستخدم نظام **"Publishable and secret API keys"** الجديد (`sb_publishable_...`) — تبيّن Mumbai يرفض صيغة legacy تماماً.
3. **قيمة Vercel متضاربة مع fallback الكود:** حتى بعد كتابة القيمة الصحيحة كـ fallback بالكود (`App.jsx`, `api/_lib/supabase-admin.js`, `api/supabase.js`)، القيمة القديمة/الخاطئة المحفوظة بـ`VITE_SUPABASE_ANON`/`SUPABASE_ANON` على Vercel كانت **تتغلب** على fallback الكود (لأن `import.meta.env.X || fallback` ما يرجع لـ fallback إلا لو X فاضية تماماً) — الحل: حذف المتغيرين نهائياً من Vercel والاعتماد على fallback الكود فقط.
4. **بعد حل مشكلة المفتاح:** ظهر خطأ جديد ومختلف تماماً — `42501 permission denied for table salons`، مع hint من Postgres نفسه: `GRANT SELECT ON public.salons TO anon`. هذا أثبت المفتاح صار صحيح 100% (وصل الطلب لقاعدة البيانات فعلاً)، لكن GRANT العمودي لدور `anon` على Mumbai كان ناقصاً أو غير مكتمل رغم إنه من المفترض طُبّق بالخطوة 1 الأصلية.

**الإصلاح النهائي (Mumbai):**
```sql
GRANT SELECT (
  id, name, owner, owner_phone, owner_email, region, gov, center, village,
  phone, address, location_url, services, prices,
  shift_enabled, shift1_start, shift1_end, shift2_start, shift2_end,
  work_start, work_end, barbers, tone, rating, status, paused,
  frozen, banned, welcome_msg, closed_days, slot_min,
  cancellation_window, total_paid, social, lang, created_at
) ON salons TO anon;
```
⚠️ **لم نطبّق** اقتراح Postgres الحرفي (`GRANT SELECT ON public.salons TO anon` بدون تحديد أعمدة) لأنه يكشف أعمدة سرية مثل `owner_pin_hash` — طُبّق GRANT عمودي محدود بنفس منطق كل الإصلاحات السابقة.

**تحقق نهائي:** `information_schema.column_privileges` أكّدت كل الأعمدة المطلوبة موجودة لـ`anon`، وطلب حي من المتصفح (Network tab) رجع **200 OK** مع بيانات صالون "الوادي" الحقيقي — أول اتصال ناجح فعلي بين التطبيق المنشور فعلياً (Vercel Production) وقاعدة Mumbai.

5. **بعد حل anon تماماً:** تسجيل دخول الصالون فشل بـ"صالون غير موجود" رغم إن البيانات صحيحة 100% بالجدول (تأكدنا بـ SQL مباشر: `owner_phone` مطابق تماماً). أضفنا `console.error` مؤقت بـ `api/owner-auth.js` وفحصنا Vercel Logs → كشف الخطأ الحقيقي: `42501 permission denied for table salons`, hint: `GRANT SELECT ON public.salons TO service_role`. **حتى دور service_role (الموثوق بالكامل، يُستخدم فقط من السيرفر) ما كان عنده GRANT أساسي على الجداول بمشروع Mumbai** — نفس فخ "سياسة بدون GRANT" يتكرر لثالث مرة اليوم، بس هذي المرة أخطر لأنه يؤثر غالباً على **كل الجداول** مو جدول وحيد.

**الإصلاح الشامل (كل الجداول، مرة واحدة):**
```sql
GRANT ALL ON ALL TABLES IN SCHEMA public TO service_role;
GRANT ALL ON ALL SEQUENCES IN SCHEMA public TO service_role;
ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT ALL ON TABLES TO service_role;
ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT ALL ON SEQUENCES TO service_role;
```
لا داعي لتقييد أعمدة هنا — service_role دور سيرفر موثوق بالكامل ويحتاج أصلاً كل الأعمدة (مثل `owner_pin_hash` للتحقق من الدخول).

**تحقق نهائي:** `information_schema.table_privileges` أكّدت صلاحيات كاملة لـ`service_role` على كل الجداول، وتسجيل دخول صالون "الوادي" الحي نجح بالكامل (لوحة التحكم ظهرت صح بعد الدخول).

**✅ الخلاصة: Mumbai أصبح فعلياً هو الإنتاج الحي — التطبيق المنشور بـ Vercel يقرأ ويكتب من Mumbai بنجاح (تصفح عام + تسجيل دخول صالون مُختبران حياً).**

**درس رئيسي متكرر اليوم (3 حالات منفصلة: `salons`/anon، ثم service_role):** إعداد RLS الكامل على مشروع جديد يحتاج فحص **GRANT منفصل تماماً عن السياسات** لكل دور مُستخدم فعلياً بالكود (`anon`, `authenticated`, `service_role`) — لا يكفي التأكد من وجود Policy، ولا حتى من نجاح استعلام SQL يدوي بنفس الجلسة (لأن SQL Editor غالباً يشتغل كـ `postgres`/`service_role` بصلاحيات كاملة أصلاً، فما يكشف نقص GRANT لباقي الأدوار).

**درس رئيسي:** استخدام fallback بالكود (بدل الاعتماد الكامل على Vercel env vars) ساعد نعزل المشكلة خطوة بخطوة، لكنه كشف أيضاً إن **وجود متغير فاضي/خاطئ بـ Vercel أخطر من عدم وجوده أصلاً** — لأنه يتغلب على أي fallback صحيح بالكود بصمت تام بدون أي تحذير.

---

### 2026-07-04 — الاختبار الشامل الحي على Mumbai: 3 أعطال إضافية (Site URL + schema مفقود + sequences)

بعد نجاح تحويل Vercel، بدأنا الاختبار الشامل الفعلي (Step 5): تسجيل عميل جديد → تسجيل دخول → حجز كامل. واجهنا 3 مشاكل جديدة، كلها انحلّت بنفس منهجية اليوم (فحص مباشر بدل تخمين):

**1. روابط بريد التحقق (OTP) تودّي لـ`localhost` بدل الموقع الحقيقي**
- **السبب:** إعداد **Site URL** بـ Supabase Auth (Authentication → URL Configuration) لمشروع Mumbai كان لسا على القيمة الافتراضية `http://localhost:3000` — لم يُحدَّث أبداً منذ إنشاء المشروع.
- **الإصلاح:** تغييره لـ `https://dork-app.vercel.app` + إضافة نفس الرابط (مع `/**`) لقائمة **Redirect URLs**.
- **تحقق حي:** تسجيل عميل جديد بالبريد نجح بعدها مباشرة.

**2. فروقات Schema حقيقية بين Sydney وMumbai — اكتُشفت بمقارنة شاملة**
بدل انتظار كل ميزة تكشف عطل جديد بالصدفة (زي ما صار مع `salons` و`service_role`)، قارنّا **كامل** هيكلة الجدولين (`information_schema.columns`) دفعة وحدة. النتيجة: 326 عمود بـSydney مقابل 262 بـMumbai. بعد استبعاد الجداول/الأعمدة غير المستخدمة فعلياً بالكود (تحقّقنا بـ`grep` مباشر، ليس تخميناً) — 3 فروقات حقيقية:

| الجدول | المشكلة | الحل |
|---|---|---|
| `customers` | 8 أعمدة ناقصة: `favs`, `google_uid`, `history`, `lang`, `location_lat`, `location_lng`, `notifications`, `photo` | `ALTER TABLE ADD COLUMN` لكل عمود + `GRANT SELECT/UPDATE` لـ`anon`/`authenticated` |
| `app_settings` | بنية مختلفة جذرياً (كان `key`/`value` بدل `id`/`loyalty_settings`/`social_links`/`ui_settings`) | إضافة الأعمدة المطلوبة (الجدول كان فارغاً أصلاً على Mumbai فما احتجنا حذف القديم) |
| `reviews` | عمود `owner_reply` ناقص (يُستخدم فعلياً لرد الصالون على التقييم) | `ALTER TABLE ADD COLUMN` + GRANT |

**غير مهم (تحقّقنا وتجاهلناها بأمان):** `financial_records` و`notification_logs` (جداول موجودة بSydney لكن صفر استخدام بالكود الحالي)، `salons_backup_auth_migration` (جدول نسخة احتياطية قديم، ليس جزءاً من التطبيق)، `salons.oath_done`/`salons.bookings(jsonb)` (أعمدة قديمة صفر استخدام، قرار مأخوذ سابقاً وقت نقل صالون 17).

**3. فخ الصلاحيات يتكرر — بس هذي المرة على Sequences وليس الجداول**
حتى بعد GRANT صحيح على الجداول (`bookings`, `reviews`, `app_settings`)، محاولة **إضافة** صف جديد فشلت برسالة: `permission denied for sequence bookings_id_seq`. الأعمدة التلقائية (bigint/bigserial) تعتمد على **sequence منفصل** يحتاج صلاحية GRANT خاصة به، غير كافية صلاحية الجدول نفسه.
```sql
GRANT USAGE, SELECT ON bookings_id_seq TO anon, authenticated;
GRANT USAGE, SELECT ON app_settings_id_seq TO anon, authenticated;
```

**تحقق نهائي شامل:** حجز عميل تجريبي كامل (اختيار حلاق نشط + خدمة + موعد + تأكيد) نجح فعلياً على Mumbai، وتأكّدنا بـSQL مباشر من وجود الصف بجدول `bookings` ببيانات صحيحة تماماً.

---

### 2026-07-07 — نسخ الهيكلة الكاملة من DAWRAK (الإنتاج) إلى DAWRAK-TEST (فارغ)

**الهدف:** DAWRAK-TEST أُنشئ فارغاً تماماً (2026-07-06) — نبيه جاهز فعلياً للاختبار، فلازم يطابق هيكلة DAWRAK بالكامل: جداول، أعمدة، قيود، فهارس، دوال، triggers، RLS + policies، وصلاحيات GRANT.

**بدون Terminal (بقرار أحمد)** — الطريقة: استعلام SQL واحد يقرأ كتالوج Postgres (`pg_catalog`/`information_schema`) على DAWRAK وينتج نص SQL كامل جاهز للنسخ، يُشغَّل بعدها كاملاً على DAWRAK-TEST.

**جرد أولي على DAWRAK (2026-07-07) قبل بناء السكربت النهائي:**

| tables | views | functions | triggers | policies | sequences |
|---|---|---|---|---|---|
| 26 | 1 | 10 | 7 | 45 | 20 |

**قرار تصميم مهم:** أي عمود بمفتاح تلقائي (`DEFAULT nextval(...)`) يُعاد كتابته كـ`GENERATED BY DEFAULT AS IDENTITY` بدل نسخ اسم الـ sequence كما هو — يضمن توليد sequence صحيح تلقائياً بـDAWRAK-TEST بدون تعارض أسماء، ويطابق نفس تسمية Postgres التلقائية (`{table}_{column}_seq`) المستخدمة أصلاً بحل مشكلة `bookings_id_seq` أعلاه.

**اكتشاف مهم أثناء المراجعة قبل التطبيق:** عدّ أعمدة `id` (bigint) المعتمدة على sequence تلقائي أعطى 21 عموداً، لكن الجرد الأولي قال 20 sequence فقط على DAWRAK — فرق واحد. التحقق (`pg_get_expr` على كل عمود `id`) أكّد السبب: **`bookings_archive.id` يشارك نفس sequence حق `bookings.id`** (`bookings_id_seq`) — الأرقام تكمل متسلسلة بين الجدولين حتى بعد الأرشفة، وليس كل جدول له sequence مستقل كما افترضنا أول مرة.

**الإصلاح بالسكربت النهائي:** بدل تحويل كل عمود `nextval(...)` تلقائياً لـ`GENERATED BY DEFAULT AS IDENTITY` (يفصل كل جدول بـsequence مستقل خاص فيه)، الكود عدّل ليعامل `bookings`/`bookings_archive` استثناءً: `CREATE SEQUENCE bookings_id_seq` صريح قبل الجداول، وكلا العمودين يستخدمان `DEFAULT nextval('bookings_id_seq'::regclass)` (وليس IDENTITY) — يحافظ على نفس المشاركة الموجودة فعلياً بالإنتاج. باقي الـ19 عمود (بلا مشاركة) استخدمت `GENERATED BY DEFAULT AS IDENTITY` بأمان.

**السكربت النهائي (1148 سطر) يغطي:** 10 دوال، 26 جدول (+ sequence مشترك واحد لـbookings/bookings_archive)، view واحد، كل القيود (PK/Unique/FK/Check)، الفهارس، 7 triggers، تفعيل RLS + 45 policy، وكل صلاحيات GRANT (جدول/عمود/sequence) لـanon/authenticated/service_role — أُرسل لأحمد كملف جاهز للصق كامل على DAWRAK-TEST.

**التنفيذ (2026-07-07):**
1. تشغيل السكربت الرئيسي (10 دوال، 26 جدول، view، القيود، الفهارس، 7 triggers، RLS + 45 policy، كل GRANTs) — نجح من أول محاولة بعد إصلاح ترتيب الأقسام (كان قسم الدوال يشتغل قبل الجداول؛ دالتين منها بلغة SQL خالصة تتحقق من وجود الجداول وقت الإنشاء مباشرة — أُعيد الترتيب: جداول → عروض → دوال → قيود → فهارس → triggers → RLS → GRANTs).
2. **مقارنة الجرد الكامل بعد التطبيق** (`pg_class`/`pg_proc`/`pg_trigger`/`pg_policy`) — تطابق 100%: 26 جدول، 1 view، 10 دوال، 7 triggers، 45 policy، 20 sequence.
3. **اكتُشف فرق بالصلاحيات (GRANT)** رغم تطابق الهيكلة: DAWRAK-TEST عنده صلاحيات أوسع افتراضياً (مشروع Supabase جديد يمنح CRUD كامل لـ`anon`/`authenticated` تلقائياً على أي جدول جديد) — 184 صلاحية زايدة بمستوى الجدول، 131 بمستوى العمود (تغطي 1195 عمود فردي)، 112 بمستوى الـsequence، مقارنة بـDAWRAK. تم توليد ودقّ REVOKE statements دقيقة (مقارنة `information_schema.role_table_grants`/`column_privileges` وaclexplode على pg_class.relacl حرفياً بين المشروعين، مو تخميناً) وتطبيقها.
4. **فخ إضافي اكتُشف أثناء REVOKE:** لما تُسحب صلاحية على **مستوى الجدول** (table-level) لدور كان يملك فقط صلاحية عمود-محدد ضمنية عبرها (مو صلاحية عمود صريحة منفصلة)، تختفي الصلاحية بالكامل — حتى لو كان المطلوب إبقاء نسخة مقيّدة بأعمدة معيّنة. صار هذا بالضبط مع 5 حالات كانت DAWRAK تُخفي فيها أعمدة حساسة عمداً عن anon/authenticated (`salons.password`, `salons.owner_pin_hash`, `salons.owner_pin_locked_until`, `salons.auth_uid`؛ وتقييد `customers` لـauthenticated على أعمدة تطبيق فقط لا بيانات حساسة) — انحلّت بإعادة GRANT صريح لنفس مجموعة الأعمدة المقيّدة بالضبط.
5. **التحقق النهائي (بعد كل الإصلاحات):** `role_table_grants` = 383، `column_privileges` (مفاتيح فريدة) = 198، `sequence` ACL = 68 — **تطابق تام 100% مع DAWRAK** على الثلاث مستويات.

**الحالة: ✅ مكتمل بالكامل 2026-07-07 — DAWRAK-TEST نسخة طبق الأصل من DAWRAK هيكلةً وصلاحياتٍ.**

---

### 2026-07-07 — خلل قديم مكتشَف بالصدفة: `promo_codes` بدون GRANT/Policy لـ INSERT/DELETE على DAWRAK

أثناء استخدام أحمد الفعلي للوحة الإدارة (توليد كود خصم تطبيق)، ظهرت رسالة **"permission denied for table promo_codes"**. هذا خلل قديم موجود بالإنتاج نفسه (DAWRAK) — **غير متعلق بترحيل Mumbai ولا بشغل DAWRAK-TEST اليوم**، انكشف بالصدفة لأنه أول مرة يجرَّب توليد كود فعلياً.

**السبب:** صفحة `admin/src/app/(admin)/promo-codes/page.tsx` تتصل مباشرة بقاعدة البيانات عبر مفتاح `anon` (`sb` من `supabase-browser.ts`)، ودور `anon` كان يملك فقط `SELECT`/`UPDATE` على `promo_codes` — بدون `INSERT` ولا `DELETE` (لا GRANT ولا RLS policy).

**الإصلاح المطبَّق على DAWRAK:**
```sql
GRANT INSERT, DELETE ON public.promo_codes TO anon;

CREATE POLICY promo_codes_public_insert ON public.promo_codes
  FOR INSERT TO public WITH CHECK (true);

CREATE POLICY promo_codes_public_delete ON public.promo_codes
  FOR DELETE TO public USING (true);
```

**تحقق حي:** توليد كود تطبيق فعلي من لوحة الإدارة الحقيقية (project-nfzka.vercel.app) نجح بعد الإصلاح — تحقّق عبر Network tab (طلب `201 Created`).

✅ **طُبِّق على DAWRAK-TEST أيضاً (2026-07-07)** — تحقّق: تشغيل ناجح لنفس GRANT+policies.

---

### 2026-07-07 — خلل قديم ثانٍ مكتشَف بالصدفة: `promotions_id_seq` بدون GRANT لـ anon/authenticated على DAWRAK

أثناء اختبار أحمد الحي لميزة "إرسال عرض ترويجي" (كود خصم DAWRAK، باقة فضي)، ظهر خطأ **`permission denied for sequence`** (كود 42501). نفس فخّ `bookings_id_seq` المكتشَف سابقاً اليوم، بس على sequence جدول `promotions` هذي المرة: الجدول نفسه عنده GRANT INSERT لـ`anon`/`authenticated`، لكن sequence الـid (`promotions_id_seq`) كان ممنوح فقط لـ`service_role`.

**الإصلاح المطبَّق على DAWRAK:**
```sql
GRANT USAGE, SELECT ON SEQUENCE public.promotions_id_seq TO anon, authenticated;
```

**تحقق حي:** إعادة إرسال نفس العرض التجريبي من التطبيق الحقيقي نجحت بعد الإصلاح.

✅ **طُبِّق على DAWRAK-TEST أيضاً (2026-07-07)**.

---

### 2026-07-07 — إغلاق فخّ "sequence بدون GRANT" نهائياً على DAWRAK (استباقي + دائم)

بعد ما تكرر نفس فخّ "permission denied for sequence" ثلاث مرات بنفس اليوم (`bookings_id_seq`, `app_settings_id_seq`, `promotions_id_seq`)، بدل ننتظر كل جدول يكتشف عطله بالصدفة أثناء استخدام حقيقي — فحصنا **كل** الجداول اللي عندها INSERT لـ`anon`/`authenticated` مقابل صلاحيات الـsequences الفعلية دفعة وحدة (مقارنة `role_table_grants` مع الـsequence ACL المحفوظة من فحص اليوم، بدون تخمين).

**لُقيت 4 جداول ثانية عندها نفس الفخّ نايم (لسا ما انكشف لأن حد ما جرّب الإضافة فيها فعلياً):** `messages`, `notifications`, `reviews`, `waiting_list`.

**إصلاح استباقي مطبَّق على DAWRAK:**
```sql
GRANT USAGE, SELECT ON SEQUENCE public.messages_id_seq TO anon;
GRANT USAGE, SELECT ON SEQUENCE public.notifications_id_seq TO anon;
GRANT USAGE, SELECT ON SEQUENCE public.reviews_id_seq TO anon, authenticated;
GRANT USAGE, SELECT ON SEQUENCE public.waiting_list_id_seq TO anon;
```

**إصلاح دائم (يقفل الفخّ للأبد — أي جدول جديد مستقبلاً يرث الصلاحية تلقائياً):**
```sql
ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT USAGE, SELECT ON SEQUENCES TO anon, authenticated;
```

✅ **طُبِّق على DAWRAK-TEST أيضاً (2026-07-07)** — بما فيها `ALTER DEFAULT PRIVILEGES` الدائم، فالفخّ مقفول بالمشروعين معاً الآن.

**النتيجة النهائية:** كل الإصلاحات الأربعة المؤكدة اليوم (promo_codes، promotions_id_seq، الفخّ الاستباقي للـ4 جداول، والحل الدائم) مطبَّقة على **DAWRAK وDAWRAK-TEST معاً** — المشروعان متطابقان 100% هيكلةً وصلاحياتٍ.

**درس إضافي مهم:** لما تتكرر نفس فئة العطل (GRANT ناقص) أكثر من مرتين بنفس اليوم، الأفضل التحول من "أصلح كل ما يظهر خطأ" إلى **"قارن الهيكلة الكاملة مرة وحدة"** — أسرع وأشمل بكثير من انتظار كل ميزة تكتشف عطل جديد بالصدفة أثناء استخدام حقيقي.

---

### 2026-07-14 — حذف ميزة نقاط الولاء بالكامل (قرار أحمد)

بطلب صريح من أحمد، حُذفت ميزة "نقاط الولاء" بالكامل من التطبيق — كانت غير مفعّلة عملياً أصلاً (لا واجهة استخدام للعميل، لا آلية اكتساب تلقائي عند إتمام حجز، قيمة يدوية بس تُعدَّل من لوحة الإدارة).

**حذف الكود (منجز، مرفوع على main):** سؤال الأسئلة الشائعة (4 لغات)، `admin/src/app/api/settings/route.ts`، `admin/src/app/api/customers/[id]/route.ts`، صفحة عملاء الإدارة (عمود التجميد + شارة النشاط + تصدير PDF)، سجل التدقيق. تحقّق: type-check للوحة الإدارة نظيف بدون أخطاء.

**حذف قاعدة البيانات (منجز، بتأكيد صريح مزدوج من أحمد قبل التنفيذ):**
```sql
ALTER TABLE customers DROP COLUMN IF EXISTS loyalty_points;
ALTER TABLE customers DROP COLUMN IF EXISTS loyalty_frozen;
ALTER TABLE app_settings DROP COLUMN IF EXISTS loyalty_settings;
```
طُبِّق على **DAWRAK وDAWRAK-TEST معاً** (نفس السكربت بالضبط على الاثنين) — تحقّق: "Success. No rows returned" على المشروعين.

**الحالة: ✅ مكتمل بالكامل — حذف نهائي على مستوى الكود وقاعدة البيانات، بالمشروعين معاً.**

---

### 2026-07-14 — خللان قديمان مكتشَفان أثناء اختبار "حذف حساب الصالون" (نقطة 25)

أثناء اختبار أحمد الحي لميزة حذف الحساب (سجّل صالون تجريبي "لقمان" ليجرّب عليه)، اكتُشف خللان قديمان غير متعلقين بترحيل Mumbai:

**1. الصالونات المجمّدة (تجميد إداري أو حذف ذاتي) كانت تظهر للعملاء بالصفحة الرئيسية بعلامة "مجمّد" بدل الاختفاء كلياً.**
- استعلام جلب الصالونات للعملاء (`App.jsx`) كان يجيب كل الصالونات المعتمدة بغض النظر عن `frozen`، وSalonCard يعرضها بعلامة بدل إخفائها
- **قرار توضيحي من أحمد:** الحالتين (تجميد إداري مؤقت مثل رسوم غير مسددة، وحذف ذاتي دائم) لازم تختفي من العميل بنفس الطريقة — الفرق بينهم (يرجع أو لا) يبقى محفوظاً من جهة تسجيل الدخول/الإدارة فقط
- **الإصلاح (L122):** إضافة `&frozen=eq.false` لاستعلام العملاء الرئيسي — مصدر بيانات وحيد تتفرّع منه كل الشاشات (مفضّلة/مقارنة/بحث)، فالإصلاح شمل الكل تلقائياً

**2. زر تسجيل صالون جديد بدون حماية من الضغط المزدوج — يسبب تسجيل الصالون مرتين.**
- اكتُشف عملياً: أحمد سجّل صالون "لقمان" فظهر مكرراً بلوحة الإدارة (نسخة "موافق" ونسخة "انتظار") — نفس البيانات بالضبط، يعني نداءين منفصلين لـ`/api/register-salon`
- **السبب:** زر "إنشاء الحساب" بـ`RegisterView` ما كان فيه أي `disabled`/state أثناء الإرسال — ضغطتين متتاليتين (لمس سريع أو بطء شبكة) = تسجيلين منفصلين
- **الإصلاح (L123):** إضافة `submitting` state، تعطيل الزر أثناء الإرسال
- **تحقّق حي:** أحمد سجّل صالون تجريبي جديد بعد الإصلاح — "أنضف مرة واحدة فقط" ✅
- **ملاحظة تنظيف:** النسخة المكرّرة القديمة لصالون "لقمان" احتاجت حذف يدوي من لوحة الإدارة (بيانات فعلية، مو شي يُصلَح بتحديث كود)

**الحالة: ✅ الإصلاحان مرفوعان على main ومختبران حياً بنجاح.**

---

### 2026-07-14 — لوحة الإدارة: تمييز الصالونات المحذوفة عن المجمّدة + إصلاحات فلاتر مرافقة

أثناء مراجعة أحمد لصفحة الصالونات بلوحة الإدارة (بعد اختبار حذف صالون "البرنس")، لاحظ إن الصالون المحذوف يظهر بعلامة "مجمّد" العامة بدون تمييز، وطلب قسم/فلتر منفصل للمحذوفين.

**التغييرات المنجزة:**
1. **`admin/src/app/api/salons/route.ts`:** إضافة حساب `is_deleted` لكل صالون (`frozen && !owner_pin_hash`) بالسيرفر، بدون تسريب `owner_pin_hash` نفسه للمتصفح
2. **`admin/src/app/(admin)/salons/page.tsx`:**
   - شارة مميّزة "🗑 محذوف (بقرار المالك)" منفصلة عن "🔒 مجمّد"
   - زرّان منفصلان بالفلاتر: "🔒 المجمّدة" و"🗑 المحذوفة" (بدل زر واحد مشترك بمحاولة أولى، صُحِّح بطلب أحمد)
   - إخفاء المجمّد/المحذوف من القائمة الافتراضية، يظهر فقط عند اختيار الفلتر المخصّص
   - حذف حالة "موقوفة" بالكامل (تبويب الفلتر + زر "⏸ تعليق") — بطلب أحمد، لأنها تكرار مفهومي لـ"مجمّد"
3. **إصلاح خلل مرافق:** فلتر حالة الصالون (تنتظر مراجعة/موافق/مرفوض) كان يجلب البيانات من السيرفر (`?status=`) فيُسقط أي صالون مجمّد/محذوف تماماً من `salons` state عند اختيار أي تبويب غير "الكل" — يخفي زري المجمّدة/المحذوفة بالكامل رغم وجود بيانات. حُوِّل لفلترة محلية (client-side) مطابقة لأسلوب فلتر الاشتراك الموجود أصلاً

**تحقّق حي:** أحمد أكّد الزرّين يعملان صح ويبقيان ظاهرين بغض النظر عن تبويب الحالة المفتوح.

**الحالة: ✅ مكتمل، مرفوع على main.**

---

### 2026-07-14 — تقييم صادق لمستوى حماية بيانات العملاء + الحاجة لمراجعة أمنية مستقلة (Pentest) قبل الإطلاق

أحمد سأل تقييماً صادقاً لمستوى حماية بيانات العملاء حالياً (سيبابيس + الكود)، وهل فيه شي يحمي 100%، وهل إنسان يحميها أفضل من كلود. التسجيل هنا للرجوع له مستقبلاً قبل أي إطلاق تجاري حقيقي.

**تقييم تقديري (مو رقم علمي دقيق، بناءً على شغل الجلسات الفعلي):**
- **سيبابيس (RLS + GRANT):** ~75-80% بعد تحقق ومطابقة شاملة اليوم. قبل اليوم كان أقل بكثير — اكتُشف 4 جداول بنفس فخّ الـsequence، وpromo_codes بدون INSERT/DELETE أصلاً. هذا رقم "اللحظة الحالية" فقط، يحتاج انضباط مستمر (نفس قواعد CLAUDE.md) وإلا يتراجع مع أي جدول/عمود جديد
- **الكود:** ~55-65%. رمز دخول الصالون محمي (hash بالسيرفر). رمز دخول **العميل** أضعف معمارياً — يتحقق محلياً بـlocalStorage بشكل أساسي، والتحقق بالسيرفر خلفي صامت لا يمنع الدخول (اكتُشف أثناء اختبار نقطة 19)

**لا شي يحمي 100% — مبدأ أساسي بالأمن السيبراني (defense in depth، مو رقم نهائي).**

**الفرق بين مراجعة الكود (اللي نسويها بالجلسات) وبين Pentest حقيقي:**
مراجعة الكود = "هل هذا يطابق القصد؟" (عقلية تحقّق). Pentest = هجوم فعلي على التطبيق **المنشور الحي** بعقلية عدائية ("كيف أكسره؟"). أمثلة ثغرات لم تُختبر إطلاقاً بأي جلسة حتى الآن:
1. **IDOR** — هل عميل يقدر يشوف حجز/بيانات عميل أو صالون ثاني بس بتغيير ID بالطلب؟
2. **تجاوز Rate Limiting** — عبر IP rotation/VPN، هل يصمد الحد الفعلي؟
3. **XSS** — هل حقول نصية (اسم صالون، رسالة ترحيب، رسائل) تُرمَّز صح وقت العرض، أو تُنفَّذ كـHTML/JS؟
4. **أمان الجلسة/الكوكيز** — HttpOnly/Secure/SameSite مضبوطة صح؟ هل جلسة تسجيل الدخول قابلة للسرقة؟
5. **إساءة استخدام منطق العمل** — استغلال أكواد الخصم أو تدفقات الحجز بطرق غير مقصودة
6. **تسريب أسرار بكود المتصفح** — فحص شامل لملفات الـJS المُجمّعة (bundle) العامة

**ليش كلود ما يعوّض هذا كامل:** بدون أدوات فحص متخصصة (SAST/DAST)، بدون متصفح حي يهاجم فيه التطبيق المنشور فعلياً وقت التحليل، وبعقلية افتراضية "تحقق من الصحة" مو "حاول تكسر" — فرق جوهري بمجال الأمن.

**القرار:** غير عاجل حالياً (بدون عملاء حقيقيين وأموال فعلية بعد — يطابق قرار تأجيل Tier 2 السابق). **إلزامي قبل أي إطلاق تجاري حقيقي.** خيارات عملية غير مكلفة: مستشار أمن مستقل لمراجعة مركّزة (أرخص من شركة كبيرة)، أو أدوات فحص آلية (OWASP ZAP، Snyk لفحص ثغرات المكتبات). النقطة مسجّلة أيضاً كنقطة #92 بـ`الفحص-الشامل.md`.

**دليل عملي مبسّط — أحمد يفحص تطبيقه بنفسه (بأدوات المتصفح، بدون برامج إضافية):**

⚠️ **قاعدة أمان قبل البدء:** اختبارات "الكتابة" (XSS، استغلال الأكواد) على **صالون تجريبي ببياناتك أنت بس** — مو صالون "الوادي" الحقيقي. اختبارات "القراءة" (IDOR) لا تتعمّد تعديل بيانات حد ثاني حتى لو قدرت تشوفها.

**1. XSS — هل النصوص المُدخلة تُنفَّذ كـHTML/JS؟**
- بأي حقل نصي يظهر لاحقاً لطرف ثاني (اسم صالون، رسالة ترحيب، رسالة بالمحادثة، تعليق تقييم) — اكتب بالضبط: `<img src=x onerror=alert('XSS')>`
- احفظ، وشوف الحقل من حساب ثاني (عميل يشوف اسم الصالون، أو صالون يشوف تعليق العميل)
- **آمن:** يظهر كنص حرفي `<img src=x...>` بدون أي نافذة تنبيه. **خطر:** تطلع نافذة "XSS" منبثقة — هذا يعني أي زائر ممكن يُحقن كود خبيث فعلي.

**2. IDOR — هل تقدر تشوف بيانات حساب ثاني بس بتغيير رقم؟**
- افتح F12 → Network، وسوّي أي عملية تجيب بياناتك (مثلاً فتح تفاصيل حجزك، أو صفحة "حساباتي")
- لاقي الطلب اللي فيه رقم ID بياناتك بالرابط أو بالـbody
- انسخ نفس الطلب لكن غيّر الرقم لرقم غير رقمك (جرّب أرقام قريبة، ١، ٢، ٣...)
- **آمن:** يرجع خطأ أو "غير موجود". **خطر:** يرجع بيانات حساب حد ثاني فعلياً.

**3. تجاوز Rate Limiting — هل الحد الأقصى للمحاولات يصمد؟**
- بشاشة تسجيل دخول الصالون أو العميل، جرّب رمز سري خاطئ بسرعة أكثر من 5 مرات متتالية
- **آمن:** يقفل بعد العدد المحدد برسالة واضحة (زي ما اختبرنا اليوم فعلاً ✅). **خطر:** يستمر يقبل محاولات بلا حد

**4. أمان الجلسة (Cookies) + CSRF**

**لصاحب الصالون** (كوكي حقيقي محمي):
- سجّل دخول كصالون، افتح F12 → Application (أو Storage) → Cookies
- دوّر على الكوكي `dork_owner_session`
- شوف الأعمدة: **HttpOnly** لازم ✓، **Secure** لازم ✓، **SameSite** لازم `Lax` أو `Strict`
- **آمن:** الثلاثة مفعّلة (تحقّق كودي 2026-07-14 أكّد الثلاثة مضبوطة صح افتراضياً بـ`api/_lib/cookies.js`). **خطر:** أي وحدة منهم مو مفعّلة

**للعميل** (⚠️ مختلف تماماً — ما فيه كوكي `dork_customer_session` أصلاً، كانت معلومة غلط بهذا الدليل قبل تصحيحها 2026-07-14):
- جلسة العميل تُخزَّن عبر مكتبة Supabase مباشرة بـ**localStorage** (`persistSession: true`)، مو بكوكي HttpOnly
- افتح F12 → Application → Local Storage → دوّر على مفتاح يبدأ بـ`sb-` وينتهي بـ`-auth-token`
- **هذا بحد ذاته يعتبر "خطر" معماري معروف ومسجّل مسبقاً** (ضعف تسجيل دخول العميل بملف الأمان) — أي كود JS يشتغل بالصفحة (حتى لو حقن عبر XSS) يقدر يقرأ هذا التوكن مباشرة، على عكس كوكي HttpOnly. هذا سبب إضافي يخلي اختبار XSS (رقم 1) أهم بكثير لجهة العميل تحديداً — لو XSS نجح، يقدر يسرق جلسة العميل كاملة

**5. تسريب أسرار بكود المتصفح**
- F12 → Sources (أو Ctrl+U لعرض المصدر)، وابحث (Ctrl+F داخل الملفات) عن كلمات: `service_role`, `SECRET`, `PRIVATE_KEY`
- **آمن:** ولا نتيجة (أو بس مفاتيح anon/publishable المعروف إنها عامة). **خطر:** أي ظهور لكلمة service_role أو secret أو private key فعلية

**6. إساءة استخدام منطق العمل (اختياري، أعقد شوي)**
- جرّب استخدام نفس كود خصم أكثر من `max_uses` المحدد له — يفترض يرفض بعد الحد
- جرّب حجز نفس الموعد بنفس الحلاق مرتين بسرعة (نافذتين متصفح) — يفترض يمنع التعارض

لو أي اختبار طلع نتيجة "خطر"، سجّلها هنا بتاريخها وأرسلها لي أول ما تصحى — نصلحها فوراً بنفس المنهجية.

---

### 2026-07-14 — ثغرة حقيقية مكتشَفة: `customers.pin_hash` قابل للقراءة والتعديل من anon/authenticated

أثناء توثيق نقاط Tier 2 المؤجلة بملف الأمان الجديد، لوحظ إن نفس فخّ "أعمدة حساسة مكشوفة" اللي أُصلح على `salons` اليوم (owner_pin_hash) **لم يُطبَّق على `customers`** — فخّ موازٍ غير مكتشف قبل الآن.

**الخطورة:** `anon`/`authenticated` عندهم GRANT SELECT **و** UPDATE على `pin_hash`, `pin_fails`, `pin_locked_until` لجدول `customers`. التطبيق نفسه ما يطلب هذي الأعمدة أبداً (تحقّق: صفر نتائج بحث لـ`pin_hash` بكل استدعاءات `sb("customers"...)` بـApp.jsx) — لكن أي طلب API مباشر (خارج التطبيق، زي Postman أو تعديل طلب بـNetwork tab) يقدر:
- يقرأ `pin_hash` لأي عميل (مشفّر، لكن يفتح باب هجوم offline)
- **الأخطر: يكتب/يغيّر `pin_hash` لأي عميل مباشرة** — استيلاء كامل على أي حساب عميل بدون كسر أي تشفير، فقط لأن الصلاحية مفتوحة بقاعدة البيانات

**الإصلاح المطبَّق (على DAWRAK وDAWRAK-TEST معاً، بتأكيد أحمد):**
```sql
REVOKE SELECT (pin_hash, pin_fails, pin_locked_until), UPDATE (pin_hash, pin_fails, pin_locked_until) ON customers FROM anon, authenticated;
```
آمن 100% — لا يؤثر على أي ميزة (التحقق من رمز العميل يصير عبر `api/customer-auth.js` بصلاحية service_role، مو عبر anon مباشرة).

**الحالة: ✅ مُصلح على المشروعين.**

**ملاحظة مهمة للمستقبل:** هذا اكتشاف بالصدفة أثناء توثيق، مو نتيجة فحص منهجي شامل لكل الأعمدة الحساسة بكل الجداول. **الفجوة الجذرية الحقيقية لسه موجودة** — قسم "Tier 2" أعلاه بهذا الملف (من 2026-07-02) يوثّق إن `customers`/`bookings`/`reviews`/`promo_codes` عندها RLS policies مفتوحة بالكامل (`USING (true)`) للقراءة والتعديل، لأن ما فيه نظام هوية حقيقي (`auth.uid()`) مطبَّق بعد. هذا القرار كان مؤجَّل بوعي وقت ما فيه عملاء حقيقيين — **يحتاج مراجعة الآن** بما إن فيه صالون حقيقي (الوادي) وتوجّه نحو إطلاق تجاري. راجع تفاصيل الحل الكامل بقسم Tier 2 أعلاه ونقطة 86 بـ`الفحص-الشامل.md`.

---

### 2026-07-14 — فحص فعلي لثغرات المكتبات الجاهزة (`npm audit`) — أول تنفيذ حقيقي لنقطة كانت "لم تُفحص بعد"

أحمد سأل هل كل نقاط الأمان المذكورة بالجلسة فعلاً مسجّلة أو نسينا شي. أثناء المراجعة، شُغّل `npm audit --omit=dev` فعلياً (أول مرة، كانت مسجّلة كـ"لم يُفحص بعد" بس بدون تنفيذ):

**التطبيق الرئيسي (`dork-app`, جذر المستودع):** ✅ **صفر ثغرات** (`found 0 vulnerabilities`)

**لوحة الإدارة (`admin/`):** 🔴 **14 ثغرة** (10 متوسطة، 3 عالية، 1 حرجة/critical) — جميعها بسبب حزمتين:
1. **`next@14.2.5`** (النسخة المثبَّتة فعلياً بالإنتاج) — عدة ثغرات موثّقة رسمياً بـGitHub Advisories تشمل: **تجاوز صلاحيات (authorization bypass)**، تسميم Cache، SSRF عبر Middleware، وXSS بسكربتات beforeInteractive. **الإصلاح آمن ومنخفض الخطورة:** تحديث لـ`next@14.2.35` — نفس الإصدار الرئيسي (14)، مو قفزة كبيرة، `npm audit fix` وحدها (بدون `--force`) ما نفّذته تلقائياً بس لأن `package.json` مثبّت برقم دقيق (`"14.2.5"` بدون `^`).
2. **حزم `firebase`/`@firebase/*`** (عبر تبعية `undici` قديمة بداخلها) — الإصلاح يتطلب قفزة إصدار رئيسي كامل (`firebase@12.16.0`، حالياً على إصدار أقدم بكثير) — **يحتاج اختبار توافق كامل قبل التطبيق**، مو تحديث آمن مباشر مثل Next.js.

**الحالة: 🔴 لم يُصلح بعد — يحتاج قرار أحمد.** تحديث Next.js وحده (الخطوة الآمنة) يقفل الثغرة الحرجة (authorization bypass) والأغلبية العالية/المتوسطة المرتبطة به مباشرة.

**درس مهم لنفس سؤال أحمد:** هذا مثال حي على "نقطة مذكورة بالكلام لكن ما نُفِّذت فعلياً" — كانت مسجّلة كسطر بجدول من أول يوم إنشاء ملف الأمان لكن بدون تشغيل فعلي حتى الآن. يؤكد فائدة المراجعة الدورية لقائمة "لسه ناقص" بدل افتراض إنها بس تفاصيل شكلية.

---

### 2026-07-14 — بدء تنفيذ Tier 2 فعلياً: المرحلة 1 (تعبئة auth_uid) + اكتشاف وإصلاح خلل عرض owner_email

بعد قرار أحمد إعطاء Tier 2 أولوية قصوى (بند 25 بملف الاتفاقيات)، بدأ التنفيذ الفعلي بمرحلة آمنة تحضيرية قبل أي تعديل RLS:

**1. مراجعة الكود كشفت البنية التحتية أكثر اكتمالاً مما وُثِّق سابقاً:** `sb()` بـ`App.jsx` فعلاً ترسل توكن Supabase حقيقي تلقائياً لو فيه جلسة حية (مو معامل ميت كما وُصف بخطأ بقسم Tier 2 الأصلي أعلاه)، و`owner-auth.js` فعلياً يصدر جلسة Supabase Auth حقيقية للمالك عند الدخول الناجح (عبر آلية magiclink OTP، مطابقة لآلية `customer-auth.js`) والعميل بالمتصفح يفعّلها بـ`setSession`.

**2. الفجوة الحقيقية المتبقية:** `owner-auth.js` (بعكس `customer-auth.js`) ما كان يعبّي `salons.auth_uid` تلقائياً بعد إصدار الجلسة — يعني حتى لو المالك يوصله توكن حقيقي، قاعدة البيانات ما فيها ربط دائم بين هويته وصف الصالون. **الإصلاح (L124 جزء 1):** إضافة نفس منطق self-healing الموجود بـ`customer-auth.js` لـ`owner-auth.js` — يعبّي `auth_uid` تلقائياً أول دخول ناجح بعد التحديث.

**3. اكتُشف أثناء التحقق: صالون "الوادي" الحقيقي ما كان عنده `owner_email` مضبوط أصلاً (NULL)** — يعني آلية إصدار الجلسة (تتطلب بريد) ما كانت تشتغل له إطلاقاً من الأساس. أحمد أضاف البريد يدوياً من لوحة تحكم الصالون (owner_email: moodi1440@gmail.com) وضغط حفظ.

**4. خلل حقيقي انكشف فور الاختبار:** بعد الحفظ، ظهرت رسالة "تم حفظ التعديلات ✅"، لكن لما أحمد رجع لنفس الصفحة اختفى البريد من الشاشة — بدا وكأن الحفظ فشل. **تحقق مباشر بقاعدة البيانات (Table Editor) أثبت العكس: البريد محفوظ صح 100%** — المشكلة كانت عرض بس، مو حفظ. السبب: دالة `save()` بمكوّن `OwnerSettings` (`App.jsx`) ترسل `owner_email` للسيرفر (السيرفر `api/owner-salon.js` يقبله ضمن `OWNER_EDITABLE_FIELDS` ويحفظه صح)، لكن تحديث الحالة المحلية بعد النجاح (`setSalons(...)`) كان **ناقص حقل `owner_email`** — فالنموذج يرجع يبني نفسه من نسخة قديمة (فاضية) بمجرد إعادة فتح الصفحة بنفس الجلسة.

**الإصلاح (L124 جزء 2):** إضافة `owner_email:f.ownerEmail||null` لكائن تحديث الحالة المحلية بسطر `setSalons`. **تحقّق حي:** أحمد أكّد البريد يبان صح بعد التحديث.

**الحالة: ✅ مرحلة 1 مكتملة ومرفوعة على `main`** — صالون "الوادي" جاهز الآن لتفعيل الجلسة الحقيقية عند أول دخول قادم.

**✅ تحقّق حي نهائي (نفس اليوم):** أحمد سجّل خروج ثم دخول من جديد لصالون "الوادي" — `information_schema`/Table Editor أكّد `salons.auth_uid` تعبّى تلقائياً بقيمة حقيقية (`21fdd0d1-df3d-45e5-b790-121...`) بدل NULL. **المرحلة 1 مؤكدة تعمل فعلياً على الإنتاج، مو نظرياً بس.**

**التالي:** المرحلة 2 — تصميم واختبار سياسات RLS الفعلية (`auth.uid()` بدل `true`) على DAWRAK-TEST أولاً، ثم DAWRAK.

---

### 2026-07-14 — جرد شامل لمرحلة 2 (Tier 2): نمطان يعقّدان قفل RLS + ثغرة خصوصية حية مكتشَفة أثناء الفحص

قبل كتابة أي سياسة RLS جديدة، رُوجعت كل نقاط الوصول المباشر من `App.jsx` لجداول `customers` و`bookings` (أكثر من 30 موضع). النتيجة: قفل RLS على "كل مستخدم يشوف بياناته بس" مباشرة سيكسر ميزات حقيقية شغّالة، للسببين التاليين:

**1. استعلامات "قبل الهوية" (`customers`):** تسجيل الدخول واستعادة الحساب يبحثان بالجوال/البريد **قبل** ما يصير عند المستخدم أي جلسة (`App.jsx:7918, 7947, 8085`) — لازم هذي تتحول لمسارات سيرفر (service_role) بدل قراءة مباشرة بمفتاح anon، قبل أي قفل RLS.

**2. استعلام عام غير مفلتر (`customers`, سطر 1851):** يجيب حتى 500 عميل دفعة وحدة بلا فلتر — يلتزم بحد "الكمية" (نقطة 19.3 بملف الاتفاقيات، بدون علاقة بمشكلة استهلاك الـ18GB القديمة) لكنه يخرق "الصلاحية" (مين يحق له يشوف بيانات غيره).

**3. 🔴 ثغرة خصوصية حية مكتشَفة أثناء الفحص (مو افتراضية — قائمة الآن على الإنتاج):** دالة `refreshSalonBookings(salonId)` (`App.jsx:2095-2098`) تجيب **كل حجوزات صالون معيّن كاملة** (`customer_name`, `customer_phone` لكل حجز) بمجرد معرفة `salon_id` — بدون فلترة لعميل معيّن. تُستدعى من شاشة "حجوزاتي" بجهة العميل (`App.jsx:8350-8354`) بنيّة "جيب حجوزات صالوناتي أنا" (`customer.history`)، لكن **الاستعلام الفعلي يجيب حجوزات كل عملاء ذاك الصالون، مو حجوزات هذا العميل بس.** يعني حالياً، أي شخص (حتى بدون تسجيل دخول) يقدر يستدعي نفس الطلب برقم `salon_id` ويحصل على **أسماء وأرقام جوال كل من حجز بذاك الصالون** — بسبب سياسة RLS المفتوحة (`USING(true)`) الحالية على `bookings`.

**تقييم الخطورة:** 🔴 عالية — تسريب بيانات شخصية حقيقية (اسم + جوال) لعملاء حقيقيين، قائم الآن على الإنتاج (DAWRAK)، ومكتشَف بالصدفة أثناء تحضير Tier 2 وليس نتيجة فحص أمني منهجي مخصّص.

**الإصلاح المزدوج المطلوب:**
- **إصلاح فوري (لا ينتظر RLS):** تعديل الاستدعاء بسطر 8353 ليرسل فلتر إضافي `customer_id=eq.${customer.id}` (أو مطابق برقم الجوال) — يقلل التسريب فوراً بغض النظر عن حالة RLS، لأنه يجلب حجوزات العميل نفسه بس مو الصالون كامل
- **إصلاح جذري (يحتاج Tier 2):** سياسة RLS نفسها تقيّد القراءة بمن له صلاحية فعلية (صاحب الحجز أو صاحب الصالون)، بحيث حتى لو الكود غفل عن الفلترة مرة ثانية، القاعدة تمنع التسريب من جذوره

**✅ الإصلاح الفوري مطبَّق ومرفوع على `main` (L125، نفس اليوم):** `refreshSalonBookings` صار تقبل معامل ثانٍ اختياري `customerId` — لو مُمرَّر (حالة جهة العميل)، يُضاف فلتر `customer_id=eq.` للطلب الفعلي (يقفل تسريب بيانات باقي العملاء من جذر الشبكة)، والدمج بالحالة المحلية بعدها **يدمج بدل الاستبدال** (يمنع تضييق `salon.bookings` المشترك المستخدم بميزات ثانية مثل عداد الحجوزات بمقارنة الصالونات وإحصاءات لوحة تحكم الصالون). استدعاءات جهة الصالون (owner dashboard) لم تتغيّر — تبقى تجيب كل الحجوزات كما كانت (صلاحية شرعية لصاحب الصالون).

**الحالة: ✅ الإصلاح الفوري (تقليل التسريب) مكتمل. 🔲 الإصلاح الجذري (سياسة RLS تمنعه من جذوره حتى لو تكرر خطأ مشابه بالكود مستقبلاً) لسه ينتظر مرحلة 2 الكاملة.**

**التالي بعد القرار:** استكمال نفس الجرد لجداول `reviews` و`promo_codes`، ثم تصميم سياسات RLS الكاملة + إصلاح كل نقاط الوصول المكتشَفة، اختبار على DAWRAK-TEST، ثم DAWRAK.

---

### 2026-07-14 — 🔴 اكتشاف: إصلاح `customers.pin_hash` الصباحي لم يُغلق الثغرة فعلياً + عطل حي بتسجيل دخول العميل

أثناء اختبار Tier 2 مرحلة 1 (تسجيل دخول/خروج متكرر بنفس المتصفح لصالون "الوادي" ثم لحساب عميل)، ظهر خطأ حي بشاشة دخول العميل:

```
Supabase GET customers: {"code":"42501",...,"hint":"Grant the required privileges to the current role with: GRANT SELECT ON public.customers TO authenticated;","message":"permission denied for table customers"}
```

**السبب الأول (عطل الدخول):** المتصفح كان يحمل جلسة Supabase Auth حقيقية متبقّية من دخول صاحب الصالون قبلها (نفس آلية Tier 2 الجديدة) — و`sb()` تلقائياً استخدمت توكن `authenticated` بدل مفتاح `anon` العام. الدور `authenticated` **ما كان عنده ولا صلاحية SELECT/UPDATE واحدة على جدول customers بمستوى الجدول أو العمود** — أي طلب من مستخدم عنده جلسة حقيقية (بدل anon) يفشل بالكامل.

**الاكتشاف الأخطر أثناء التشخيص:** فحص `information_schema.table_privileges` كشف إن `anon` عنده `GRANT SELECT, UPDATE` على **جدول customers كامل** (كل الأعمدة). هذا يعني **إصلاح REVOKE العمودي لـ`pin_hash`/`pin_fails`/`pin_locked_until` من نفس صباح اليوم لم يُغلق الثغرة فعلياً** — الأمر نجح ("Success. No rows returned") لكن بلا أثر حقيقي، لأن الوصول كان قادماً أصلاً من منح الجدول الكامل، لا من صلاحية عمودية منفصلة. **يعني `pin_hash` كان لا يزال قابل للقراءة والتعديل من `anon` طول اليوم رغم "الإصلاح" الصباحي**، حتى اكتشاف هذا العطل المنفصل بالمساء.

**الإصلاح الشامل المطبَّق على DAWRAK (بتأكيد أحمد، عبر 3 استعلامات تحقق متتالية):**
```sql
REVOKE SELECT, UPDATE ON customers FROM anon;

GRANT SELECT (
  admin_notes, auth_uid, blocked, created_at, email, favs, google_uid,
  history, id, lang, location_lat, location_lng, name, notifications,
  phone, photo, profile_img
) ON customers TO anon, authenticated;

GRANT UPDATE (
  favs, history, location_lat, location_lng, name, phone, email,
  lang, notifications, photo, profile_img
) ON customers TO anon, authenticated;
```
ملاحظة: `pin_hash`, `pin_fails`, `pin_locked_until` مُستبعَدة عمداً من قائمتي SELECT وUPDATE — هذي المرة الاستبعاد فعلي 100% لأن الوصول الوحيد المتبقي هو المنح العمودي الصريح أعلاه، ولا يوجد منح جدول كامل يتجاوزه.

**تحقق نهائي (3 خطوات متتالية):**
1. `table_privileges` بعد الإصلاح: صفر صفوف SELECT/UPDATE لـ`anon`/`authenticated` (بس TRUNCATE/REFERENCES/TRIGGER الغير حساسة) ✅
2. اختبار حي: تسجيل دخول عميل (نفس المتصفح اللي فشل فيه قبل) نجح بدون خطأ 42501 ✅
3. الصفحة الرئيسية للعميل ظهرت صح بعد الدخول (صالونات "لقمان" و"الوادي" ظاهرة) ✅

**الحالة: ✅ مُصلح فعلياً على DAWRAK (مؤكد بثلاث طبقات تحقق).** ⏳ **لسا يحتاج تطبيق نفس السكربت على DAWRAK-TEST** (بعد تأكيد الاستقرار — القاعدة المعتادة بالمشروع).

**درس جديد مُضاف لـCLAUDE.md (بند 11):** REVOKE عمودي لا يقدر يسحب صلاحية موروثة من منح جدول كامل — ينجح بصمت بدون أي تحذير. أي REVOKE لعمود حساس يجب يُتحقق بعده من `table_privileges` كمان، مو `column_privileges` بس.

---

### 2026-07-14 — فحص شامل نهائي: هل نفس فخّ "منح جدول كامل يتجاوز REVOKE عمودي" موجود بجداول ثانية؟

بعد اكتشاف الفخّ على `customers.pin_hash`، فُحصت **كل الأعمدة الحساسة بكل الجداول دفعة وحدة** (نمط `pin`/`hash`/`password`/`secret`/`token` عبر `information_schema.columns`) مقابل `information_schema.table_privileges` لـ`anon`/`authenticated` بنفس الاستعلام المدمج (LEFT JOIN واحد، مو جدولاً جدولاً):

| الجدول.العمود | النتيجة |
|---|---|
| `admin_users.password_hash` | ✅ نظيف — ولا منح جدول كامل |
| `customers.pin_hash/pin_fails/pin_locked_until` | ✅ نظيف (بعد إصلاح اليوم) |
| `salons.owner_pin_hash/owner_pin_fails/owner_pin_locked_until/password` | ✅ نظيف — تحقّق مزدوج (فحص مباشر لجدول salons + هذا الفحص الشامل) |
| `fcm_tokens.device_token` | ✅ نظيف |
| `notification_logs.fcm_token_id` | ✅ نظيف |
| `user_sessions.session_token` | ✅ نظيف |

**النتيجة:** فخّ "منح جدول كامل يتجاوز REVOKE عمودي" كان **محصوراً بـ`customers.pin_hash` بس** — بقية الجداول الحساسة سليمة فعلياً (تأكيد بالفحص، مو افتراض). هذا يقفل ملف "الأعمدة الحساسة" (بند 9 بـCLAUDE.md) بثقة كاملة لأول مرة منذ فتحه.

**✅ تطبيق على DAWRAK-TEST (نفس اليوم، بعد تأكد استقرار DAWRAK):** قارن أحمد قائمة أعمدة `customers` بين المشروعين — مطابقة 100% (20 عمود). شُغّل نفس سكربت الإصلاح (REVOKE جدول + GRANT عمودي) على DAWRAK-TEST، وتحقّق `table_privileges` بعده أكّد نفس النتيجة النظيفة (بس TRUNCATE/REFERENCES/TRIGGER، صفر SELECT/UPDATE). **DAWRAK وDAWRAK-TEST متطابقان تماماً على هذا الإصلاح الآن.**

---

### 2026-07-14 — استكمال جرد Tier 2: جداول `reviews` و`promotions`/`promo_codes`

استكمالاً لجرد `customers`/`bookings` أعلاه، رُوجعت كل نقاط الوصول المباشر لآخر جدولين ضمن نطاق Tier 2:

**`reviews` — أخف خطورة من المتوقع:**
- **القراءة مفتوحة (بدون فلترة) — هذا مقصود ومناسب**، لأن التقييمات محتوى عام أصلاً (زي أي منصة تقييمات) — عرضها لأي زائر ليس تسريباً، بعكس بيانات الحجوزات.
- **الكتابة (إنشاء/تعديل تقييم) لا تمر مباشرة عبر anon أصلاً** — تمر عبر `api/submit-review.js` (service_role) اللي يتحقق فعلياً من وجود حجز معتمد مطابق قبل قبول أي تقييم — تصميم آمن أصلاً، غير متأثر بحالة RLS.
- ⚠️ **ثغرة حقيقية واحدة متبقية:** `owner_reply` (رد الصالون على تقييم) يُعدَّل مباشرة عبر `sb()` بمفتاح anon (`App.jsx:5683,5693`) بدون أي تحقق ملكية — حالياً أي شخص يقدر يعدّل رد أي صالون على أي تقييم. خطورة متوسطة (تلاعب بمحتوى عرض الصالون العام، مو تسريب بيانات شخصية) — يحتاج سياسة RLS تربط `reviews.salon_id` بـ`salons.auth_uid = auth.uid()` ضمن Tier 2.
- **ملاحظة IDOR جانبية غير متعلقة بـRLS:** `api/submit-review.js` يثق بـ`customerId` المُرسَل بالطلب دون تحقق هوية فعلي — نظرياً يمكن إرسال تقييم باسم عميل آخر لو عنده حجز معتمد مطابق. خطورة منخفضة-متوسطة (انتحال هوية بتقييم، مو سرقة بيانات) — يحتاج معالجة منفصلة عن Tier 2 (تحقق هوية بالسيرفر، مو RLS).

**`promotions`/`promo_codes` — خطورة مالية/تشغيلية، مو بيانات شخصية:**
- القراءة (عروض نشطة) مفتوحة ومقصودة — محتوى تسويقي عام يفترض يشوفه كل عميل.
- ⚠️ **إنشاء/إلغاء عرض ترويجي** (`App.jsx:6006,6048,6056`) بدون تحقق ملكية — أي شخص نظرياً يقدر يلغي عرض صالون آخر أو ينشئ عرض باسم صالون آخر.
- ⚠️ **تعديل `promo_codes`** (استخدام/انتهاء كود خصم، `App.jsx:5984,6016`) بدون تحقق ملكية — يفتح الباب نظرياً لتلاعب بعداد الاستخدام أو تمديد صلاحية كود خصم بدون وجه حق (إساءة استخدام منطق العمل المالي، مذكورة أصلاً بدليل الفحص الذاتي بند 6).
- لا يوجد بيانات عملاء شخصية (اسم/جوال) بهذين الجدولين — الخطورة مالية/تشغيلية بحتة، ليست خصوصية.

**الخلاصة الشاملة لكل جداول Tier 2 الأربعة:**

| الجدول | نوع الخطورة الفعلية | الأولوية |
|---|---|---|
| `customers` | 🔴 تسريب بيانات شخصية (تم تخفيفه، الجذر لسه مفتوح) | عالية |
| `bookings` | 🔴 تسريب بيانات شخصية (تم تخفيفه، الجذر لسه مفتوح) | عالية |
| `reviews` | 🟡 تلاعب محتوى عام (`owner_reply` فقط) | متوسطة |
| `promotions`/`promo_codes` | 🟡 إساءة استخدام منطق مالي/تشغيلي | متوسطة |

**الحالة: 🔲 الجرد الكامل مكتمل لكل الجداول الأربعة. تصميم وتطبيق سياسات RLS الفعلية (auth.uid()) لكل جدول لسا ينتظر جلسة Tier 2 المخصّصة.**

---

### 2026-07-14 — تحديث `next.js` بلوحة الإدارة (14.2.5 → 14.2.35): الثغرة الحرجة انصلحت، اكتشاف إضافي عن باقي الثغرات

طُبِّق الإصلاح الآمن المخطَّط له سابقاً (`npm install next@14.2.35`)، ثم بناء كامل (`npm run build`) وفحص TypeScript (`tsc --noEmit`) — كلاهما نظيف بدون أخطاء.

**النتيجة بعد التحديث:** الثغرة **الحرجة** (تجاوز صلاحيات) اختفت بالكامل (0 critical، كانت 1). لكن **اكتشاف مهم أثناء التطبيق:** `npm audit` لسا يُظهر عدة ثغرات Next.js متبقية (DoS، XSS، تسميم Cache) — تبيّن إنها **غير مرتبطة بنسخة 14.2.5 تحديداً**، بل موجودة بكل نسخ Next.js 14 كاملة (النطاق المتأثر بالأدلة الرسمية: `9.3.4-canary.0 - 16.3.0-canary.5`) ولا تُصلح إلا بالقفز لـNext.js 16 — قفزة إصدار رئيسي كاسرة (breaking)، مو تحديث patch بسيط كما افترضنا أول مرة بالتقييم الأولي.

**القرار:** الاكتفاء بإصلاح الثغرة الحرجة الآن (خطوة آمنة منخفضة الخطورة، منجزة ومرفوعة على `main`)، وتأجيل القفزة لـNext.js 16 + firebase@12 (كلاهما تغييرات كاسرة تحتاج جلسة اختبار توافق كاملة منفصلة) — نفس منطق تأجيل firebase من التقييم الأولي، بس الآن يشمل جزء من Next.js كمان.

**الحالة: ✅ الثغرة الحرجة مُصلحة ومؤكدة (build + type-check ناجحين). 🔲 باقي ثغرات DoS/XSS بـNext.js + ثغرات firebase تحتاج قفزة إصدار رئيسي، مؤجَّلة لجلسة منفصلة.**

---

### 2026-07-15 — اختبار حي لنقطة 28 (استعادة الرمز السري للصالون): 3 مشاكل بنية تحتية حقيقية اكتُشفت وأُصلحت

أثناء أول اختبار حي فعلي لميزة استعادة الرمز السري (مبنية من L112، لم تُختبر قبل الآن)، واجهنا سلسلة أعطال متتالية — كلها بنية تحتية/إعدادات، مو أخطاء بالكود نفسه:

**1. البريد الافتراضي من Supabase يرسل رابط "Sign in" بس، بدون كود أرقام يُكتب** — بينما `App.jsx` يتوقع من المستخدم يكتب كود 6 أرقام (`resetOtp`, `verifyOtp` بـ`type:'email'`). السبب: قالب "Magic Link" الافتراضي بـSupabase ما يعرض `{{ .Token }}` أصلاً، وتعديل القوالب **مقفول** طالما نستخدم خدمة البريد الافتراضية (رسالة "Set up custom SMTP to edit templates" بالواجهة).

**الحل:** أُعدَّ SMTP خارجي مجاني (**Resend**، بدون بطاقة ائتمان، خطة مجانية 100 رسالة/يوم):
- إنشاء حساب Resend + مفتاح API مخصص (`supabase-smtp`)
- Supabase → Authentication → Emails → SMTP Settings: تفعيل Custom SMTP بـ`smtp.resend.com:465`, username=`resend`, password=مفتاح API، sender=`onboarding@resend.dev` (نطاق تجريبي، بدون توثيق نطاق خاص بعد)
- تعديل قالب "Magic link or OTP" (صار قابل للتعديل بعد تفعيل SMTP) — إضافة `<p>رمز التحقق: <strong>{{ .Token }}</strong></p>`

**⚠️ اكتُشف أثناء هذا الإعداد: كان فيه محاولة سابقة غير مكتملة لإعداد SMTP** (Username محفوظ خطأً كـ"DAWRAK-TEST" بدل "resend"، Host فاضي) — من جلسة سابقة، ما وصلت لنتيجة، ولا حد لاحظ إنها ناقصة لين الليلة.

**2. طول كود OTP بـSupabase كان 8 أرقام (`Email OTP length`)، والتطبيق مبني بثبات على 6 أرقام** (`maxLength={6}`, `.slice(0,6)` بعدة أماكن بـApp.jsx) — أول محاولة فشلت بـ"Token has expired or is invalid" لأن حقل الإدخال قصّ الكود لأول 6 أرقام بس. **الإصلاح:** تغيير `Email OTP length` بإعدادات Supabase (Sign In/Providers → Email) من 8 لـ6 — أسهل وأسلم من تعديل كود التطبيق بعدة أماكن.

**3. بعد نجاح كل الخطوات، اكتشف أحمد إن النظام يقبل نفس الرمز السري القديم كـ"رمز جديد"** بدون أي اعتراض. تحسين أمان/سياسة (مو ثغرة حرجة — الهوية تحقّقت صح عبر البريد+الكود، لكن قبول نفس الرمز يُضعف الفايدة لو كان سبب الاستعادة اشتباه اختراق). **الإصلاح:** `api/salon-reset-pin.js` صار يقارن هاش الرمز الجديد بالقديم، ويرفض لو متطابقين ("الرمز السري الجديد يجب أن يختلف عن الرمز السابق").

**⏳ ملاحظة متبقية غير عاجلة — قرار أحمد (2026-07-15):** الرسائل تصل حالياً لمجلد Spam بجيميل (متوقع، لأن `onboarding@resend.dev` نطاق تجريبي مشترك غير موثّق باسمنا). **الحل الحقيقي الوحيد يحتاج شراء نطاق حقيقي** (زي `dork-app.com`، ~10-15$ سنوياً) — التطبيق حالياً على رابط Vercel المجاني (`dork-app.vercel.app`) فقط، وما يمكن توثيق بريد على نطاق لا تملكه/تتحكم بـDNS تبعه. **قرار أحمد: تأجيل شراء النطاق وتوثيق البريد لجلسة قادمة بذهن صافٍ** (قرار اسم النطاق يستاهل تفكير، مو قرار الساعة 6 فجراً) — مسجّلة كنقطة صريحة، مو منسية. **الميزة تعمل وظيفياً 100% رغم هذا** — المستخدم يحتاج بس يفتح Spam مرة، وتحسين تدريجي مضمون بعد "ليست رسالة مزعجة" اللي ضغطها أحمد.

**تحقق نهائي:** استعادة رمز سري كاملة لصالون "الوادي" الحقيقي — طلب → بريد وصل (Spam) → كود صحيح 6 أرقام → رمز جديد مختلف عن القديم → حفظ ناجح → **دخول فعلي ناجح بالرمز الجديد**.

**الحالة: ✅ نقطة 28 مختبرة حياً بنجاح كامل، أول مرة منذ بنائها بـL112. 3 إصلاحات مرفوعة على `main`.**

---

### 2026-07-15 — تكملة: إضافة مؤقت انتظار إعادة الإرسال + لغز "الإعداد القديم الشغّال" لـSMTP

بعد نجاح نقطة 28، أثناء محاولة اختبار نقطة 29 (استعادة رمز العميل)، ظهرت مشاكل إضافية:

**1. رفض متكرر للكود ("الكود غير صحيح") بشاشتي التسجيل واستعادة الرمز.** السبب الأرجح: عدم وجود مؤقت انتظار حقيقي بين طلبات الكود، فيتولد أكثر من كود بفارق ثوانٍ ويُلغي كل طلب جديد الكود السابق تلقائياً (سلوك طبيعي بـSupabase). **الإصلاح (L126):** إضافة مؤقت "دقيقتين" قبل السماح بإعادة إرسال كود جديد، مع عرض عدّاد واضح وزر "إعادة إرسال" — لشاشتي استعادة رمز الصالون والعميل معاً (لم يكن موجوداً إطلاقاً بشاشة الصالون، وموجود جزئياً بالتسجيل فقط).

**2. أحمد صحّح تحليلاً خاطئاً مني:** ذكر إنه بمرحلة سابقة (قبل هذي الجلسة) كان فيه **كود حقيقي + عداد شغّال + تعبئة تلقائية فوق الكيبورد تعمل 100%** — يعني SMTP خارجي كان مُعدّاً ويعمل فعلاً بمرحلة ما، خلافاً لافتراضي الأولي إنه ما كان مُعدّاً إطلاقاً من قبل.

**تحقيق أثري (بطلب أحمد):** فُتش تاريخ git الكامل (275 كوميت) بحثاً عن أي ذكر لـSMTP/Resend/نطاق بريد — **صفر نتائج** (الإعداد القديم، لو وُجد، تم بالكامل يدوياً بلوحة Supabase، لم يُوثّق بأي ملف). فُحص أيضاً حساب Resend الحالي (قسم Domains) — **"No domains yet"**، يعني ولا نطاق موثّق بهذا الحساب تحديداً.

**الحالة: 🔲 لغز غير محلول — لا يوجد دليل كافٍ لتحديد تفاصيل الإعداد القديم (أي حساب/خدمة/نطاق استُخدم، ومتى ولماذا توقف).** **قرار أحمد: تسجيلها كنقطة، بانتظار جلسة قادمة.** المسار للأمام لا يعتمد على حل هذا اللغز — نفس حل نقطة #94 (شراء وتوثيق نطاق حقيقي) يحل المشكلة من جذورها بغض النظر عن تاريخ الإعداد القديم المفقود.

---

### 2026-07-15 — 🔴 ثغرة حرجة مكتشَفة: تبويب "رقم الجوال" بدخول العميل بدون أي تحقق PIN

أحمد لاحظ (أثناء مراجعة شاشات الدخول استعداداً لاختبار نقطة 29) إن تبويب "رقم الجوال" بشاشة دخول العميل يسجّل الدخول **بمجرد كتابة رقم جوال**، بدون طلب أي رمز سري. تحقق من الكود أكّد الملاحظة 100%:

```js
const loginWithPhone=async()=>{
  const rows=await sb("customers","GET",null,`?...&phone=eq.${phone.trim()}&limit=1`);
  if(!rows.length){...return;}
  const c=toAppCustomer(rows[0]);
  setCustomerSession(c);setView("home"); // دخول مباشر — بدون أي تحقق PIN!
};
```

**الخطورة:** 🔴 حرجة جداً — **استيلاء كامل على أي حساب عميل بمجرد معرفة رقم جواله** (أو حتى تجربة أرقام عشوائية/متسلسلة). أخطر من ثغرة `pin_hash` السابقة، لأنه باب دخول مباشر من داخل واجهة التطبيق نفسها، بدون حاجة لأي أداة تقنية خارجية أو معرفة تقنية.

**اكتشاف إضافي مرتبط أثناء المراجعة:** زر "دخول بالبصمة" **لا يستخدم WebAuthn حقيقي إطلاقاً** — فقط يتحقق من دعم المتصفح نظرياً (`window.PublicKeyCredential`) ثم يقرأ `dork_biometric_id` من localStorage ويُدخل المستخدم مباشرة، بدون أي طلب بصمة/Face ID فعلي من نظام الجهاز. تسمية مضلّلة — فعلياً "دخول تلقائي كآخر مستخدم بهذا الجهاز"، مو تحقق بيومتري حقيقي. **خطورته أقل** (يحتاج وصول فعلي للجهاز، مو هجوم عن بُعد) لكنه لسا خداع للمستخدم بخصوص مستوى الحماية. **قرار أحمد: تسجيلها، التركيز الآن على ثغرة رقم الجوال الأخطر.**

**القرار المتفق عليه لإصلاح ثغرة رقم الجوال:** تبويب "رقم الجوال" يصير يطلب **جوال + PIN معاً** (بنفس نمط تبويب دخول الصالون)، مع تحقق حقيقي بالسيرفر عبر `/api/customer-auth` الموجود أصلاً (محمي بـrate limiting وقفل بعد 5 محاولات) — بدل الاستعلام المباشر غير المحمي الحالي. **رُفض عمداً** اقتراح تعقيد الـPIN بحروف كبيرة/صغيرة/أرقام (Ahmed اقترحها، Claude أوصى ضدها) — القفل بعد 5 محاولات كافٍ لحماية PIN رقمي بسيط، والتعقيد الإضافي يضر تجربة الاستخدام بدون فايدة أمنية تناسب حجم المخاطر. **أُضيف بدلاً منه:** مربع "تذكر رقم الجوال" (نفس نمط صفحة الصالون) — يحفظ الجوال بس، مو الـPIN، فما يقلل الأمان.

**✅ الإصلاح مطبَّق ومرفوع على `main` (L127):**
- تبويب "رقم الجوال" صار يطلب PIN إجبارياً (6 أرقام) بجانب الجوال
- `loginWithPhone` صار يستدعي `/api/customer-auth` (تحقق حقيقي بالسيرفر عبر service_role، محمي بـrate limiting + قفل بعد 5 محاولات فاشلة — تماماً نفس آلية دخول الصالون)
- عند نجاح التحقق: تُطبَّق جلسة Supabase حقيقية (`setSession`) لو رجعت `access_token`/`refresh_token` — نفس نمط Tier 2 الجاري
- مربع "تذكر رقم الجوال" كان موجوداً أصلاً بالكود، لم يحتج تعديل

**الحالة: ✅ الثغرة الحرجة (دخول بمجرد رقم الجوال) مُغلقة بالكامل. مبني ومختبر (build نظيف) — بانتظار اختبار حي من أحمد.**

---

### 2026-07-15 — تحسينات مرافقة لشاشة دخول العميل (L128)

بعد إصلاح ثغرة رقم الجوال، طلب أحمد سلسلة تحسينات UX مرتبطة:

**1. حذف تبويب "دخول سريع" بالكامل** — بعد نقاش (Claude أوصى بالحذف، أحمد وافق): كان يتحقق من PIN **محلياً بالمتصفح فقط** (بدون رحلة للسيرفر)، يعني بدون أي rate limiting أو قفل بعد محاولات فاشلة — أضعف من تبويب "رقم الجوال" الجديد المُصلح. حُذفت دالة `loginWithPin` بالكامل + حالة `loginMethod` + مفاتيح الترجمة اليتيمة (`cust_login.phone_tab`, `pin_tab`, `pin_title`) من الأربع لغات.

**2. زر "الدخول عبر Google" نُقل لتبويب "حساب جديد" فقط** — أُزيل من تبويب الدخول العادي بطلب أحمد (منطقي: Google Sign-In إنشاء/دخول موحّد، أنسب مكانه عند إنشاء الحساب).

**3. رابط "نسيت الرمز السري؟" نُقل ليكون مباشرة تحت خانة PIN** — بدل موضعه القديم (كان بس بالتبويب المحذوف).

**4. تفعيل التعبئة التلقائية (autofill) لكود استعادة رمز الصالون** — شاشة استعادة رمز الصالون كانت تستخدم حقل نص عادي (`<input>`) بدل مكوّن `OtpInput` المخصّص (اللي فيه حقل مخفي بخاصية `autoComplete="one-time-code"` يفعّل اقتراح النظام فوق الكيبورد). استُبدل بمكوّن `OtpInput` نفسه المستخدم بالتسجيل واستعادة رمز العميل — الثلاثة الآن يستخدمون **نفس المكوّن بالضبط**، فأي تحسين مستقبلي (أو نجاح توثيق النطاق لاحقاً) ينعكس على الثلاثة تلقائياً.

**توضيح مهم لأحمد (بعد سؤاله):** التعبئة التلقائية فوق الكيبورد **آلية واحدة** بكل الشاشات (نفس المكوّن)، مو أنظمة منفصلة. اللي كان يظهر شغّال بشاشة "حساب جديد" فقط (بالصور القديمة) وما يظهر بباقي الشاشات، السبب الحقيقي إنه **نفس مشكلة وصول البريد لـSpam** (نقطة #94) — مو فرق بالكود بين الشاشات. الآن كل الشاشات متطابقة تقنياً، والنتيجة النهائية (ظهور الاقتراح فوق الكيبورد أو لا) تعتمد بالكامل على وصول البريد لصندوق الوارد بدل Spam.

**الحالة: ✅ كل التحسينات مبنية ومختبرة (build نظيف)، مرفوعة على `main`.**

---

### 2026-07-15 — 🔴 السبب الجذري الحقيقي وراء "الكود غير صحيح" المتكرر بنقطة 29 (تصادم صامت بين توكنات GoTrue)

بعد إصلاحات L126 (مؤقت الانتظار)، أحمد أعاد اختبار نقطة 29 (استعادة رمز العميل) وحصل **نفس الخطأ من جديد** ("الكود غير صحيح")، رغم إن لقطة الشاشة أثبتت المؤقت شغّال بشكل صحيح (عدّاد "إعادة الإرسال متاحة بعد 0:53" ظاهر ونشط) — يعني كود الاستعادة المُرسَل **حديثاً فعلاً** كان يُرفض، مو كود قديم متكرر الطلب.

**التحقيق:** بمراجعة `api/customer-auth.js` و`api/owner-auth.js` (تحديث L127 هذا الجلسة نفسها لتفعيل تسجيل دخول العميل بجوال+PIN حقيقي)، تبيّن أن كلا الملفين يشغّلان **بصمت** (بكل عملية دخول جوال+PIN ناجحة) الكود التالي لإصدار جلسة Supabase حقيقية لدعم Tier 2 (`auth.uid()`):

```js
const { data: linkData } = await sb.auth.admin.generateLink({ type: "magiclink", email: authEmail });
const otp = linkData?.properties?.email_otp;
const { data: sessionData } = await createAnonClient().auth.verifyOtp({ email: authEmail, token: otp, type: "email" });
```

بالمقابل، شاشة "نسيت الرمز السري" (بكل من `CustomerLogin` و`OwnerLogin`) تستخدم بالضبط نفس الآلية من طرف العميل:

```js
supabase.auth.signInWithOtp({ email: resetEmail.trim(), ... })   // إرسال
supabase.auth.verifyOtp({ email: resetEmail.trim(), token: otpClean, type: "email" })  // تحقق
```

**كلاهما يستخدمان نفس نوع التوكن (`magiclink`/`email`) بقاعدة GoTrue لنفس عنوان البريد.** بحث خارجي (GitHub Issues رسمية بـ`supabase/auth` #784 و#1357) أكّد إن `generateLink`/`signInWithOtp` لنفس الغرض (magiclink) يتشاركان **خانة توكن واحدة فعّالة بأي لحظة لكل مستخدم** — أي طلب جديد (حتى لو صامت وبدون إرسال بريد فعلي، مثل `generateLink` الداخلي هنا) **يُبطل تلقائياً** أي كود سابق لم يُستخدم بعد لنفس الغرض ونفس البريد.

**السيناريو الفعلي المرجّح:** أحمد كان يختبر تسجيل الدخول العادي (جوال+PIN، لتأكيد إصلاح L127) بنفس بريد الاختبار قريباً زمنياً من اختبار "نسيت الرمز السري" (نقطة 29) — كل عملية دخول عادية ناجحة كانت تُولّد صامتاً كود `magiclink` جديد لنفس البريد وتستهلكه فوراً بالسيرفر، مُبطلةً أي كود استعادة كان أحمد بانتظار كتابته بصندوق بريده، **حتى لو وصل حديثاً والمؤقت يعرضه صحيحاً بالواجهة** (المؤقت محلي بالمتصفح فقط، ما له علاقة بصلاحية التوكن الفعلية بالسيرفر).

**الإصلاح (L129):** تغيير نوع التوليد الصامت بالملفين من `magiclink`/`email` إلى `recovery` — خانة توكن **منفصلة تماماً** بقاعدة GoTrue (`recovery_token`)، غير مستخدمة بأي مكان ثانٍ بالتطبيق إطلاقاً (لا يوجد استدعاء `resetPasswordForEmail` بكامل الكود) — فما فيه أي احتمال تصادم مستقبلي معها:

```js
// customer-auth.js + owner-auth.js
generateLink({ type: "recovery", email })
verifyOtp({ email, token: otp, type: "recovery" })
```

**درس جديد يُضاف لقائمة CLAUDE.md الإلزامية:** أي إصدار جلسة Supabase صامت بالسيرفر (`generateLink`) لبريد قد يُستخدم أيضاً بمسار OTP يواجه المستخدم مباشرة (تسجيل/استعادة) **يجب** يستخدم نوع توكن مختلف تماماً (`recovery` بدل `magiclink`) — وإلا كل دخول عادي ناجح يُبطل صامتاً أي كود استعادة/تسجيل قيد الانتظار لنفس البريد، بدون أي رسالة خطأ تدلّ على السبب الحقيقي.

**الحالة: ✅ الإصلاح مبني (build نظيف) ومرفوع — بانتظار إعادة اختبار حي نظيفة (طلب كود جديد بدون أي تسجيل دخول عادي متزامن لنفس الحساب).**

---

### 2026-07-15 — إصلاح ثانٍ (L130): عدم تطابق حساسية الأحرف (case-sensitivity) بين تحقق Supabase Auth واستعلام جدول customers

بعد رفع L129، أحمد أعاد اختبار نقطة 29 — **الكود اتحقق بنجاح هذي المرة** (لا "الكود غير صحيح" — تأكيد إن إصلاح L129 نجح فعلاً)، لكن ظهر خطأ جديد: "لا يوجد حساب بهذا الرقم" (رسالة `cust_login.err_not_found`) بعد التحقق مباشرة.

**السبب:** `verifyResetOtp` بعد نجاح `supabase.auth.verifyOtp` يبحث عن العميل بجدول `customers` عبر:
```js
sb("customers","GET",null,`?...&email=eq.${encodeURIComponent(resetEmail.trim())}&limit=1`)
```
هذا استعلام **حساس لحالة الأحرف** (`eq.` بـPostgREST = مطابقة تامة بالضبط). بالمقابل، `supabase.auth.verifyOtp`/`signInWithOtp` بقاعدة Supabase Auth **غير حساسة لحالة الأحرف** إطلاقاً بمطابقة البريد (سلوك GoTrue الافتراضي، تطبيع تلقائي). البريد بجدول `customers` نفسه يُخزَّن كما كُتب بالضبط وقت التسجيل (`email:email.trim()` بدون `.toLowerCase()`) — فلو اختلفت حالة الأحرف بين وقت التسجيل ووقت كتابة البريد بشاشة الاستعادة (ولو بحرف واحد)، تحقق Auth ينجح لكن استعلام قاعدة البيانات يفشل بصمت (0 نتائج) ويظهر كـ"لا يوجد حساب".

**الإصلاح:** استبدال `email=eq.` بـ`email=ilike.` (مطابقة تامة لكن غير حساسة لحالة الأحرف بـPostgREST، بدون حاجة لـwildcard) — نفس السلوك المتوقَّع من طبقة Auth الآن منعكس بطبقة قاعدة البيانات أيضاً.

**ملاحظة للمستقبل (لم تُنفَّذ الليلة، خارج نطاق الإصلاح الفوري):** الأصح معمارياً تطبيع (`.toLowerCase()`) البريد الإلكتروني وقت التسجيل/الحفظ بكل مكان بالتطبيق، لا الاكتفاء بإصلاح موضعي بكل استعلام — يحتاج مراجعة شاملة لكل مكان يخزّن/يقارن بريد إلكتروني بجلسة منفصلة.

**الحالة: ✅ الإصلاح مبني (build نظيف) ومرفوع — بانتظار إعادة اختبار حي.**

---

### 2026-07-15 — نقطة 29 (استعادة رمز العميل): نجاح كامل بعد إصلاحين + اكتشاف كون الفشل الثالث بيانات اختبار لا كود

بعد رفع L130، أحمد أعاد الاختبار وحصل **نفس الخطأ** ("لا يوجد حساب بهذا الرقم") مرة ثالثة، رغم إن الكود اتحقق بنجاح (لا خطأ "الكود غير صحيح" إطلاقاً هذي المرة ولا سابقتها). بفحص جدول `customers` مباشرة (Table Editor، DAWRAK) تبيّن السبب الحقيقي: **الحساب التجريبي (id=1، "أحمد - تجريبي") كان مسجَّلاً ببريد وهمي `test-customer@dork.internal`** — لا علاقة له ببريد أحمد الحقيقي `moodi1440@gmail.com` الذي كان يكتبه بشاشة الاستعادة. يعني لا L129 ولا L130 كانا فيهما أي خلل متبقٍ — الاستعلام كان يبحث بشكل صحيح تماماً عن بريد **غير موجود أصلاً** بهذا الحساب.

**الإصلاح:** تعديل قيمة عمود `email` لحساب id=1 يدوياً عبر Table Editor إلى `moodi1440@gmail.com`. بعدها **نجحت العملية بالكامل** — تحقق الكود، تسجيل دخول ناجح للتطبيق، مسح الرمز السري القديم من localStorage تلقائياً، وتوجيه لإنشاء رمز جديد من إعدادات الحساب (رسالة "تم مسح رمزك القديم — أنشئ رمزاً جديداً من إعدادات الحساب").

**الحالة: ✅ نقطة 29 مختبرة حياً بنجاح كامل الآن. سلسلة التشخيص الكاملة (تصادم توكن magiclink/recovery → حساسية حالة الأحرف بالبريد → بيانات اختبار خاطئة) مثال جيد على أهمية عدم التوقف عند أول إصلاح "منطقي" — كل طبقة كشفت طبقة مختلفة تحتها.**

---

### 2026-07-16 — بدء نقاش Tier 2: اكتشاف `loadData` (بوابة أوسع من `refreshSalonBookings`) + تصحيح تقييم الخطورة الفعلي + اكتشاف واسع منفصل: فجوة GRANT لـ`authenticated` بـ5 جداول

بطلب أحمد ("نكمل بمشروع Tier 2")، بدأت مراجعة كل نقاط الوصول المباشر لـ`customers`/`bookings` استعداداً لتصميم سياسات RLS. اكتشفت دالة `loadData` (`App.jsx:1844`) — تشتغل **تلقائياً بمجرد فتح التطبيق، لأي زائر، قبل أي تسجيل دخول** — تجيب حتى 1000 حجز (بأسماء وجوالات كل عميل حجز بأي صالون) و500 عميل (اسم/جوال/بريد/موقع/سجل) **بدون أي فلتر صف (row filter)**، فقط `limit`. هذا **مختلف تماماً** عن `refreshSalonBookings` (المُصلحة بـL125) — تلك تشتغل فقط لما عميل مسجّل دخول يفتح "حجوزاتي"، وهذي تشتغل لكل زائر دائماً كجزء من تحميل التطبيق الأساسي. تأكدت بـ`git log -L` إن هذا الاستعلام (بعمودي `customer_name`/`customer_phone` وأعمدة العميل) **لم يُلمس بأي فلتر منذ إنشائه (30 يونيو)**.

**أحمد شكّك بالاكتشاف بقوة (بحق) وطلب تأكيد قاطع** — تحقّق عبر F12 → Network مباشرة على الموقع الحي أثبت الاستعلام يرجع 200 (نجاح) بنمط الرابط المطابق تماماً (بدون أي `id=eq.`/`customer_id=eq.` بالبداية)، **لكن الرد الفعلي احتوى بس على حساب أحمد التجريبي (id=1)** — لأن قاعدة البيانات حالياً ما فيها عميل حقيقي ثانٍ. **تصحيح تقييم الخطورة:** الثغرة حقيقية بالكود/الصلاحيات (ستُسرّب بيانات أي عميل حقيقي ثانٍ فور تسجيله)، لكن **الضرر الفعلي الحالي صفر** — نفس منطق تأجيل Tier 2 الأصلي بـ2 يوليو ("ما فيه عملاء حقيقيين"). أحمد أكّد: لا يوجد عملاء حقيقيون غيره لحد الآن. **القرار:** Tier 2 يُسجَّل كمشروع موثَّق بالكامل، أولوية "قبل انضمام أي عميل حقيقي ثانٍ" — مو طارئ فوري.

**اكتشاف منفصل تماماً أثناء نفس الجلسة (بطلب أحمد "الأسهل، سجّل الباقي"):** أثناء فحص لقطات F12 اللي أرسلها أحمد للتحقق من نقطة `loadData`، ظهرت طلبات فاشلة (403) متكررة لجدولي `promotions` وطلب `customers?id=eq.1`.

1. **`customers?id=eq.1` (403):** تبيّن إنه كود ميت غير آمن بدالة `savePin` (`App.jsx:7493`) — يحاول PATCH مباشر لعمودي `pin`/`pin_length` (النسخة القديمة قبل `pin_hash`) عبر مفتاح anon، يفشل بصمت دائماً (403، هذي الأعمدة غير ممنوحة عمداً). المسار الآمن الحقيقي (استدعاء `/api/customer-auth` بـ`action=set_pin`، بالسطر التالي) كان ينجح دائماً، فالميزة نفسها سليمة. **الإصلاح (L134):** حذف السطر الميت.

2. **`promotions` (403) — اكتشاف أوسع بكثير:** تشخيص عبر `information_schema` كشف إن `promotions_public_read` (سياسة RLS) تسمح `USING: true` للجميع، **لكن منح الجدول الفعلي (GRANT) لدور `authenticated` كان فاضياً تماماً** — صفر SELECT/INSERT/UPDATE، بينما `anon` عنده وصول كامل. بما إن Tier 2 مرحلة 1 (تفعيل جلسات Supabase حقيقية عند الدخول) شغّالة، أي مستخدم مسجّل دخول يبعث توكن `authenticated` بدل `anon` — فينكشف أي جدول ناقص صلاحيات لهذا الدور، بالضبط نفس نمط بند 7 بـCLAUDE.md (لكن بمنح، مو بسياسة).

**فحص شامل استباقي (بدل انتظار كل جدول يُكتشف بالصدفة):** استعلام واحد قارن `anon` مقابل `authenticated` على **كل جداول public دفعة وحدة** — كشف **4 جداول إضافية** بنفس الفجوة:

| الجدول | ناقص عند `authenticated` |
|---|---|
| `promotions` | SELECT, INSERT, UPDATE (+ REFERENCES/TRUNCATE/TRIGGER) |
| `messages` | INSERT, SELECT |
| `notifications` | INSERT |
| `promo_codes` | DELETE, INSERT, SELECT, UPDATE (الكل) |
| `waiting_list` | DELETE, INSERT, SELECT, UPDATE (الكل) |

**الإصلاح المطبَّق على DAWRAK (بتأكيد أحمد، عبر استعلامي GRANT + تحقّق):**
```sql
GRANT SELECT, INSERT, UPDATE, REFERENCES, TRUNCATE, TRIGGER ON promotions TO authenticated;
GRANT INSERT, SELECT ON messages TO authenticated;
GRANT INSERT ON notifications TO authenticated;
GRANT DELETE, INSERT, SELECT, UPDATE ON promo_codes TO authenticated;
GRANT DELETE, INSERT, SELECT, UPDATE ON waiting_list TO authenticated;
```
**تحقّق نهائي:** نفس استعلام المقارنة الشامل رجّع **صفر صفوف** — لا يوجد أي جدول بـ`public` عنده صلاحية لـ`anon` بدون مقابلها عند `authenticated`.

**ملاحظة تصميم مقصودة:** الإصلاح طابق صلاحيات `authenticated` مع `anon` (مو أضاف تقييد ملكية جديد) — الهدف استعادة الوظيفة فقط، مو تشديد الأمان (ملفات `promo_codes`/`waiting_list`/`promotions` أصلاً موثَّقة كنقاط Tier 2 تحتاج تقييد ملكية لاحقاً، هذا الإصلاح لا يغيّرها ولا يسوّئها).

**الحالة: ✅ الإصلاح مطبَّق ومؤكَّد على DAWRAK وDAWRAK-TEST معاً (2026-07-16)** — فحص DAWRAK-TEST قبل التطبيق أظهر **نفس الخمس جداول بالضبط ونفس الفجوات** (لم يُفترض التطابق، تحقّق مسبق فعلي)، ثم طُبِّق نفس سكربت GRANT، والتحقق النهائي رجّع صفر صفوف بالمشروعين. ⏳ متبقٍ: اختبار حي للميزات الأربع (رسائل، إشعارات، أكواد خصم، قائمة انتظار) من حساب مسجّل دخول فعلياً. مشروع Tier 2 الأصلي (سياسات RLS + إصلاح `loadData`) لسا موثَّق بانتظار الأولوية المناسبة.**

**درس جديد يُضاف لـCLAUDE.md:** بعد تفعيل أي نظام جلسات حقيقية (`authenticated` role) على تطبيق كانت كل حركته تمر بـ`anon` سابقاً، يجب فحص شامل لكل الجداول (مقارنة `anon` مقابل `authenticated` بـ`information_schema.table_privileges`) فوراً — بدل انتظار كل ميزة تنكشف بالصدفة واحدة تلو الثانية (زي ما صار هنا مع `customers` سابقاً، ثم `promotions`، واكتشفنا 4 جداول إضافية بس بفحص استباقي واحد).

---

### 2026-07-16 — 🔴→✅ اكتشاف وإصلاح جذري (L135): كل طلبات `PATCH`/`DELETE` بالتطبيق تطلب إرجاع الصف كامل بلا داعٍ — تفشل بصمت لو الصف فيه عمود مستبعَد عمداً

بينما كنا نختبر الميزات الأربع من إصلاح فجوة GRANT، أحمد جرّب حجز موعد جديد ولاحظ **إنه ما يظهر بشاشة "حجوزاتي"** رغم إن الحجز نجح فعلياً. تتبّع Network كشف:

- طلب إنشاء الحجز (`bookings` POST) → **201 نجاح**
- طلب تحديث سجل حجوزات العميل (`customers?id=eq.1` PATCH، يحفظ `history`) → **403 فشل**

**رسالة الخطأ الدقيقة (من تبويب Response):**
```json
{"code":"42501","message":"permission denied for table customers","hint":"Grant the required privileges to the current role with: GRANT SELECT ON public.customers TO authenticated;"}
```

**السبب الجذري:** دالة `sb()` (`App.jsx:126`، المسؤولة عن كل طلبات قاعدة البيانات بالتطبيق) كانت تُرسل `Prefer: return=representation` **لكل الطلبات دون استثناء** (حتى `PATCH`/`DELETE`، السطر كان فعلياً `method === "POST" ? "return=representation" : "return=representation"` — ternary ميت، القيمتان متطابقتان). هذا يطلب من PostgREST إرجاع الصف **كامل** بعد التعديل. بما إن طلب `history` PATCH ما يحدّد `select=` صريح بالرابط، PostgREST يحاول إرجاع كل الأعمدة — بما فيها `pin_hash`/`pin_fails`/`pin_locked_until` المستبعَدة عمداً من GRANT (نفس إصلاح 2026-07-14) — فيفشل **الطلب كامل** (مو بس العمود المستبعَد)، رغم إن تحديث `history` نفسه كان مسموح تماماً.

**فحصت الكود بالكامل: لا يوجد ولا موضع واحد يستخدم القيمة الراجعة من `PATCH` أو `DELETE`** (`grep` لـ`= await sb(...,"PATCH"` و`"DELETE"` رجّع صفر نتائج) — يعني `return=representation` لهذين النوعين كان **غير ضروري إطلاقاً من الأساس**، وهو نفسه سبب فشل صامت محتمل بعدة أماكن ثانية غير `customers` (`salons` PATCH لتغيير اللغة، `reviews` PATCH لـ`owner_reply`، وأي PATCH ثانٍ على جدول فيه عمود مستبعَد من GRANT).

**الإصلاح (L135):**
```js
"Prefer": method === "POST" ? "return=representation" : "return=minimal",
```
`POST` وحدها تحتاج إرجاع الصف (لقراءة الـ`id` التلقائي المُولَّد للحجوزات/التسجيلات الجديدة) — `PATCH`/`DELETE` تستخدمان `return=minimal` الآن (السيرفر يرجّع 204 بدون محتوى، الكود أصلاً ما يقرأ المحتوى لهذين النوعين).

**الأثر المحتمل:** هذا على الأرجح كان يسبب فشلاً صامتاً لعدة عمليات PATCH بمواضع مختلفة من التطبيق منذ إعادة هيكلة صلاحيات `customers`/`salons` على مستوى العمود (2026-07-14) — مو مقتصراً على "حجوزاتي" بس. الإصلاح عام (بدالة `sb()` المركزية)، يحل الفئة كاملة دفعة وحدة.

**الحالة: ✅ مبني (build نظيف) ومرفوع. تأكيد حي 2026-07-16: حجز جديد ظهر فوراً بـ"حجوزاتي" — الإصلاح شغّال.**

---

### 2026-07-16 — 🔴→✅ اكتشاف وإصلاح ثلاثي الطبقات: `customer_messages` (رسائل العميل للصالون) لم تكن تعمل إطلاقاً

أثناء اختبار "حجوزاتي" بعد إصلاح L135، أحمد جرّب إرسال رسالة للصالون من شاشة تفاصيل الحجز — فشلت بـ"خطأ في الإرسال". **أحمد أكّد إن هذي الميزة كانت مُختبرة وشغّالة سابقاً 100% (من العميل وللإدارة)** — احتمال الأقوى إنها ميزة اشتغلت مرة ثم انكسرت لاحقاً (يُحتمل أثناء تنظيف صلاحيات 2026-07-07)، مو معطّلة من التصميم.

**السبب الجذري اتضح متعدد الطبقات (3 فجوات منفصلة، اكتُشفت وحُلّت وحدة تلو الأخرى عبر تكرار الاختبار الحي فعلياً):**

1. **صفر GRANT على الجدول لأي دور** (لا `anon` ولا `authenticated`) — رغم وجود سياستَي RLS (`cm_select`, `cm_insert_customer`) تسمحان نظرياً. **مختلف عن كل الفجوات المكتشفة اليوم** (تلك كانت `anon` عنده وصول و`authenticated` لأ) — هنا الاثنين بلا أي GRANT من الأساس، نفس مبدأ بند 1 بـCLAUDE.md ("RLS Policy وحدها ما تكفي"). **الإصلاح:** `GRANT SELECT, INSERT ON customer_messages TO anon, authenticated;`
2. **صفر GRANT على الـsequence** (`customer_messages_id_seq`) — نفس فخّ "sequence بدون GRANT" (2026-07-07)، فات هذا الجدول تحديداً من ذاك الإصلاح الاستباقي. **الإصلاح:** `GRANT USAGE, SELECT ON SEQUENCE customer_messages_id_seq TO anon, authenticated;`
3. **سياستا RLS مقيّدتان بدور `anon` بس** (`roles: {anon}`) — بمجرد ما صار عند أحمد جلسة `authenticated` حقيقية (Tier 2 مرحلة 1)، ما فيه سياسة تنطبق عليه، فيُرفض الإدراج رغم وجود GRANT صحيح. **الإصلاح:** `ALTER POLICY cm_select ON customer_messages TO anon, authenticated;` و`ALTER POLICY cm_insert_customer ON customer_messages TO anon, authenticated;`

**ملاحظة منهجية:** كل طبقة كانت **مخفية خلف الطبقة اللي قبلها** — رسالة الخطأ تغيّرت 3 مرات مختلفة (`permission denied for table` → `permission denied for sequence` → `new row violates row-level security policy`) مع كل إصلاح، ولم يكن ممكناً معرفة الطبقة التالية إلا بعد حل السابقة وإعادة الاختبار حياً. **لم يُخمَّن أي إصلاح — كل خطوة استندت لرسالة الخطأ الدقيقة من تبويب Response.**

**تحقق نهائي حي (2026-07-16):** إرسال رسالة من حساب العميل التجريبي ← وصلت فوراً بلوحة إدارة الصالون (تبويب "الرسائل" → "العملاء") ← رد الصالون ← وصل فوراً لشاشة العميل. **دورة كاملة ناجحة بالاتجاهين.**

**السبب المرجَّح للانكسار الأصلي:** تنظيف صلاحيات 2026-07-07 (سحب ~184 صلاحية "زايدة" دفعة وحدة بمقارنة تلقائية بين DAWRAK وDAWRAK-TEST) — من المرجَّح إنه سحب GRANT هذا الجدول بالغلط ضمن التنظيف العام، بدون مراعاة إنه جدول له سياسة RLS مخصّصة تعتمد على GRANT منفصل تماماً عنها.

**الحالة: ✅ مُصلح بالكامل ومؤكَّد حياً على DAWRAK. ✅ نفس الثلاث إصلاحات طُبِّقت ومؤكَّدة على DAWRAK-TEST أيضاً (2026-07-16) — فحص مسبق أظهر نفس الفجوات الثلاث بالضبط قبل التطبيق، والتحقق النهائي بعد الإصلاح أكّد تطابقاً تاماً مع DAWRAK.**

---

### 2026-07-16 — ✅ اختبار حي كامل ناجح لكل الأربع ميزات المتأثرة بفجوة GRANT + إصلاح صغير جانبي (L136)

بعد إصلاح الفجوات (الجدولين الخمسة + `customer_messages`)، اختُبرت الأربع ميزات حياً بالترتيب من التطبيق الفعلي:

1. **الرسائل** (`customer_messages`) — رسالة من العميل وصلت للصالون، ورد الصالون وصل للعميل ✅
2. **الإشعارات** (`notifications`) — قبول حجز من الصالون أرسل إشعاراً داخل التطبيق للعميل، ظهر فوراً ✅
3. **قائمة الانتظار** (`waiting_list`) — إضافة عميل لقائمة انتظار صالون نجحت وظهرت بلوحة التحكم ✅
4. **أكواد الخصم** (`promo_codes`/`promotions`) — كود مولَّد من لوحة الإدارة (`DAWRAK`) طُبِّق بنجاح، عرض ترويجي انشأ وظهر للعملاء مع عدّاد تنازلي ✅

**إصلاح صغير جانبي اكتُشف أثناء الاختبار (L136):** عرض ترويجي بمدة 5 دقائق كان يعرض "ينتهي خلال 1 ساعة" رغم إن الوقت الفعلي المتبقي أقل من ساعة — السبب: حساب `hrsLeft` يستخدم `Math.ceil` (تقريب لأعلى)، فأي وقت متبقي بين دقيقة و59 دقيقة يقرَّب لـ"1 ساعة" دائماً. **الإصلاح:** إضافة `minsLeft`، وعرض الدقائق بدل الساعات لما يكون المتبقي أقل من ساعة فعلياً.

**الحالة: ✅ كل الأربع ميزات مؤكَّدة تعمل حياً بالكامل. مشروع فجوة GRANT لـ`authenticated` مغلق تماماً (اكتشاف → إصلاح → تحقق → مزامنة DAWRAK-TEST → اختبار حي) لكل الجداول الستة المتأثرة (`promotions`, `messages`, `notifications`, `promo_codes`, `waiting_list`, `customer_messages`).**

---

### 2026-07-16 — نقطة #31 (عداد إعادة إرسال OTP): اكتشافان إضافيان أثناء الاختبار الحي (L140, L141)

بعد إصلاح L139 (الزر يعرض العدّ التنازلي)، أحمد اختبر حياً وأكّد الزر يتحرك فعلياً (شوهد ينزل من 1:56 إلى 0:06). أثناء نفس الاختبار طلعت مشكلتان إضافيتان:

**1. خطأ حي أوقف التسجيل بالكامل (L140):** فور نجاح التحقق من كود OTP، ظهر خطأ "Ye.rpc(...).catch is not a function" (اسم متغيّر مصغَّر من عملية البناء، يشير لعميل Supabase). السبب: فحص القائمة السوداء (`is_blacklisted`) بدالتي `register()` ودخول Google كان يربط `.catch()` مباشرة على نتيجة `supabase.rpc()` بافتراض إنها ترجع Promise صالح دائماً — لو رجعت بأي شكل غير متوقع (حتى مؤقتاً)، يفشل استدعاء `.catch()` نفسه بخطأ JavaScript صريح، بدل التجاهل اللطيف المقصود أصلاً (`.catch(()=>({data:false}))`). **الإصلاح:** استبدال النمط بـ`try/catch` حقيقي حول `await` بالموضعين (يتعامل مع أي شكل فشل — synchronous throw أو promise rejection — بموثوقية، بعكس `.catch()` المربوط الذي يفترض شكلاً واحداً فقط للفشل).

**2. تناقض تصميمي حقيقي لاحظه أحمد بنفسه قبل حتى ما يجرّب (L141):** توست النجاح يقول "صلاحية الكود 5 دقائق"، لكن مؤقت إعادة الإرسال يسمح بطلب كود جديد بعد **دقيقتين بس** (`resendTimer=120`). يعني لو المستخدم طلب كود جديد بالدقيقة الثانية (مسموح بالواجهة)، يكسر صامتاً كوده الأول رغم إنه لسا يُفترض صالح 3 دقائق إضافية — نفس آلية تصادم التوكن المكتشفة بنقطة 29 (L129)، لكن هذي المرة العميل نفسه (المستخدم) يسبب التصادم بنفسه بدون قصد، بسبب فجوة بين "مدة الصلاحية المعروضة" و"مدة السماح بإعادة الإرسال". **الإصلاح:** توحيد `resendTimer` مع مدة صلاحية الكود الفعلية (300 ثانية بدل 120) بكل الشاشات الثلاث (تسجيل، استعادة عميل، استعادة صالون) — الآن ما يقدر المستخدم يطلب كود جديد إلا لما القديم يكون قارب ينتهي أصلاً، فينتفي احتمال كسر كود لسا صالح بالاستخدام الطبيعي.

**الحالة: ✅ الإصلاحان مبنيان (build نظيف) ومرفوعان — بانتظار إعادة اختبار حي نظيفة للتسجيل الكامل.**

**تحديث (بقرار أحمد بعد نقاش):** أحمد رفض 5 دقائق كمدة انتظار ("كثير") — بعد نقاش قصير حدّد بنفسه **2:50 (170 ثانية)** كقيمة نهائية للاثنين معاً (صلاحية الكود ومدة إعادة الإرسال). طُبِّق بالكود (L142) **وبإعداد Supabase نفسه** (Authentication → Sign In/Providers → Email → "Email OTP expiration" = 170 ثانية، ضبطه أحمد يدوياً وأكّد الحفظ) — بدون مطابقة الإعداد الفعلي بالسيرفر، العداد المعروض بالواجهة كان سيبقى مجرد رقم تجميلي لا يعكس الصلاحية الحقيقية.

---

### 2026-07-16 — 🔴→✅ اكتشاف وإصلاح ثلاثي الطبقات إضافي: تسجيل عميل جديد كان معطَّلاً بالكامل (`customers` بلا INSERT إطلاقاً)

بعد إصلاحات L140/L141/L142، أحمد جرّب تسجيل حساب جديد نظيف وواجه سلسلة أخطاء مألوفة النمط (نفس مسار تشخيص `customer_messages` بالضبط، طبقة تلو الأخرى):

1. **`permission denied for table customers` (POST):** فحص `information_schema.table_privileges` أظهر **صفر INSERT لـ`anon` ولـ`authenticated` على `customers`** — لا حتى فجوة بينهما، الاثنين ناقصين من الأساس، رغم وجود سياسة RLS تسمح به صراحة (`anyone_create_customer`, INSERT, roles=public). **الإصلاح:** `GRANT INSERT ON customers TO anon, authenticated;`
2. **توقّع استباقي طُبِّق فوراً بدون انتظار فشل ثانٍ:** بما إن طلب POST يستخدم `return=representation` بدون `select=` صريح (يحتاجها فعلياً لقراءة الـ`id` التلقائي)، توقّعت نفس فخّ L135 (محاولة إرجاع كل الأعمدة تشمل `pin_hash` المستبعَد) — فأُضيف `select=id,name,phone,email,google_uid,history,favs,location_lat,location_lng,created_at,blocked` صريح لطلبي POST (تسجيل عادي + تسجيل Google) بنفس الكوميت المُصلح للـGRANT (L143)، بدل انتظار الاختبار الحي يكشفه بشكل منفصل.
3. **`permission denied for sequence customers_id_seq`:** بعد الإصلاحين أعلاه، ظهر فخّ الـsequence المعروف (نفس نمط `customer_messages_id_seq`) — **الإصلاح:** `GRANT USAGE, SELECT ON SEQUENCE customers_id_seq TO anon, authenticated;`

**✅ تحقق حي نهائي:** تسجيل حساب عميل جديد كامل (اسم + جوال + بريد + كود تحقق) نجح 100% — دخول تلقائي للصفحة الرئيسية بعد الإنشاء مباشرة.

**✅ مطبَّق ومؤكَّد على DAWRAK وDAWRAK-TEST معاً** (فحص مسبق على DAWRAK-TEST أظهر نفس الفجوة بالضبط، طُبِّق نفس الإصلاح، تحقّق نهائي أكّد تطابقاً تاماً).

**ملاحظة مهمة للمستقبل:** هذا يعني **تسجيل عميل جديد كان معطَّلاً بالكامل على الإنتاج** لفترة غير معروفة (على الأرجح منذ تنظيف صلاحيات 2026-07-07، نفس تاريخ انكسار `customer_messages`) — أي عميل حقيقي حاول يسجّل حساب جديد خلال هذي الفترة كان سيفشل 100%. لحسن الحظ ما فيه دليل إن عملاء حقيقيين حاولوا (التطبيق لسا بمرحلة اختبار)، لكن هذا يبرز أهمية **اختبار حي دوري شامل** لكل المسارات الحرجة (تسجيل، دخول، حجز) بعد أي تنظيف صلاحيات واسع — لا الاكتفاء بمراجعة الكود.

**الحالة: ✅ تسجيل العميل الجديد يعمل بالكامل الآن، مؤكَّد حياً، مطابَق على المشروعين.**
