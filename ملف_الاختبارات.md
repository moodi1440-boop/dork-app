# ملف الاختبارات — Dork App

> **الغرض:** كل نقطة منجزة في الفحص الشامل تبدأ هنا بـ 🔲 — أحمد يضع ✅ فقط بعد الاختبار الفعلي بنفسه.
> **آخر تحديث:** 2026-06-30

---

## القسم الأول: SQL يجب تشغيله في Supabase أولاً

> هذه النقاط **لن تعمل** إلا بعد تشغيل SQL في Supabase SQL Editor.

| # | الميزة | كيف تختبر | الحالة |
|---|--------|-----------|--------|
| 8 | CHECK constraints على أعمدة status/attendance + COMMENT ON COLUMN | Supabase → Table Editor → جدول `bookings` → Column `status` → تحقق وجود CHECK constraint | 🔲 |
| 26 | حذف تلقائي لـ OTP القديم عبر pg_cron | SQL: `SELECT * FROM cron.job WHERE jobname LIKE '%otp%';` — يجب أن يظهر job | 🔲 |
| 29 | مراقبة Table Bloat شهرياً | SQL: `SELECT * FROM table_bloat_monitor;` — يجب أن ترجع صفوف | 🔲 |
| 39 | RLS على جميع الجداول | SQL: `SELECT tablename, rowsecurity FROM pg_tables WHERE schemaname='public';` — كل tablename يجب أن يكون rowsecurity=true | 🔲 |
| 40 | Audit Triggers على bookings/salons/customers | أجرِ DELETE على حجز → SQL: `SELECT * FROM admin_audit_log ORDER BY created_at DESC LIMIT 5;` — يجب أن تظهر العملية | 🔲 |
| 51 | جدول bookings_archive ودالة الأرشفة | SQL: `SELECT to_regclass('public.bookings_archive');` — يجب أن لا ترجع NULL | 🔲 |
| 73 | JWT Expiry=3600s + Reuse Detection | Supabase Dashboard → Authentication → Settings → JWT expiry = 3600 + Enable reuse detection = ON | 🔲 |
| 76 | UNIQUE constraint لمنع الحجز المزدوج + Idempotency Key | حاول إرسال نفس الحجز مرتين بنفس الـ idempotency_key → يجب أن يفشل الثاني | 🔲 |
| 81 | Database Indexes على الأعمدة الأكثر استعلاماً | SQL: `SELECT indexname FROM pg_indexes WHERE tablename IN ('bookings','salons','customers');` — يجب أن تظهر indexes | 🔲 |
| 84 | عمود auth_uid في جدول salons | SQL: `SELECT column_name FROM information_schema.columns WHERE table_name='salons' AND column_name='auth_uid';` — يجب أن يظهر الصف | 🔲 |

---

## القسم الثاني: اختبارات الأمان والدخول

| # | الميزة | كيف تختبر | الحالة |
|---|--------|-----------|--------|
| 1 | تشفير PIN العميل (SHA-256) | سجّل PIN → افتح DevTools → Application → localStorage → تأكد القيمة المخزنة لا تشبه الرقم العادي | 🔲 |
| 2 | الحماية ضد SQL Injection و XSS | أدخل `<script>alert(1)</script>` في حقل الاسم → يجب ألا ينفّذ الكود | 🔲 |
| 13 | API Keys محمية — service role غير مكشوف | DevTools → Network → افتح أي طلب Supabase → تحقق أن الـ Authorization header يستخدم anon key فقط | 🔲 |
| 17 | بوابة دخول الأدمن | راجع `docs/login-gateway-tests.md` § 3 ونفّذ الخطوات | 🔲 |
| 18 | بوابة دخول الصالونات | راجع `docs/login-gateway-tests.md` § 2 ونفّذ الخطوات | 🔲 |
| 19 | بوابة دخول العملاء (OTP + PIN) | راجع `docs/login-gateway-tests.md` § 1 ونفّذ الخطوات | 🔲 |
| 20 | npm audit — صفر ثغرات | شغّل: `npm audit --production` → يجب أن تظهر "0 vulnerabilities" | 🔲 |
| 37 | Rate Limiting على API | حاول الدخول 6 مرات متتالية خلال دقيقة → يجب الحجب عند المحاولة الخامسة أو السادسة | 🔲 |
| 57 | صد Brute Force — 5 محاولات خاطئة | أدخل PIN خاطئ 5 مرات → يجب ظهور رسالة "الحساب مقفل" مع الوقت المتبقي | 🔲 |
| 58 | تشفير PIN الصالون (hashOwnerPin) | Supabase → جدول `salons` أو `owners` → عمود `pin_hash` → القيمة لا تشبه الرقم العادي | 🔲 |
| 59 | سجل نشاط دخول الصالون | سجّل دخول صالون → SQL: `SELECT * FROM admin_audit_log ORDER BY created_at DESC LIMIT 3;` | 🔲 |
| 60 | اختبار API Keys في الإنتاج | افتح الكود في GitHub → ابحث عن service_role → يجب ألا تجده | 🔲 |
| 68 | قفل OTP بعد 5 محاولات خاطئة | راجع `docs/login-gateway-tests.md` § 1 — اختبار فشل OTP | 🔲 |
| 75 | CORS Whitelist | DevTools → Network → Headers على أي API call → تحقق وجود `Access-Control-Allow-Origin` بالـ domain الصحيح فقط | 🔲 |

---

## القسم الثالث: اختبارات الواجهة والتجربة

| # | الميزة | كيف تختبر | الحالة |
|---|--------|-----------|--------|
| 5 | البناء يعمل (583KB chunks منفصلة) | شغّل `npm run build` → لا أخطاء + حجم app chunk < 600KB | 🔲 |
| 21 | سياسة الخصوصية | افتح التطبيق → ابحث عن رابط سياسة الخصوصية → يجب أن تفتح وتُقرأ | 🔲 |
| 22 | FAQ لكل ميزة رئيسية | افتح قسم المساعدة في التطبيق → تأكد وجود إجابات للأسئلة الشائعة | 🔲 |
| 24 | ضغط الصور (JPEG 75%, 150px) | غيّر صورة البروفايل → DevTools → Network → حجم الصورة المُرسَلة < 100KB | 🔲 |
| 25 | Circuit Breaker (3 فشل → 30 ثانية توقف) | أوقف الإنترنت → حاول فتح التطبيق → بعد 3 محاولات يظهر البانر الأحمر العلوي | 🔲 |
| 44 | ضغط الصور WebP تلقائياً | غيّر صورة → Network Tab → تحقق أن نوع الصورة المُرسَلة WebP أو JPEG (مش PNG كبير) | 🔲 |
| 50 | Realtime بين الصالون والعميل | افتح التطبيق على هاتفين → اصنع حجز من العميل → يظهر فوراً في لوحة الصالون | 🔲 |
| 52 | المناطق الزمنية (تخزين UTC، عرض +3) | اصنع حجز → في Supabase تحقق أن الوقت محفوظ UTC → في التطبيق يظهر بتوقيت السعودية | 🔲 |
| 62 | Color Contrast ≥ 4.5:1 | بدّل بين الثيمات الأربعة (dark/dim/light/lgray) → النصوص مقروءة في كل ثيم | 🔲 |
| 63 | Responsive — iPhone SE (375px) | افتح Chrome DevTools → اختر iPhone SE → تأكد لا يوجد scroll أفقي في أي صفحة | 🔲 |
| 64 | عملية الحجز (5 خطوات خفيفة) | أكمل حجزاً كاملاً → تأكد أن التدفق واضح ومريح | 🔲 |
| 65 | Dark Mode متوافق (4 ثيمات) | بدّل الثيمات الأربعة → كل شيء مرئي ولا عناصر مختفية | 🔲 |
| 66 | Toast Notifications للأحداث | احجز → رسالة نجاح خضراء. أدخل بيانات ناقصة → رسالة خطأ حمراء | 🔲 |
| 67 | Accessibility — خط ≥ 10px في مؤشر الخطوات | تأكد أن أرقام مؤشر خطوات الحجز والتسجيل قابلة للقراءة | 🔲 |
| 69 | Offline Handling | فصل الإنترنت → يظهر بانر "الخدمة متوقفة مؤقتاً — سيُعاد المحاولة خلال 30 ثانية" | 🔲 |
| 70 | Push Notifications (iOS + Android) | افتح الإعدادات → فعّل الإشعارات → اصنع حجز → يجب وصول إشعار | 🔲 |

---

## القسم الرابع: اختبارات الأداء

| # | الميزة | كيف تختبر | الحالة |
|---|--------|-----------|--------|
| 42 | Core Web Vitals — TTFB < 200ms, FCP < 1.5s | Vercel Dashboard → Analytics → Web Vitals → تحقق القيم | 🔲 |
| 43 | Lazy Loading — vendor chunks منفصلة | DevTools → Network → Disable cache → تحقق وجود ملفات vendor-react, vendor-supabase منفصلة | 🔲 |
| 45 | Cache الصالونات (localStorage 24h) | افتح التطبيق → أغلقه → أعد الفتح → تظهر قائمة الصالونات فوراً بدون loading | 🔲 |
| 46 | Vercel Function Duration | Vercel Dashboard → Functions → تحقق أن Duration < 500ms لكل function | 🔲 |
| 83 | Core Web Vitals كـ KPIs | نفس نقطة 42 — Vercel Dashboard → Analytics | 🔲 |

---

## القسم الخامس: اختبارات البنية التحتية

| # | الميزة | كيف تختبر | الحالة |
|---|--------|-----------|--------|
| 11 | حذف الكود الميت (_lib/request.js) | تأكد وجود ملف `_lib/request.js` وأن `readJson` و`getSalonId` تُستخدم من الـ API | 🔲 |
| 31 | لا console.log في الإنتاج | افتح المتصفح على الرابط الرسمي → DevTools → Console → لا رسائل غير ضرورية | 🔲 |
| 33 | Security Audit قبل كل إصدار | شغّل `npm audit && cd admin && npm audit` → صفر ثغرات عالية | 🔲 |
| 38 | .env غير موجود في git | شغّل: `git log --all -- .env` → لا نتائج | 🔲 |
| 53 | تسجيل أخطاء API في admin_audit_log | ابحث في admin_audit_log عن `action='API_ERROR'` بعد أي خطأ server-side | 🔲 |
| 55 | RLS على Realtime — العميل لا يرى بيانات غيره | سجّل دخول بعميلين مختلفين → تأكد أن الإشعارات لا تختلط | 🔲 |
| 56 | Cleanup عند الخروج من الصفحة | DevTools → Network → WebSocket → أغلق التبويب → تأكد إغلاق الاتصال | 🔲 |
| 72 | RLS على جميع الجداول | نفس نقطة 39 — SQL: `SELECT tablename, rowsecurity FROM pg_tables WHERE schemaname='public';` | 🔲 |
| 74 | Service Role للعمليات الحساسة فقط | تحقق أن App.jsx لا يحتوي على service_role key — يستخدم anon فقط | 🔲 |
| 77 | Sentry Crash Reporting | أضف `VITE_SENTRY_DSN` في Vercel → ابحث عن خطأ مقصود → يظهر في Sentry dashboard | 🔲 |
| 78 | Graceful Degradation | نفس نقطة 69 — البانر يظهر بدل تجميد التطبيق | 🔲 |
| 79 | npm audit في CI/CD | GitHub → Actions → آخر build → تأكد خطوة npm audit تعمل | 🔲 |
| 80 | Security Headers (CSP + HSTS) | DevTools → Network → أي طلب → Headers → تحقق من: X-Frame-Options, Content-Security-Policy, Strict-Transport-Security | 🔲 |
| 82 | Incident Response Runbook | افتح `docs/incident-response-runbook.md` → تأكد أن الخطوات واضحة وقابلة للتنفيذ | 🔲 |

---

## القسم السادس: Auth الصالونات (الجديد)

> ⚠️ هذا النظام جديد — يتطلب تشغيل migration script أولاً (نقطة 86 ⏳ بانتظار أحمد)

| # | الميزة | كيف تختبر | الحالة |
|---|--------|-----------|--------|
| 85 | Migration Script جاهز | شغّل: `node scripts/migrate-salon-auth.js --dry-run` → لا أخطاء | 🔲 |
| 87 | owner-auth.js يدعم OTP | بعد تشغيل المايجريشن — سجّل دخول صالون بـ OTP بدل PIN فقط | 🔲 |
| 88 | sb() تستخدم authToken | DevTools → Network → طلبات الصالون → Authorization header يحتوي session token وليس anon key | 🔲 |
| 89 | OwnerLogin ينشئ session بعد الدخول | بعد الدخول → DevTools → Application → Supabase Auth session موجودة | 🔲 |
| 90 | register-salon.js ينشئ Supabase Auth user | سجّل صالون جديد → Supabase Dashboard → Authentication → Users → يظهر المستخدم الجديد | 🔲 |

---

## ملخص

| الحالة | العدد |
|--------|-------|
| ✅ تم اختباره | 0 |
| 🔲 لم يُختبر بعد | 55 |
| **المجموع** | **55** |
