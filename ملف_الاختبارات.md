# ملف الاختبارات — Dawrak App

> **الغرض:** كل نقطة منجزة تبدأ بـ 🔲 — أحمد يقول "تم" وكلود يضع ✅.
> **آخر تحديث:** 2026-06-30

---

## القسم الأول: SQL يجب تشغيله في Supabase أولاً

> هذه الميزات **لن تعمل** إلا بعد تشغيل SQL في Supabase SQL Editor.

| # | الميزة | خطوة الاختبار | الحالة |
|---|--------|--------------|--------|
| 8 | CHECK constraints على أعمدة status/attendance | Supabase → Table Editor → جدول `bookings` → عمود `status` → تحقق وجود CHECK constraint | 🔲 |
| 26 | حذف OTP القديم تلقائياً عبر pg_cron | SQL: `SELECT * FROM cron.job WHERE jobname LIKE '%otp%';` — يجب أن يظهر job | 🔲 |
| 29 | مراقبة Table Bloat شهرياً | SQL: `SELECT * FROM table_bloat_monitor;` — يجب أن ترجع صفوف | 🔲 |
| 39 | RLS على جميع الجداول | SQL: `SELECT tablename, rowsecurity FROM pg_tables WHERE schemaname='public';` — كل جدول rowsecurity = true | 🔲 |
| 40 | Audit Triggers على bookings/salons/customers | احذف حجزاً → SQL: `SELECT * FROM admin_audit_log ORDER BY created_at DESC LIMIT 5;` — تظهر العملية | 🔲 |
| 51 | جدول bookings_archive ودالة الأرشفة | SQL: `SELECT to_regclass('public.bookings_archive');` — لا ترجع NULL | 🔲 |
| 73 | JWT Expiry=3600s + Reuse Detection | Supabase → Authentication → Settings → JWT expiry = 3600 + Enable reuse detection = ON | 🔲 |
| 76 | UNIQUE constraint لمنع الحجز المزدوج | أرسل نفس الحجز مرتين بنفس idempotency_key → الثانية تفشل | 🔲 |
| 81 | Database Indexes على الأعمدة الأكثر استعلاماً | SQL: `SELECT indexname FROM pg_indexes WHERE tablename IN ('bookings','salons','customers');` — تظهر indexes | 🔲 |
| 84 | عمود auth_uid في جدول salons | SQL: `SELECT column_name FROM information_schema.columns WHERE table_name='salons' AND column_name='auth_uid';` — يظهر الصف | 🔲 |

---

## القسم الثاني: اختبارات الأمان والدخول

| # | الميزة | خطوة الاختبار | الحالة |
|---|--------|--------------|--------|
| 1 | تشفير PIN العميل (SHA-256) | سجّل PIN → DevTools → Application → localStorage → القيمة المخزنة ليست رقماً عادياً | 🔲 |
| 2 | الحماية ضد SQL Injection وXSS | أدخل `<script>alert(1)</script>` في حقل الاسم → لا ينفّذ | 🔲 |
| 13 | service_role key غير مكشوف | DevTools → Network → أي طلب Supabase → Authorization header = anon key فقط | 🔲 |
| 17 | بوابة دخول الأدمن | راجع `docs/login-gateway-tests.md` § 3 ونفّذ الخطوات | 🔲 |
| 18 | بوابة دخول الصالونات | راجع `docs/login-gateway-tests.md` § 2 ونفّذ الخطوات | 🔲 |
| 19 | بوابة دخول العملاء (OTP + PIN) | راجع `docs/login-gateway-tests.md` § 1 ونفّذ الخطوات | 🔲 |
| 20 | npm audit — صفر ثغرات | شغّل: `npm audit --production` → 0 vulnerabilities | 🔲 |
| 37 | Rate Limiting على API | حاول الدخول 6 مرات خلال دقيقة → محجوب عند المحاولة الخامسة | 🔲 |
| 57 | صد Brute Force — 5 محاولات خاطئة | أدخل PIN خاطئ 5 مرات → رسالة "الحساب مقفل" مع الوقت المتبقي | 🔲 |
| 58 | تشفير PIN الصالون (hashOwnerPin) | Supabase → جدول salons/owners → عمود pin_hash → القيمة ليست رقماً عادياً | 🔲 |
| 59 | تسجيل نشاط الدخول في audit_log | سجّل دخول صالون → SQL: `SELECT * FROM admin_audit_log ORDER BY created_at DESC LIMIT 3;` | 🔲 |
| 60 | service_role غير موجود في git | ابحث في GitHub في الكود عن "service_role" → لا تجد قيمة حقيقية | 🔲 |
| 68 | قفل OTP بعد 5 محاولات خاطئة | راجع `docs/login-gateway-tests.md` § 1 — قسم فشل OTP | 🔲 |
| 75 | CORS Whitelist | DevTools → Network → Headers على أي API call → Access-Control-Allow-Origin = domain الصحيح فقط | 🔲 |

---

## القسم الثالث: اختبارات الواجهة والتجربة

| # | الميزة | خطوة الاختبار | الحالة |
|---|--------|--------------|--------|
| 5 | البناء يعمل (chunks منفصلة < 600KB) | شغّل `npm run build` → لا أخطاء + app chunk < 600KB | 🔲 |
| 21 | سياسة الخصوصية | افتح التطبيق → ابحث عن رابط سياسة الخصوصية → تفتح وتُقرأ | 🔲 |
| 22 | FAQ للميزات الرئيسية موجودة | افتح قسم المساعدة → تأكد وجود أسئلة وإجابات لكل ميزة رئيسية | 🔲 |
| 23 | دقة إجابات FAQ | اقرأ FAQ → تحقق أن الإجابات تصف الميزات الحالية بدقة (لا ميزات محذوفة أو معلومات قديمة) | 🔲 |
| 24 | ضغط الصور (JPEG 75%, 150px) | غيّر صورة البروفايل → DevTools → Network → حجم الصورة المُرسَلة < 100KB | 🔲 |
| 25 | Circuit Breaker (3 فشل → 30 ثانية توقف) | أوقف الإنترنت → حاول فتح التطبيق → بعد 3 محاولات يظهر البانر الأحمر | 🔲 |
| 34 | توافق الميزات بين التطبيق والأدمن | افتح لوحة الأدمن → تأكد أن كل حجز/رسالة/عرض موجود في التطبيق يظهر في الأدمن | 🔲 |
| 44 | ضغط الصور WebP تلقائياً | غيّر صورة → Network Tab → نوع الصورة المُرسَلة WebP أو JPEG (ليس PNG كبير) | 🔲 |
| 50 | Realtime بين الصالون والعميل | افتح التطبيق على هاتفين → احجز من العميل → يظهر فوراً في لوحة الصالون | 🔲 |
| 52 | المناطق الزمنية (UTC → توقيت السعودية) | اصنع حجز → في Supabase الوقت UTC → في التطبيق يظهر بتوقيت +3 | 🔲 |
| 62 | Color Contrast ≥ 4.5:1 في الثيمات الأربعة | بدّل dark/dim/light/lgray → النصوص مقروءة في كل ثيم | 🔲 |
| 63 | Responsive — iPhone SE (375px) | Chrome DevTools → iPhone SE → لا scroll أفقي في أي صفحة | 🔲 |
| 64 | عملية الحجز (5 خطوات خفيفة) | أكمل حجزاً كاملاً → التدفق واضح ومريح | 🔲 |
| 65 | Dark Mode متوافق (4 ثيمات) | بدّل الثيمات الأربعة → كل شيء مرئي ولا عناصر مختفية | 🔲 |
| 66 | Toast Notifications | احجز → رسالة نجاح خضراء. أدخل بيانات ناقصة → رسالة خطأ حمراء | 🔲 |
| 67 | Accessibility — خط 10px في مؤشر الخطوات | تأكد أن أرقام مؤشر خطوات الحجز والتسجيل مقروءة | 🔲 |
| 69 | Offline Handling | فصل الإنترنت → يظهر بانر "الخدمة متوقفة مؤقتاً — سيُعاد المحاولة خلال 30 ثانية" | 🔲 |
| 70 | Push Notifications (PWA) | الإعدادات → فعّل الإشعارات → اصنع حجز → يصل إشعار | 🔲 |

---

## القسم الرابع: اختبارات الأداء

| # | الميزة | خطوة الاختبار | الحالة |
|---|--------|--------------|--------|
| 42 | Core Web Vitals — TTFB < 200ms, FCP < 1.5s | Vercel Dashboard → Analytics → Web Vitals → تحقق القيم | 🔲 |
| 43 | Lazy Loading — vendor chunks منفصلة | DevTools → Network → Disable cache → تظهر ملفات vendor-react/vendor-supabase منفصلة | 🔲 |
| 45 | Cache الصالونات (localStorage 24h) | افتح التطبيق → أغلقه → أعد الفتح → قائمة الصالونات تظهر فوراً بدون loading | 🔲 |
| 46 | Vercel Function Duration | Vercel Dashboard → Functions → Duration < 500ms لكل function | 🔲 |
| 83 | Core Web Vitals كـ KPIs في Vercel | Vercel Dashboard → Analytics → Web Vitals موجودة ومحدّثة | 🔲 |

---

## القسم الخامس: اختبارات البنية التحتية

| # | الميزة | خطوة الاختبار | الحالة |
|---|--------|--------------|--------|
| 11 | حذف الكود الميت (_lib/request.js) | تأكد وجود `_lib/request.js` وأن readJson وgetSalonId تُستخدم من الـ API | 🔲 |
| 12 | تنظيف branches الميتة | شغّل: `git branch -r` → لا branches قديمة غير مستخدمة | 🔲 |
| 14 | الفحص الرباعي عند كل إصدار | افتح `docs/incident-response-runbook.md` § 6 → نفّذ قائمة الفحص الرباعي قبل Deploy | 🔲 |
| 31 | لا console.log في الإنتاج | افتح التطبيق الرسمي → DevTools → Console → لا رسائل غير ضرورية | 🔲 |
| 33 | Security Audit قبل كل إصدار | شغّل `npm audit && cd admin && npm audit` → صفر ثغرات عالية | 🔲 |
| 38 | .env غير موجود في git | شغّل: `git log --all -- .env` → لا نتائج | 🔲 |
| 41 | API endpoints موثّقة في /docs | افتح `docs/api-endpoints.md` → كل endpoint موثّق ويطابق الكود الفعلي | 🔲 |
| 49 | مطابقة تقارير الأرباح (شهرياً) | قارن أرقام admin/finance مع سجلات الصالون الفعلية — يجب أن تتطابق | 🔲 |
| 53 | تسجيل أخطاء API في admin_audit_log | ابحث في admin_audit_log عن `action='API_ERROR'` بعد أي خطأ server-side | 🔲 |
| 55 | RLS على Realtime — عميل لا يرى بيانات غيره | سجّل دخول بعميلين مختلفين → الإشعارات لا تختلط | 🔲 |
| 56 | Cleanup عند إغلاق الصفحة | DevTools → Network → WebSocket → أغلق التبويب → الاتصال ينقطع | 🔲 |
| 72 | RLS على جميع الجداول | SQL: `SELECT tablename FROM pg_tables WHERE schemaname='public' AND rowsecurity=false;` → لا نتائج | 🔲 |
| 74 | service_role في server-side فقط | ابحث في App.jsx عن "service_role" → لا توجد | 🔲 |
| 77 | Sentry Crash Reporting | أضف VITE_SENTRY_DSN في Vercel → أحدث خطأ مقصود → يظهر في Sentry dashboard | 🔲 |
| 78 | Graceful Degradation عند انقطاع الخدمة | نفس نقطة 69 — البانر يظهر بدل تجميد التطبيق | 🔲 |
| 79 | npm audit في CI/CD | GitHub → Actions → آخر build → خطوة npm audit تعمل | 🔲 |
| 80 | Security Headers (CSP + HSTS) | DevTools → Network → أي طلب → Headers → X-Frame-Options + Content-Security-Policy + HSTS موجودة | 🔲 |
| 82 | Incident Response Runbook مكتمل | افتح `docs/incident-response-runbook.md` → كل قسم مكتمل وقابل للتنفيذ | 🔲 |
| 91 | لا select=* في الكود | شغّل: `grep -r "select=\*" src/ api/` → صفر نتائج | 🔲 |

---

## القسم السادس: Auth الصالونات (الجديد)

> ⚠️ يتطلب تشغيل Migration Script أولاً (نقطة 86 ⏳ بانتظار أحمد)

| # | الميزة | خطوة الاختبار | الحالة |
|---|--------|--------------|--------|
| 85 | Migration Script جاهز | شغّل: `node scripts/migrate-salon-auth.js --dry-run` → لا أخطاء | 🔲 |
| 87 | owner-auth.js يدعم OTP | بعد تشغيل المايجريشن — سجّل دخول صالون بـ OTP | 🔲 |
| 88 | sb() تستخدم authToken | DevTools → Network → طلبات الصالون → Authorization = session token وليس anon key | 🔲 |
| 89 | OwnerLogin ينشئ session | بعد الدخول → DevTools → Application → Supabase Auth session موجودة | 🔲 |
| 90 | register-salon.js ينشئ Supabase Auth user | سجّل صالون جديد → Supabase → Authentication → Users → يظهر المستخدم | 🔲 |

---

## ملخص

| الحالة | العدد |
|--------|-------|
| ✅ تم اختباره | 0 |
| 🔲 لم يُختبر بعد | 71 |
| **المجموع** | **71** |
