# النقاط المتبقية — Dork App

## ✅ نقاط منجزة

### 1. إكمال ترجمة اللغات 100%
- العربية ✅ مكتملة
- الإنجليزية ✅ مكتملة
- التركية ✅ مكتملة
- الأوردية ✅ مكتملة

### 2. نصوص عربية مشفرة مباشرة في الكود ✅
تم إنشاء قسم `ui` بـ 142 مفتاح في ملفات الترجمة الأربعة (ar/en/ur/tr)، واستبدال جميع النصوص المرئية في JSX بـ `{t('ui.key')}` أو `t('ui.key')` حسب السياق.

### 3. ترجمة الإشعارات حسب لغة المستقبِل ✅
- عمودا `lang` أُضيفا في Supabase (تم تشغيل SQL)
- الإشعارات بين الصالون والعميل تصل بلغة كل شخص
- Push notifications + إشعارات قاعدة البيانات كلها مترجمة

### 4. ترجمة رسائل Toast وإشعارات البث ✅ (43 رسالة + 5 إشعارات)
- جميع رسائل toast المشفرة بالعربية استُبدلت بـ `i18n.t('ui.KEY')`
- أُضيف 43 مفتاح جديد لقسم `ui` في ملفات الترجمة الأربعة
- إشعارات البث الخمسة (تعديل موعد، قائمة انتظار، إلغاء حجز) تعمل بـ `ntxt(lang).type()`
- تم تعديل `NOTIF_TEXTS` بإضافة 5 أنواع جديدة في اللغات الأربع
- البناء `npx vite build` ✅ نجح بدون أخطاء

---

## ❌ نقاط مفتوحة — تحتاج تحقق في الجلسة القادمة

### 1. الدردشة — الصالون لا يستطيع الرد على العميل
- الخطأ: "فشل الإرسال — أعد المحاولة"
- الملف: `api/owner-chat.js` سطر 120 — `Number(null)=0` يسبب 400
- الإصلاح المطبق في L96: `bookingId` أصبح اختياريًا، والشرط أصبح `!customerId || !text` فقط
- **يحتاج تحقق فعلي في الإنتاج**

### 2. الدردشة — الرسائل تختفي عند التنقل بين الأقسام
- العميل يفتح قسماً آخر ويرجع → الدردشة تختفي
- الملف: `App.jsx` سطر 7785 — `openChatBookingId` يُعاد لـ null عند unmount
- الإصلاح المطبق في L96: حفظ الحالة في `sessionStorage`
- **يحتاج تحقق فعلي في الإنتاج**

### 3. الدردشة — وصول الرسائل متأخر (يحتاج تحديث الصفحة)
- الملف: `App.jsx` سطرا 6254 و6399 — polling كل 8 ثوانٍ
- الإصلاح المطبق في L96: تسريع polling إلى 3 ثوانٍ
- ملاحظة: Supabase Realtime يحتاج `REPLICA IDENTITY FULL` على جدول `customer_messages` لتعمل الـ realtime بشكل كامل — لم يُشغَّل بعد
- **يحتاج تحقق فعلي في الإنتاج**

### 4. تصدير CSV — نص مشوه وكل البيانات في عمود واحد
- المشكلة: BOM خارج `encodeURIComponent` في data URI → ترميز مكسور → Excel/Numbers يقرأ كل شيء كنص في عمود واحد
- الملف: `admin/src/lib/csv.ts`
- الإصلاح المطبق في L96: استبدال data URI بـ `Blob` + `createObjectURL` مع BOM داخل محتوى الـ Blob
- **يحتاج تحقق فعلي بعد نشر Admin على Vercel**

---

## 📌 قواعد
- لا تنشر إصلاحاً منفرداً — اجمع النقاط في نشرة واحدة
- لا ترفع إلا بأمر صريح من أحمد
- النقاط تُرقَّم تسلسلياً (1، 2، 3...)
