# سياق المشروع — تطبيق Dork (للقراءة قبل البدء بأي إصلاح)

## البنية
- `/home/user/dork-app/App.jsx` — تطبيق العميل/صاحب الصالون (React/Vite, عربي RTL, بدون TypeScript)
- `/home/user/dork-app/admin/` — لوحة الإدارة (Next.js 14 + TypeScript + Tailwind، App Router)
- قاعدة بيانات Supabase مشتركة بين الاثنين

## فخاخ حرجة يجب معرفتها قبل أي تعديل

- **الثيمات (App.jsx):** useEffect بـApp.jsx:844-883 يضبط متغيرات CSS: `--shell-bg, --surface-1, --surface-2, --border-ui, --text-primary, --text-muted, --bg-input, --p`. `var(--p)` يصير أسود تقريبًا (#1c1c1e) فقط بثيم lgray — أي عنصر يستخدمه كنص فوق خلفية غامقة ثابتة يختفي بـlgray.

- **الثيمات (admin/):** ThemeProvider.tsx + globals.css يعمل override بـ!important لكلاسات Tailwind الشائعة فقط بثيمي light/lgray. القيم بصيغة `bg-[#xxxxxx]`/`text-[#xxxxxx]`/inline style لا تُغطّى — هذي مصدر أخطاء الألوان بالإدارة.

- **الحجز (App.jsx):** `toBooking` (App.jsx:1081-1089) يحفظ اسم العميل بمفتاح `.name` فقط — أي كود يقرأ `.customerName` أو `.customer_name` سيحصل دايمًا undefined.

- **middleware.ts:** `if (isApi) return NextResponse.next()` — كل /api/* كان يعبر بدون تحقق (مُصلَح الآن، لكن تذكّر هذا عند أي تعديل أمني).

- **الأيقونات:** مكتبة SVG كاملة موثّقة في `ICONS.md`. App.jsx: 104/106 رمز فريد منجز. admin/: 78/78 منجز. نقاط الارتكاز الرئيسية: `NotifIcon`، `LabelWithIcon`، `Row`، مصفوفات `BACKGROUNDS`/`THEME_OPTIONS`/`PACKAGES`.

---

## آخر إصدار مُنشَر: v1.3.19

---

## ❌ نقاط جديدة — مفتوحة

_(لا توجد نقاط مفتوحة حالياً — تُضاف هنا فور اكتشافها بالترقيم المتسلسل)_

---

## 📌 قواعد مُسجَّلة — لا تُخالَف أبداً

- **لا تنشر إصلاحاً صغيراً منفرداً** — اجمع كل النقاط المعلقة في نشرة واحدة.
- **لا ترفع إلا بأمر صريح من أحمد.**
- **لا تعدّل إلا بعد الموافقة.**
