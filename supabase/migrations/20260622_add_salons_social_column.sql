-- إضافة عمود social (روابط التواصل الاجتماعي) لجدول salons.
-- العمود غير موجود فعلياً بقاعدة البيانات الحية، وهذا سبب حذفه سابقاً من
-- استعلام التحميل في App.jsx (loadData) لتفادي خطأ 42703 الذي كان يعطّل
-- تحميل كل الصالونات. هذا الترحيل إضافي بحت (ADD COLUMN IF NOT EXISTS)
-- ولا يلمس أي بيانات موجودة — آمن للتشغيل في أي وقت.
--
-- بعد تشغيله بنجاح على قاعدة البيانات الحية، يجب إعادة إضافة "social" إلى
-- نص استعلام select في App.jsx (loadData) ليصل العمود فعلياً للعميل.
ALTER TABLE salons ADD COLUMN IF NOT EXISTS social jsonb DEFAULT '{}'::jsonb;
