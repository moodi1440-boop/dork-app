# Dork App - اتفاق العمل مع Claude

---

## 1. طريقة العمل
- خطوة بخطوة، استشارة قبل التنفيذ، بدون تسرع
- كل خطوة تُعرض على المستخدم قبل الإكمال

---

## 2. قبل أي تعديل
- أخذ نسخة احتياطية (git commit) قبل أي تعديل
- عرض الخطة والانتظار لـ "OK" قبل التنفيذ

---

## 3. الرفع للـ GitHub والنشر
- الرفع بعد الموافقة فقط، مع رسالة commit واضحة، بدون force push بدون إذن
- خطوات الرفع الإلزامية بالتسلسل:
  1. اختبار محلي — بدون أخطاء
  2. git commit برسالة واضحة
  3. git push للـ GitHub
  4. شيك PR: "Able to merge" + "All checks passed" + "No conflicts"
  5. اضغط "Merge pull request"
  6. انتظر deployment Vercel
  7. قل "وصل التحديث" فقط بعد تأكيد الـ deployment
- لو في conflict: أحله من GitHub مباشرة
- لا أقول "وصل" إلا لما كل شيء تمام

---

## 4. الاختبار
- التأكد من عدم وجود أخطاء قبل الرفع
- شرح النتائج بوضوح للمستخدم

---

## 5. الترابط والإدارة
- أي إضافة مهمة (PIN، بريد، رقم جوال) = يجب تكون في Admin Panel + 2-way sync
- الإضافات المهمة تُضاف في التطبيق + الإدارة معاً بدون سؤال
- الإضافات البسيطة (ألوان، خطوط) → أسأل أولاً
- عند الشك → اسأل

---

## 6. الأمان والبيانات الحساسة
- بدون حذف بيانات العملاء بدون إذن صريح
- حماية البيانات الشخصية والحساسة
- التعامل الآمن مع كلمات المرور والـ PIN

---

## 7. الاختبار على جميع الأجهزة
- اختبار على: iPhone + iPad + Android + الكمبيوتر قبل الرفع

---

## 8. توثيق التغييرات
- رسالة commit واضحة تشرح السبب وليس فقط الماذا

---

## 9. النسخ الاحتياطية
- git هو المرجع — لا تحذف الملفات القديمة بدون سبب

---

## 10. التوافقية والأداء
- بدون إبطاء التطبيق، التأكد من التوافقية مع جميع الإصدارات والأجهزة

---

## 11. التواصل في الحالات الطارئة
- عند أي مشكلة غير متوقعة (Vercel error، GitHub conflict، bug) → أخبر المستخدم فوراً قبل الاستمرار

---

## 12. تسهيل سير العمل
- أعطِني السياق الكامل في البداية — لو قلت "باشتغل" انتظر النتيجة بدون تقاطع

---

## 13. التركيز على المطلوب فقط
- أشتغل على بالضبط اللي طُلب، بدون إضافات أو تحسينات غير مطلوبة
- لو رأيت تحسينات → أسأل أولاً

---

## 14. اسأل عند عدم الفهم
- بدون تخمين — أي نقطة غير واضحة → اسأل فوراً قبل الشغل

---

## 15. الاستباقية بالأسئلة
- أنا أسأل أسئلة استباقية لأفهم الاحتياجات، لا أنتظر الشرح دائماً

---

## 16. اللغة العربية
- جميع الردود والملخصات بالعربية دائماً
- الكود والأوامر التقنية بالإنجليزي، الشرح بالعربي

---

## 17. معيار التطبيقات الكبرى
- مع كل ميزة جديدة: أشرح كيف تسويها التطبيقات الكبرى (Airbnb، Talabat) والفرق عن طريقتنا
- المستخدم يقرر بعد الشرح

---

## 18. التزامن اللحظي الثلاثي
- أي ميزة تمس البيانات تكون لحظية: عميل ↔ صالون ↔ إدارة
- الأدوات: Supabase Realtime + Polling 10 ثواني + visibilitychange

---

## 19. قواعد استعلامات قاعدة البيانات (NON-NEGOTIABLE)

**يُمنع منعاً باتاً `select=*`:**
```javascript
// ❌ ممنوع
sb("bookings", "GET", null, "?select=*")
// ✅ صحيح
sb("bookings", "GET", null, "?select=id,salon_id,date,status")
```

**القواعد الإلزامية:**
1. تحديد الأعمدة دائماً — ممنوع `select=*`
2. فلتر WHERE إلزامي لجداول: `bookings`, `messages`, `waiting_list`
3. `limit` إلزامي: Messages=50 | Reviews=20 | Bookings=100-500 | Customers=500
4. RLS مفعّل — أضف `Authorization: Bearer ${sessionToken}` في Headers
5. لا Realtime مفتوح بدون cleanup:
```javascript
return () => channel.unsubscribe()
```
6. Circuit Breaker: توقف بعد 3 فشل متتاليين، انتظر 30 ثانية
7. البيانات الثابتة (مدن، إعدادات) في localStorage — Cache 7 أيام
8. لا صور خام — استخدم Thumbnails/Optimized دائماً

**فحص ذاتي قبل كل commit:**
- [ ] بدون `select=*`؟
- [ ] كل استعلام فيه `limit`؟
- [ ] كل Realtime subscription فيها `unsubscribe()`؟
- [ ] البيانات الثابتة في localStorage؟

---

## 20. نظافة الكود (Zero-Redundancy)
- عند نقل كود: يُحذف من مكانه القديم فوراً
- عند حذف ميزة: حذف كامل (Functions + Components + Variables + Imports + Routes + JSON keys)
- ممنوع وجود Dead Code أو كود معلق

---

## 21. بروتوكول التكامل البرمجي (Binding)
1. **الملف كاملاً دائماً:** يُمنع تقديم أجزاء من الكود — دائماً الملف كاملاً
2. **حرمة الكود القائم:** لا حذف أو استبدال سطر موجود إلا بموافقة صريحة
3. **حذف الميزة = حذف شامل:** Functions + Components + Variables + Imports + Routes + JSON keys
4. **حظر الإعادة:** ميزة حُذفت بقرار → محظور إعادتها إلا بإذن صريح في نفس الجلسة
5. **حظر git revert:** يُمنع الرجوع لتاريخ git قديم بدون إذن صريح — عند الطريق المسدود: توقف واشرح، لا تتصرف منفرداً
6. **تحقق من الأسطر:** إضافة ميزة → الأسطر تزيد | حذف ميزة → الأسطر تنقص
7. **تعارض منطقي:** توقف فوري وإبلاغ المستخدم — لا تعديل حتى يُحل التعارض

---

## 22. الجراحة البرمجية والمسؤولية
1. **قفل النطاق:** أعمل فقط على ما طُلب — ما لم يُذكر = محظور اللمس
2. **المسؤولية الكاملة:** تعديل A أثّر على B → أنا المسؤول، أُصلحه فوراً بلا مبررات
3. **إعلان قبل البدء:** "سأعدل: [الملف/السطر] — لن أمس: كل ما عدا ذلك"
4. **بروتوكول الشك:** إذا التعديل قد يمس جزءاً آخر → أتوقف وأخبر المستخدم فوراً
5. **تقرير بعد الإنجاز:** ماذا عدّلت + ماذا تركت + أي خطر جانبي

---

## 23. الصدق في الإنجاز وانضباط الرفع
1. **"تم" بعد التحقق البصري فقط** — الظن لا يكفي
2. **إذا التحقق يحتاج اختباراً من المستخدم:** قل صراحةً "التأكد الكامل يحتاج اختباراً من طرفك"
3. **فشلان بنفس النهج = توقف كامل:** شخّص السبب الجذري واقترح نهجاً مختلفاً
4. **ترتيب الرفع الإلزامي:** تحقق ← دمج ← رفع ← رقم الإصدار
5. **رقم الإصدار إلزامي** بعد كل رفع ناجح (مثال: L113)

---

## 24. قائمة الميزات المحذوفة بقرار (محظور إعادتها بدون إذن صريح)

- **تصدير CSV** (لوحة الإدارة) — حُذفت بقرار بجلسة سابقة (قبل 2026-07-04). بقي أثرها اسم ملف مضلّل `csv.ts` كان يحتوي فعلياً تصدير PDF فقط — أُعيدت تسميته لـ`pdf.ts` بجلسة 2026-07-04 (تأكيد صريح من أحمد إن هذا ليس إعادة لميزة CSV، فقط تصحيح اسم ملف).
- **نقاط الولاء (Loyalty Points)** — حُذفت بالكامل من التطبيق بقرار أحمد بجلسة 2026-07-14. كانت غير مفعّلة عملياً أصلاً (لا واجهة استخدام، لا آلية اكتساب تلقائي عند الحجز — قيمة يدوية بس بلوحة الإدارة). الحذف شمل: سؤال الأسئلة الشائعة (4 لغات)، `admin/src/app/api/settings/route.ts` (LOYALTY_DEFAULTS + منطق القراءة/الحفظ)، `admin/src/app/api/customers/[id]/route.ts` (loyalty_points/loyalty_frozen من الحقول القابلة للتعديل)، صفحة عملاء الإدارة (عمود التجميد + شارة النشاط + تصدير PDF)، سجل التدقيق. **تحديث 2026-07-14:** بتأكيد صريح من أحمد، حُذفت الأعمدة فعلياً من قاعدة البيانات على **DORK وDORK-TEST معاً** (`ALTER TABLE customers DROP COLUMN loyalty_points, loyalty_frozen` + `ALTER TABLE app_settings DROP COLUMN loyalty_settings`) — الحذف نهائي ومكتمل بكل المستويات (كود + قاعدة بيانات).

---

## 25. أولوية معالجة فجوة RLS المفتوحة (Tier 2)

**قرار أحمد (2026-07-14):** مشروع Tier 2 (بناء نظام هوية حقيقي `auth.uid()` للصالونات والعملاء + إعادة كتابة سياسات RLS لتحل محل `USING (true)` المفتوحة على `customers`/`bookings`/`reviews`/`promo_codes`) **يُنفَّذ أولاً، قبل أي نقطة أخرى متبقية** (بما فيها #28/#29 استعادة الرمز السري، والأخطاء الصغيرة #18/#30/#31/#32) — يلغي/يتجاوز قرار "الأسهل أولاً" السابق نفس اليوم لخصوص هذي النقطة تحديداً.

**السبب:** هذي أخطر فجوة أمان مفتوحة حالياً — لا تحتاج أي "اختراق" تقني، بس طلب شبكي مباشر بمفتاح anon العام (المكشوف أصلاً بكود أي تطبيق Supabase) يقدر يقرأ/يعدّل بيانات أي عميل أو حجز أو تقييم مباشرة، بدون تسجيل دخول وبدون كتابة كود.

**التفاصيل الكاملة والحل المخطَّط (4 خطوات):** `تقرير-أمان-RLS.md` (قسم "Tier 2")، `أمان-التطبيق-وقاعدة-البيانات.md` (نقاط أمان عاجلة)، ونقطة #93 بـ`الفحص-الشامل.md`.

---

*آخر تحديث: 2026-07-14*
