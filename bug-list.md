# سياق المشروع — تطبيق Dork (للقراءة قبل البدء بأي إصلاح)

## البنية
- `/home/user/dork-app/App.jsx` — تطبيق العميل/صاحب الصالون (React/Vite, عربي RTL, ~7900 سطر، بدون TypeScript)
- `/home/user/dork-app/admin/` — لوحة الإدارة (Next.js 14 + TypeScript + Tailwind، App Router)
- قاعدة بيانات Supabase مشتركة بين الاثنين

## فخاخ حرجة يجب معرفتها قبل تصحيح أي بند لون/ثيم
- App.jsx: نظام الثيمات (dark/dim/light/lgray) يتحكم به useEffect بـApp.jsx:844-883 يضبط متغيرات CSS: `--shell-bg, --surface-1, --surface-2, --border-ui, --text-primary, --text-muted, --bg-input, --p` (ومشتقات rgba: `--pa08,--pa25,--pr,--gold,--gold-rgb`). `var(--p)` يصير أسود تقريبًا (#1c1c1e) فقط بثيم lgray (بدل الذهبي الافتراضي) — أي عنصر يستخدمه كنص فوق خلفية غامقة ثابتة (مثل #13131f/#1a1a2e) يختفي بـlgray.
- admin/: نظام الثيمات بـThemeProvider.tsx + globals.css يستخدم `--adm-bg/--adm-card/--adm-text/--adm-muted` + يعمل override بـ!important لكلاسات Tailwind الشائعة (.text-white, .text-gray-300..600) فقط بثيمي light/lgray (globals.css:58-92). لكن الـoverride ما يغطي القيم العشوائية بصيغة `bg-[#xxxxxx]`/`text-[#xxxxxx]`/inline style — هذي هي مصدر كل أخطاء الألوان بالإدارة، وليس text-white نفسه (مؤكدة أنها مُغطّاة بـCSS صحيح).
- App.jsx: الحجز (booking object) من `toBooking` (App.jsx:1081-1089) يحفظ اسم العميل بمفتاح `.name` فقط — أي كود يقرأ `.customerName` أو `.customer_name` من نفس الكائن سيحصل دايمًا undefined.
- App.jsx: `custDashNav` (App.jsx:827، الحالة الافتراضية `{tab:"settings",section:false}`) تتحكم بعرض شاشة "بروفايل العميل القديمة" (كرت + تبويب إعدادات قديم بتعديل البيانات/PIN/الموقع/حذف الحساب). الفريق نقل كل هذي الوظائف لمسارات مستقلة جديدة بالـdrawer (custEditData, custSettingsTheme...) لكن نسي تحديث دالة `register()` (App.jsx:6751-6793) — فكل عميل جديد يسجّل حساب يشوف الشاشة القديمة المهجورة لحظة واحدة فقط (انظر بند #34 و#62).
- admin/middleware.ts: `if (isApi) return NextResponse.next()` — كل /api/* يعبر بدون أي تحقق دخول على مستوى middleware (لازم تتذكر هذا قبل أي تصحيح أمني، الحل الصحيح هو فلترة كل route بشكل صريح لا الاعتماد على middleware فقط).

## قائمة المشاكل الكاملة — 71 نقطة موثّقة (file:line لكل بند)

### 🔴 أمنية حرجة
1. كل API الإدارة بلا تحقق دخول — middleware.ts: `if (isApi) return NextResponse.next()`.
2. بوابة المالك بلا كلمة مرور — api/owner/auth/route.ts يسجّل دخول برقم الجوال فقط.
3. حذف جماعي لكل الإشعارات بلا تحقق — api/notifications DELETE `{all:true}`.
4. المالك يقدر يعدّل أي حقل بصالونه — api/owner/salon/route.ts PATCH بلا فلترة حقول.
5. Mass-assignment بتعديل العملاء/الصالونات من الإدارة — api/customers/[id], api/salons/[id].
6. صلاحيات RLS مفتوحة على fcm_tokens — `USING (true)`.
7. كلمة مرور الإدارة افتراضية ضعيفة "admin123" + admin/.env.example متتبَّع بـgit وفيه مفاتيح Firebase/Supabase حقيقية.
71. جلسة المالك قابلة للتزييف بالكامل (IDOR) — api/owner/bookings, api/owner/messages, api/owner/salon, api/owner/waiting-list (route.ts بكل واحد) تقرأ كوكي dork_owner_session وتستخدم قيمته مباشرة كـsalonId بدون توقيع (HMAC/JWT) وبدون إعادة تحقق من قاعدة البيانات. أي تعديل لقيمة الكوكي من أدوات المطوّر = وصول كامل لأي صالون آخر. أعمق وأخطر من #2.

### 🟠 أخطاء وظيفية تكسر الميزة
8. تغيير كلمة مرور الإدارة يقفل الدخول نهائياً — /api/auth يحدّث DB، لكن middleware.ts يقارن الكوكي بـprocess.env.ADMIN_SECRET مباشرة.
9. صفحة /owner غير موجودة (404) رغم API سليم.
10. بيانات الحجوزات متضاربة بين مصدرين (bookings مقابل salons.bookings القديم).
11. api/finance معلّق وغير محمي.
12. خدمة الإشعارات معطّلة بصمت — 3 ملفات تسجّل /service-worker.js لكن الملف الفعلي الوحيد public/sw.js.
13. تضارب أنظمة الإشعارات (Firebase/FCM بالإدارة × Web Push/VAPID بالتطبيق).
14. كود ميت — NotificationInitializer.tsx (admin) غير مستخدم.
15. هوية إدارة وهمية ثابتة (userId:1 بكل أكواد FCM).
16. أسرار حقيقية أخرى داخل .env.example المتتبَّع.

### 🟡 ميزات باك-أوفيس ناقصة
17. لا يوجد RBAC/حسابات إدارة متعددة.
18. لا يوجد Audit Log.
19. لا يوجد نظام نزاعات/استرجاع أموال.
20. لا يوجد توثيق مستندات عند الموافقة.
21. لا يوجد تقرير عمولات حقيقي.
22. لا يوجد نظام دعم/تذاكر.
23. لا توجد Bulk actions.
24. لا يوجد تصدير بيانات حقيقي (غير زر CSV واحد).
25. لا يوجد رصد صحة النظام.
26. لا يوجد Rate-limiting على /api/auth و /api/owner/auth.

### مسارات منفصلة
27. سياسة الخصوصية/الشروط (App.jsx) — مسودة جاهزة، حذف الحساب غير مكتمل (لا يحذف الحجوزات/المراجعات، حذف Auth يفشل بصمت — لاحظ: confirmDeleteAccount بـApp.jsx:7077-7085 يستدعي `supabase.auth.admin.deleteUser()` من كود client-side، وهذا غالبًا غير صالح بدون service-role key).
28. شات العميل↔️الصالون — تصميم فقط، صفر كود (لكن انظر #67، فيه فعليًا كود شات أساسي بـApp.jsx مع خلل لون).

### ألوان وتباين — App.jsx
29. علامة ✓ سوداء غير مقروءة بثيم lgray — App.jsx:3004 (+5351, 5392, 5452) — `color:"#000"` على `var(--p)`.
30. App.jsx:4411 — نص أبيض ثابت، عنوان قسم "الأقرب".
31. App.jsx:4430 — اسم الصالون يختفي بالثيم الفاتح.
32. App.jsx:5003 (وُصف سابقًا كـ5005) — اسم العميل بتقويم المالك يختفي بالثيم الفاتح.
33. App.jsx:6980 — اسم الصالون بلوحة الحضور يختفي.
34. App.jsx:7130-7141 + 7364-7392 — كرت بروفايل العميل + تبويب settings القديم بالكامل: تصميم قديم متروك (انظر فخ #4 أعلاه)، خلفية ثابتة + var(--p) أسود بـlgray = اختفاء كامل للاسم/الجوال/التاريخ. الإصلاح الصحيح: تحديث register() (App.jsx:6787) ليوجّه لنفس مسارات الإعدادات الجديدة، لا تلوين شاشة مفروض تكون مهجورة.
35. App.jsx:7209 — اسم الصالون بقائمة الانتظار يختفي.
36. App.jsx:7326 — عنوان الإشعار يختفي.
37. App.jsx:7571 — تسمية منتقي الثيم نفسها غير مقروءة.
38. App.jsx:7747 — رأس قسم الأسئلة الشائعة غير مقروء.
39. App.jsx:1694 — بانر خطأ الاتصال بقاعدة البيانات بخلفية ثابتة #3a1a1a غير متجاوبة مع الثيم.
40. App.jsx:4347 — ميزة "مقارنة الصالونات": اسم الخدمة بنص أبيض ثابت داخل كرت var(--surface-1).
41. App.jsx:4885 — تبويب التقييمات بلوحة المالك: اسم العميل بلون #c0c0d0 فاتح جدًا داخل كرت var(--surface-1).
42. App.jsx:4897 — نفس التبويب: رد المالك بلون #c8c8a8.
43. App.jsx:5680 — فقاعة شات العميل↔️الصالون بخلفية ثابتة #1a1a2e.
44. App.jsx:7715 — نص خطوات الدليل بقسم الإعدادات بلون #ccc مباشرة على خلفية الصفحة.

### اسم مفقود فعليًا (ليس تباين ألوان) — جذر واحد، موقعين
45. App.jsx:4710 — مربع "الحجز القادم" بلوحة المالك يقرأ `_nextBk.customerName||_nextBk.customer_name`، لكن toBooking يحفظ الاسم تحت `.name` فقط.
46. App.jsx:4750 — نفس الخلل بالضبط، بلوحة "حجوزات غداً" — `b.customerName||b.customer_name`.

### اعتماد على إيموجي بدل أيقونات
47. ~259 إيموجي بـApp.jsx + ~101 بالإدارة، صفر مكتبة أيقونات عدا 9 SVG يدوية.

### تضارب بيانات ثلاثي (مزامنة الإدارة↔️التطبيق)
48. روابط التواصل الاجتماعي للصالون لا تصل للعميل — App.jsx:1073 وtoAppSalon لا يجلبان عمود social.
49. نافذة الإلغاء (cancellation_window) غير موجودة بنموذج تعديل الصالون بالإدارة.
50. كلمة مرور المالك غير قابلة للعرض/الإعادة من الإدارة.
51. رصيد المالك "المدفوع" يظهر دايمًا صفر — toAppSalon لا يجلب total_paid.

### ألوان لوحة الإدارة
52-69. حقول إدخال بخلفية ثابتة bg-[#0d0d1a] بـ7 صفحات (promo-codes×7, notifications×5, contact×4, customers, salons, owner-login)، فقاعة رسالة bg-[#1a1a2e] (messages/page.tsx:92)، طبقات مودال bg-black/70×2، توست bg-[#0f1117] (Sidebar.tsx:102)، ظل ثابت #333 بصفحة المظهر (appearance/page.tsx:70)، شريط مقارنة bg-[#0d0d1a] (compare/page.tsx:93)، text-white ثابت بـlayout.tsx:28 (هذا الأخير غير ضار فعليًا — مغطى بـCSS override، أُبقي بالقائمة للتوثيق فقط).
70. admin/.../owner-login/page.tsx:59 — كرت نموذج الدخول نفسه (لا فقط الحقول 49/64) بخلفية bg-[#13131f] وحد border-[#2a2a3a].

### كود ميت إضافي — App.jsx
- DEMO_SALONS (797-801).
- شاشة Splash معطّلة كاملة (834 + 1384-1396).
- toAppBooking() غير مستخدمة (249-264).
- googleCalLink() غير مستخدمة (717-722).
- HomeReviewsSection (139 سطر، 2195-2333) غير مستخدم.
- LocFilter غير مستخدم (2601-2612).

### خطأ وظيفي مستقل (انتقال ناقص بعد إعادة التصميم)
62. register() (App.jsx:6751-6793) لا يستدعي setCustDashNav() قبل التنقل لـcustDash — كل عميل جديد يسجّل حساب يشوف واجهة قديمة مهجورة (بند #34) بدل تجربة الـdrawer المعاد تصميمها.

## تعليمات لبدء العمل بالدردشة الجديدة
- هذي القائمة (71 نقطة) هي السجل المعتمد الوحيد — أي مشكلة جديدة تُكتشف تُضاف بنفس الترقيم المتسلسل (72، 73...)، ولا تُسجَّل بملف خارجي، فقط بالدردشة مباشرة.
- لم يُنفَّذ أي تصحيح فعلي على أي بند لحد الآن (وقت كتابة هذي القائمة) — كل العمل لحد هذي اللحظة فحص/توثيق فقط.
- البنود الأمنية (1-7، 71) تحتاج نقاش قبل التنفيذ خصوصًا #7 (تدوير المفاتيح الحقيقية المسرّبة بـgit يحتاج خطوة خارج الكود نفسه).
- البند #34/#62 يُصحَّح معًا كوحدة واحدة (تحديث register()، لا تلوين الشاشة القديمة).

## حالة التنفيذ (آخر تحديث: فرع claude/review-agreement-file-l0igua، حتى كوميت 19bcfb9)

✅ = مؤكد بالفحص المباشر للكود الحالي | ❌ = ما زال مفتوحاً | ⚠️ = مُصحَّح جزئياً/يحتاج قرارك

1. ✅ middleware يحمي كل /api الآن
2. ✅ بوابة المالك أصبحت PIN + قفل بعد 5 محاولات
3. ✅ حذف الإشعارات الجماعي محمي بمصادقة الأدمن (لا تأكيد إضافي داخل الراوت نفسه، لكن لا يمكن الوصول له بدون تسجيل دخول أصلاً)
4. ✅ المالك مقيّد بقائمة حقول محددة (OWNER_EDITABLE_FIELDS)
5. ✅ تقييد mass-assignment بالعملاء والصالونات (ADMIN_EDITABLE_FIELDS)
6. ✅ سياسات RLS لـfcm_tokens (وأعمدة PIN المالك) مُشغّلة فعلياً على القاعدة الحية — أكّد المستخدم تشغيل supabase/migrations/20260621_owner_pin_and_fcm_tokens_rls.sql
7. ✅ ADMIN_SECRET بـ.env.example تم تغييره من "admin123" الفعلية إلى placeholder غير صالح (CHANGE_ME_...)؛ مفاتيح Firebase الأخرى بالملف (API_KEY, AUTH_DOMAIN...) آمنة بطبيعتها فعليًا (مفاتيح ويب عامة محمية بقواعد Firebase، ليست أسرار) ولا تحتاج تغييراً
8. ✅ تغيير كلمة مرور الأدمن لا يقفل الدخول (يقرأ من DB عبر getAdminPassword)
9. ✅ بُنيت صفحة /owner فعليًا (حجوزات، رسائل، قائمة انتظار، إعدادات أساسية للصالون) تستخدم نقاط /api/owner/* الموجودة مسبقًا والمحمية بجلسة المالك — كانت هذه الـAPI جاهزة بالكامل ومحمية لكن بلا أي واجهة، فالمسار كان يرجع 404 فعليًا. تعديل الخدمات/الأسعار/الحلاقين ما زال متاحًا من تطبيق الصالون فقط (خارج نطاق هذا الإصلاح)
10. ✅ admin/api/customers/[id] وadmin/api/stats أصبحا يقرآن من جدول bookings الموحّد (مثل بقية واجهات الإدارة) — api/stats كان لا يزال يقرأ عمود salons.bookings القديم المهجور (لا يكتب فيه أي كود فعليًا)، فأرقام لوحة التحكم (الإيرادات/الحجوزات) كانت تعتمد على عمود ميت؛ تم التصحيح بالكامل، لا تضارب مصدر بيانات بعد الآن
11. ✅ api/finance منفّذ بالكامل ومحمي
12. ✅ مسار service-worker موحّد (كل الإشارات تستخدم /sw.js)
13. ✅ تحقّق فعلي: النظامان ليسا متعارضين بالاستخدام — FCM لمتصفح الإدارة فقط (admin/) وVAPID لمتصفح المالك/العميل (App.jsx)، جمهور منفصل كلياً بلا تداخل. توحيدهما بمعنى دمج بنية تحتية واحدة يحتاج إعادة هيكلة ضخمة لفائدة عملية ضعيفة، لذا لا يُنصح به. ⚠️ ملاحظة أهم اكتُشفت بالفحص: كلا النظامين معطّل فعلياً من ناحية الإرسال — لا send-fcm-notification ولا أي دالة لإرسال push حقيقي يُستدعى من أي مكان بالكود حالياً (تسجيل توكنات فقط، صفر إشعارات فعلية تُرسل). هذا خارج نطاق هذا البند تحديداً (تفعيل إرسال فعلي ميزة جديدة لا إصلاح تعارض) لكن يستحق بنداً منفصلاً لو احتاج المستخدم إشعارات push حقيقية لاحقاً
14. ✅ NotificationInitializer.tsx غير موجود (كود ميت محذوف فعلياً)
15. ✅ بعد توفّر RBAC الآن، FirebaseProvider.tsx يجيب هوية الأدمن الحقيقية من جلسته (id فعلي بجدول admin_users عبر /api/me) بدل الرقم الوهمي الثابت 1 — لو لم تكن هناك جلسة RBAC حقيقية (تسجيل دخول بكلمة المرور الموحّدة القديمة، لا id رقمي حقيقي لها)، لا يُسجَّل أي توكن إطلاقاً بدل اختراع هوية وهمية
16. ✅ نفس تصحيح #7 — لا توجد أسرار حقيقية فعلية متبقية بـ.env.example (FIREBASE_SERVICE_ACCOUNT_JSON كان مجرد مثال مقطوع بالأصل، ليس قيمة حقيقية)
17. ✅ RBAC مُنفّذ كطبقة إضافية فوق النظام الحالي بدون كسره: جدول admin_users (اسم مستخدم/هاش كلمة مرور/دور super_admin|editor|viewer/فعّال أم لا)، كوكي جلسة موقّعة HMAC منفصلة dork_admin_session (نفس نمط جلسة المالك تمامًا) عبر admin-session.ts، صفحة /admin-users لإدارة الحسابات الفرعية (محمية بدور super_admin فقط)، وتعديل صفحة /login لإضافة تبديل اختياري بين تسجيل الدخول بكلمة مرور الإدارة الموحّدة (القديم، لم يتغيّر) أو باسم مستخدم/كلمة مرور حساب فرعي (الجديد). جدول admin_users أُنشئ فعلياً على القاعدة الحية (أكّد المستخدم تشغيل migrations/20260622_admin_rbac.sql)
18. ✅ Audit Log مُنفّذ: جدول admin_audit_log (الفاعل/الإجراء/نوع الهدف ومعرّفه/تفاصيل JSON/التوقيت)، دالة مشتركة logAdminAction() مُستخدمة فعليًا (لا مجرد بنية تحتية معطّلة) فمُضافة استدعاءاتها لإجراءات: تعديل/حذف صالون، تعديل/حذف عميل، تسجيل دفعة مالية، مراجعة مستند، تحديث تذكرة دعم، إنشاء/تحديث نزاع — صفحة /audit-log تعرض السجل مع بحث. ملاحظة نطاق: صفحة الحجوزات تُعدّل مباشرة من المتصفح عبر عميل Supabase (لا تمر بأي API Route)، فلم تُسجَّل أفعالها بالـAudit Log لتجنّب إعادة هيكلة كبيرة خارج نطاق هذا البند. جدول admin_audit_log أُنشئ فعلياً على القاعدة الحية
19. ✅ نظام نزاعات/استرجاع أموال مُنفّذ: جدول disputes (حجز/صالون/سبب/حالة open|in_review|resolved|rejected/مبلغ الاسترجاع/ملاحظة إدارية)، نقاط /api/disputes (عرض بفلاتر حالة/بحث + إنشاء) و/api/disputes/[id] (تعديل محصور بحقول محددة، يضبط resolved_at تلقائيًا عند الحل)، صفحة /disputes (نموذج إنشاء + قائمة قابلة للفلترة والتعديل المباشر). الإنشاء حاليًا من الإدارة فقط (لا مسار ذاتي للعميل بهذا التكرار، لتجنّب سياسات RLS عامة جديدة). جدول disputes أُنشئ فعلياً على القاعدة الحية
20. ✅ توثيق المستندات عند الموافقة مُنفّذ: جدول salon_documents (نوع المستند/رابط الملف doc_url/حالة pending|verified|rejected/ملاحظة إدارية/توقيت المراجعة)، نقاط /api/salons/[id]/documents (عرض/إضافة) و/api/salon-documents/[id] (مراجعة الحالة، حذف)، زر "📄 مستندات" جديد بصفحة /salons يفتح نافذة لإضافة ومراجعة المستندات. تبسيط متعمّد للـMVP: رابط نصي للمستند بدل رفع ملف فعلي إلى Supabase Storage (تجنّبًا لتعقيد إضافي خارج نطاق الطلب، قابل للتوسعة لاحقًا بدون كسر المخطط). جدول salon_documents أُنشئ فعلياً على القاعدة الحية
22. ✅ نظام دعم/تذاكر مُنفّذ: جدول support_tickets (الصالون/الموضوع/الرسالة/حالة open|in_progress|resolved/أولوية/رد الإدارة)، تبويب جديد "الدعم" بصفحة /owner (إنشاء تذكرة + عرض تذاكر الصالون نفسه مع ردود الإدارة) عبر /api/owner/support-tickets المحمية بجلسة المالك، صفحة /support بالإدارة لعرض كل التذاكر بفلتر حالة والرد عليها وإغلاقها كمحلولة عبر /api/support-tickets و/api/support-tickets/[id]. جدول support_tickets أُنشئ فعلياً على القاعدة الحية

ملاحظة عامة لـ17-20،22: الخمس ميزات مبنية بالكامل ضمن admin/ (تطبيق الإدارة Next.js) فقط، بدون أي لمس لـApp.jsx — لأن bug-list.md تصفها أصلاً كـ"ميزات باك-أوفيس". كل الجداول الجديدة بلا أي سياسة RLS عامة (service_role فقط عبر createAdminClient)، وبلا قيود مفتاح خارجي (لتجنّب مخاطر عدم تطابق الأنواع، بنفس نمط التحوّط ::text المستخدم بالكود الحالي) — ربط اسم الصالون بالتذاكر يتم بجلب ثانٍ + Map يدويًا (لا PostgREST embed) بنفس نمط customers/[id]/route.ts. تم التحقق: npx tsc --noEmit نظيف، و npm run build نجح (46 مسارًا). **الجداول الخمسة أُنشئت فعلياً على القاعدة الحية** — أكّد المستخدم تشغيل ملفات الترحيل الخمسة، الميزات الخمس تعمل الآن end-to-end
21. ✅ تقرير عمولات حقيقي: /api/finance كان منفّذاً بالكامل بالباك-إند (مستحق/مدفوع/رصيد لكل صالون + تسجيل دفعة) لكن بلا أي صفحة تستخدمه — تم بناء صفحة /finance فعلية تعرضه وتسمح بتسجيل الدفعات وتصدير CSV
23. ✅ Bulk actions: تأكيد جميع الحجوزات المعلقة دفعة واحدة بصفحة الحجوزات (نفس نمط "قبول الكل" الموجود مسبقاً بصفحة الصالونات)
25. ✅ رصد صحة النظام: نقطة /api/health تفحص الاتصال بقاعدة البيانات وزمن الاستجابة، مع مؤشر مباشر بلوحة التحكم (أخضر/أحمر)
24. ✅ تصدير CSV حقيقي مُضاف لصفحات العملاء/الصالونات/الحجوزات (كان فقط بصفحة المقارنة)، عبر دالة مشتركة واحدة lib/csv.ts بدل تكرار الكود
26. ✅ Rate-limiting مُفعّل على /api/auth و /api/owner/auth (10 محاولات/10 دقائق لكل IP، in-memory) — محدودية معروفة: العدّاد لا يُشارك عبر instances متعددة بالبيئات serverless، لكنه أفضل من صفر حماية سابقاً
27. ✅ حذف الحساب أصبح مقاومًا للتحايل على الحظر: confirmDeleteAccount يستدعي الآن مسار Vercel جديد /api/delete-account (مفتاح service_role، نفس نمط register-salon.js — ينشر تلقائيًا مع باقي الكود بلا أي خطوة يدوية إضافية) يحذف صف العميل ويحذف حساب Supabase Auth الحقيقي المرتبط به فعليًا (عمود جديد customers.auth_uid يُعبّى عند verifyOtp بالتسجيل)، وإذا كان الحساب محظوراً (customers.blocked) وقت الحذف تُسجَّل هاتفه/بريده في جدول customer_blacklist دائم — يُفحص عند كل تسجيل جديد عبر دالة is_blacklisted (SECURITY DEFINER، لا تكشف أي صف). أيضًا: تم سحب صلاحية UPDATE على عمود blocked وصلاحية DELETE من anon على جدول customers بالكامل (REVOKE)، فلا يمكن لعميل محظور رفع الحظر عن نفسه أو حذف حسابه مباشرة عبر REST API بالمفتاح العام حتى لو يعرفه — كل ذلك يمر فقط عبر service_role. كذلك أُضيف فحص حظر (blocked) في كل مسارات تسجيل الدخول (هاتف/PIN/بصمة/Google). لوحة العملاء بالإدارة (حظر/رفع حظر، تجميد نقاط الولاء، الملاحظات، الحذف) كانت تكتب مباشرة بمفتاح anon من المتصفح (نفس ثغرة admin_config أدناه) — تم تحويلها لتستخدم /api/customers/[id] الحالي (service_role). تأكدت تشغيل supabase/migrations/20260622_customer_ban_evasion_guard.sql (المستخدم أضافه)
28. ✅ تصحيح: الشات الفعلي هو بين المالك والإدارة/الدعم (لا يوجد شات مالك↔عميل بالكود) — كان بدون مرفقات وبحالة قراءة باتجاه واحد فقط (الإدارة تُعلّم رسائل المالك كمقروءة). أُضيفت حالة القراءة بالاتجاه الثاني: مسار Vercel جديد /api/mark-messages-read (service_role، نفس نمط delete-account.js) يُعلّم رسائل الإدارة كمقروءة عند فتح المالك للمحادثة (عمود read_at بجدول messages لا تسمح صلاحياته الحالية لـanon بالتحديث المباشر، فاحتاج مسارًا بمفتاح service_role)، وتظهر علامة "✓✓ تمت القراءة" تحت رسائل المالك المرسلة عند قراءتها من الإدارة. المرفقات تبقى خارج النطاق
29. ✅ لا يوجد أي مكان فيه #000 فوق var(--p) حالياً — تم التحقق ببحث شامل
30. ✅ نص "الأقرب" يستخدم متغيرات ثيم بكل المواضع
31. ✅ اسم الصالون بلوحة المالك يستخدم var(--p) (متجاوب)
32,33,35. ✅ تحقّق سطر-بسطر: لا يوجد color:"#fff" أو "white" داخل BookingCalendar (تقويم المالك)، AttendanceView (لوحة الحضور)، أو عرض قائمة الانتظار — الثلاثة نظيفة حالياً
34/62. ✅ register() يصحَّح ومسار العميل الجديد لا يهبط على الشاشة القديمة (كوميت 830195e)
36,37,38. ✅ عناوين الإشعار/الثيم/الأسئلة الشائعة تستخدم var(--p) على var(--surface-1) — متجاوبة فعلياً
39. ✅ بانر خطأ قاعدة البيانات أصبح متجاوباً مع الثيم (هذا الفرع)
40. ✅ مقارنة الصالونات: الكرت غير الأفضل أصبح var(--surface-1)/var(--text-muted) (هذا الفرع)
41,42. ✅ لا وجود لـ#c0c0d0 أو #c8c8a8 بالكود حالياً
43. ✅ فقاعة شات المالك/الإدارة أصبحت var(--surface-2)/var(--text-primary) (هذا الفرع)
44. ✅ لا وجود لـcolor:"#ccc" بالكود حالياً
45,46. ✅ _nextBk.name و b.name تُقرآن مباشرة الآن (لا customerName||customer_name المعطّلة)
47. ✅ **مكتبة مرجعية**: جدول كامل (إيموجي ← مكوّن SVG ← لون افتراضي) بملف [`ICONS.md`](./ICONS.md) لسهولة العودة لأي أيقونة مستقبلاً بدون البحث بهذا السجل الطويل. الإيموجي يُستبدل تدريجياً بأيقونات SVG حقيقية بمنهجية: **مُحدَّثة بطلب المستخدم** — بدل مناقشة رمز واحد بكل رسالة (97 جولة)، تُعرض دفعة رموز واحدة بصورة واحدة (٢-٣ أشكال لكل رمز) ويختار المستخدم الكل بردّ واحد. **تحديث صيغة الاختيار (الدفعة الرابعة)**: الخيارات تُرقَّم بأرقام (١/٢/٣) بدل الحروف العربية (أ/ب/ج) المستخدمة بالدفعة الثالثة، بطلب صريح من المستخدم لتسهيل الاختيار من لوحة المفاتيح. **منهجية العدّ** (محدَّثة، دقيقة، تُحسب آلياً بـnode -e regex بكل نطاقات اليونيكود [\u{1F300}-\u{1FAFF}\u{2600}-\u{27BF}\u{2190}-\u{21FF}\u{2B00}-\u{2BFF}] بدل التقدير اليدوي القديم غير الدقيق): العدد المهم هو **عدد الرموز الفريدة المختلفة** (لا عدد المواضع/التكرارات، لأن أيقونة واحدة تحل كل تكرارات نفس الرمز دفعة واحدة عبر NotifIcon/LabelWithIcon):
    - App.jsx: 106 رمز فريد إجمالي (348 موضع/تكرار) — منها 104 أُنجزت كأيقونة (الدفعة الأولى ٩: ✕،♥/♡،🗑️،📤،✂،❌،✅،📍،🔔،⭐/★،🚫،⚠️ + الدفعة الثانية ١٠: 🔥،✓،📅،💈،⚡،👤،✏️،🔄،📋،🔐 + الدفعة الثالثة ١٥: 📊،🌐،📱،💬،📞،💰،💵،🌙،❓،🚪،👥،🐦،✈️،📷،⚙️ + الدفعة الرابعة ٢٠: 🎨،🖼،🔤،🏷،🎁،📖،🔘،📈،🚀،💡،💳،📧،🕐،📌،🔍،💾،📄،✗،🔒،🎟 + الدفعة الخامسة ١٤: ☆،⚖،⬛،☀️،✨،🔲،🌊،👑،✦،🔎،✉️،🟢،🔵،❤️ + الدفعة السادسة ٢٢: 🌿،🍃،💎،🏺،🌸،🌺،🪸،🍷،🌲،🔴،🩵،🔮،🌟،🗓،🥇،🥈،🥉،🪙،✌️،🪨،🌅،🔌 + الدفعة السابعة (v1.3.18) ١٤: 7 أسهم/تنقّل ←→↑↓↩↻⇄ (`IconArrowLeft/Right/Up/Down/Return`، `IconRefresh` المعاد استخدامه، `IconSwapHorizontal`) + 7 من مصفوفة TONES 🪒🎵🎶🎺🎉🧖💦 (`IconRazor`/`IconMusicNote`/`IconTrumpet` معادة الاستخدام حرفياً من admin/ + `IconMusicNotes`/`IconConfetti`/`IconSteam`/`IconWaterDrops` تصاميم جديدة) — طُبّقت على 18 موضع عرض حقيقي بالأسهم + موضعَي عرض TONES (شريحة الاختيار بمعالج التسجيل، وRow بإعدادات العميل)) — **App.jsx أُغلق بالكامل** 104/106. الباقي رمزان فقط بلا أي موضع عرض حقيقي قابل للتحويل: ↔ (تعليق كود فقط) و📡 (نص داخل confirm() الأصلي للمتصفح، لا يستضيف JSX)
    - admin/ (لوحة الإدارة): 78 رمز فريد (218 موضع/تكرار) — **77 من 78 أُنجزت** (55 تطابق فوري + 8 إعادة استخدام بتعديل طفيف + 10 تصميم جديد بـv1.3.16: ➕🔑🖥🎲🪒🎵🎐📯🎺🤖 + 4 أسهم بـv1.3.17: ↑↓ بمكوّنين جديدين `IconArrowUp`/`IconArrowDown` و↻↺ بإعادة استخدام `IconRefresh` الموجودة، طُبّقت على آخر موضعين حقيقيين (`(admin)/layout.tsx` مؤشّر اسحب-للتحديث، `messages/page.tsx` زر تحديث)، عُرضت بصورة معاينة واختارها المستخدم بردّ واحد، عبر مكتبة `admin/src/components/Icons.tsx` ونقطة ارتكاز موحّدة `EmojiIcon` مطابقة لمبدأ `NotifIcon`؛ بدأت بـ`Sidebar.tsx` [14 إيموجي بتعديل واحد] ثم طُبّقت على كل ملفات لوحة الإدارة [لوحة التحكم، المظهر، الحجوزات، مقارنة الصالونات، التواصل، العملاء، المالية، الرسائل، الإشعارات، كلمة المرور، أكواد الخصم، الصالونات، تسجيل دخول الإدارة/المالك، بوابة المالك]). 👋 استُبعد بلا حاجة لتصميم (لا موضع عرض حقيقي له). **لوحة الإدارة أُغلقت بالكامل** — 78/78 (77 SVG فعلية + 👋 مستبعد لعدم وجود موضع عرض)
    - الإجمالي الكلي: ~184 رمز فريد بين الاثنين، أُنجز منه 181 (104 بـApp.jsx + 77 بالإدارة)، **البند مُغلق بالكامل** — الباقي 3 رموز مستبعدة نهائياً بلا حاجة لعمل عليها (↔ و📡 بـApp.jsx، 👋 بالإدارة) لعدم وجود أي موضع عرض React حقيقي قابل للتحويل لأي منها
    سجل القرارات (بالكود، App.jsx):
    - ✅ 📤 (مشاركة) → `IconShare` (PR #171)
    - ✅ 🗑️ (حذف/سلة) → `IconTrash` — مُحدَّث لشكل "غطاء عائم + مقبض دائري + ٣ خطوط" بطلب المستخدم (v1.3.4)
    - ✅ ♥/♡ (مفضّلة) → `IconHeart` نمط خطي دائري (v1.3.3)
    - ✅ ✕ (إغلاق) → `IconClose` نمط "X رفيع" (الخيار ١ من ٤، الأقرب لشكل النص الأصلي) — مُنفَّذ بكل المواضع العشرة (إغلاق الدرج، إلغاء فلاتر المنطقة/السعر، مسح البحث، إلغاء تعديل تاريخ/شهر/سنة الحلاق، إغلاق نموذج إضافة موقع، إلغاء إدخال نقدي) (v1.3.6)
    - ✅ ❌ (خطأ/رفض) → `IconError` دائرة حمراء #e74c3c + X أبيض — مُنفَّذ بالكامل بكل مواضع JSX (toast، أخطاء الفورم، تنبيهات قائمة الانتظار، badges التصنيف) عبر نقاط ارتكاز واحدة (`NotifIcon`/`LabelWithIcon`) بدل تعديل كل سطر؛ بقيت فقط alert()/Notification()/عناوين الإشعارات المخزّنة بالقاعدة كنص خام لأنها لا تدعم SVG تقنياً (v1.3.5)
    - ✅ ✅ (نجاح/قبول) → `IconSuccess` دائرة خضراء #27ae60 + ✓ أبيض — نفس منهجية ❌ أعلاه، مُنفَّذ بالكامل (v1.3.5)
    - ✅ ✂ (شعار الصالون) → `IconScissors` نمط "مقابض دائرية أعرض" (الشكل ٢ بالجولة الأولى) — مُنفَّذ بكل مواضع اسم الصالون/الخدمات/الشعار في الواجهة (~12 موضع)؛ تُرك بدون تحويل فقط: shareText (نص مشاركة خارجي)، نص الإشعارات الأصلية (Notification/DB)، وقوائم الأيقونات المختلطة الزخرفية (TONES، تبويبات الإعدادات، Row في الدرج) حفاظاً على اتساقها البصري مع باقي الإيموجي غير المعتمد لها أيقونة بعد (v1.3.5)
    - ✅ 📍 (موقع) → `IconPin` دبوس معبّأ بثقب أبيض (الخيار ٢ من ٤) — مُنفَّذ بـ16 موضع نصي/زر بكل الواجهة؛ تُرك فقط: مصفوفة فلاتر الترتيب الزخرفية المختلطة (📍⭐☆💰🪙) وshareText الخارجي (لم يُرفع/يُدمج بعد بانتظار طلب المستخدم)
    - ✅ 🔔 (إشعارات) → `IconBell` جرس خطي كلاسيكي + نقطة تنبيه حمراء (دمج خيار ١+٣) — مُنفَّذ بالأماكن الفعلية (زر تفعيل الإشعارات، عنوان وقت التذكير، NotifIcon لرسائل الإشعارات الحقيقية بدون نقطة لأنها قائمة عناصر موجودة فعلاً لا تنبيه "جديد")؛ النقطة تُطفى (`dot={false}`) بحالة "لا توجد إشعارات" تجنباً للتناقض المنطقي؛ تُرك بدون تحويل: TONES، تبويبات الإعدادات، Row بالدرج، تبويب الإشعارات الأيقوني، PACKAGES بلوحة العروض — كل القوائم الزخرفية المختلطة (لم يُرفع/يُدمج بعد بانتظار طلب المستخدم)
    - ✅ ⭐ (تقييم) → `IconStar` نجمة معبّأة كلاسيكية 5 رؤوس (الخيار ١ من ٤) — مُنفَّذ بكل مواضع عرض التقييم الرقمي (6 مواضع) وزر "إرسال التقييم"، وNotifIcon لرسائل إشعارات التقييم؛ تُرك بدون تحويل: Row بالدرج، مصفوفة فلاتر الترتيب، عنوان إشعار "كيف كانت تجربتك" (نص خام Notification)، وshareText الخارجي (لم يُرفع/يُدمج بعد بانتظار طلب المستخدم)
    - ✅ 🚫 (محظور/ممنوع) → `IconBlocked` حلقة حمراء + خط قطري كلاسيكي (الخيار ١ من ٤) — مُنفَّذ بـNotifIcon (لقراءة icon المخزّن بالقاعدة) وزر "إلغاء الحجز"؛ حُذف بادئة "🚫 " النصية من 6 رسائل toast/فورم من نوع err لأن IconError يُعرض بجانبها أصلاً (تكرار بصري)؛ تُرك بدون تحويل: toast من نوع warn (القاعدة المعتمدة: لا أيقونة لnoir warn)، alert()/confirm() الأصلية لإذن الموقع، وicon المُخزَّن كنص خام بطلب POST للإشعارات (لم يُرفع/يُدمج بعد بانتظار طلب المستخدم)
    - ✅ ⚠️ (تحذير) → `IconWarning` مثلث خطي بزوايا مدوّرة + علامة تعجب كهرمانية #f0a020 (الخيار ٣ من ٤) — مُنفَّذ بـNotifIcon، وLabelWithIcon (وسّع الـregex ليشمل ⚠️ مع ✅/❌ فيُستفاد منه تلقائياً بكل مواضع تصنيف العميل/حالة العرض/التنبيه الذكي)، وأيقونتي تأكيد حذف الحساب (شاشة العميل + ErrorBoundary)، وخيار "5 دقائق (اختبار)" بمدة العروض؛ تُرك بدون تحويل: alert() الأصلية لإذن الموقع/تكرار اسم المنطقة، ومصفوفة تبويبات الإعدادات الزخرفية (لم يُرفع/يُدمج بعد بانتظار طلب المستخدم)
    - ✅ ★ (نجمة التقييم، رمز Unicode مختلف عن ⭐) → استُبدلت بنفس `IconStar` الموجود (10 مواضع: شرائط تقييم الصالون/المراجعات للقراءة فقط، ملخص متوسط التقييم، شريحة فلتر عدد النجوم، ومُدخل التقييم التفاعلي بالعميل بحالتي العرض/الإدخال) لتوحيد كل أشكال النجمة بأيقونة واحدة
    - **الدفعة الثانية (١٠ رموز بمناقشة واحدة)**:
    - ✅ 🔥 (نار/عرض ساخن) → `IconFire` خط لهب أحمر #e74c3c — مُنفَّذ بكل مواضع العرض الفعلية؛ تُرك بدون تحويل: TONES، PACKAGES (`icon:"🔥"`)، title الإشعار الأصلي المخزّن بالقاعدة
    - ✅ ✓ (صح بسيط) → `IconCheck` نمط مربع محاط بحدود مدوّرة — مُنفَّذ بـ4 مواضع زر/تأكيد قائمة بذاتها بلا خلفية متعارضة؛ **تُرك بدون تحويل بقصد** (تعارض بصري مع شكل دائري/ملوّن موجود): نقاط التحديد الصغيرة فوق خلفية دائرية ملوّنة، علامة القراءة المزدوجة ✓✓ بالشات، شارة خطوة دائرية مرقّمة
    - ✅ 📅 (تقويم/تاريخ) → `IconCalendar` بنقطة سفلية — مُنفَّذ بكل مواضع عرض التاريخ الفعلية
    - ✅ 💈 (عمود الحلاق) → `IconBarberPole` كبسولة أحادية اللون بخطوط قطرية (اختيار تلقائي بتفويض المستخدم؛ الأقرب لأسلوب الخط الواحد بباقي الأيقونات) — مُنفَّذ بكل مواضع تبويبات/شعار الحلاقة
    - ✅ ⚡ (تنبيه عاجل/سريع) → `IconLightning` معبّأة — مُنفَّذ، ووُسّع regex الـ`LabelWithIcon` ليشملها؛ تُرك بدون تحويل: TONES، THEME_OPT الزخرفية
    - ✅ 👤 (مستخدم/عميل) → `IconUser` دائرة محيطة — مُنفَّذ بالمواضع الفعلية
    - ✅ ✏️ (تعديل) → `IconPencil` خط بسيط بلا تزيين متقطّع (بطلب المستخدم: "نريدها خطوط نفس الفيس" ليطابق أسلوب باقي الأيقونات الخطية) — مُنفَّذ
    - ✅ 🔄 (تحديث/إعادة تحميل) → `IconRefresh` سهمين متقابلين — مُنفَّذ؛ تُرك بدون تحويل: icon المخزّن كنص خام بطلب POST للإشعارات
    - ✅ 📋 (قائمة/لوحة) → `IconClipboard` — مُنفَّذ عبر اكتشاف نقطة ارتكاز جديدة (مصفوفتا تبويبات الإعدادات، حُلّت دفعة واحدة عبر `NotifIcon`)؛ تُرك بدون تحويل: title الإشعار الأصلي "📋 تم استلام حجزك"
    - ✅ 🔐 (قفل/PIN) → `IconLock` بنقطة PIN — مُنفَّذ بهيرو شاشة إدخال PIN وعنوان الحماية
    - **الدفعة الثالثة (١٥ رمز بمناقشة واحدة، بصيغة الحروف العربية أ/ب/ج لتفادي التباس A/B/C)**:
    - ✅ 📊 (إحصائيات) → `IconChart` أعمدة بارتفاعات متفاوتة — مُنفَّذ بزر تبويب الإحصائيات وبطاقة "الإجمالي" بملخص الحلاق
    - ✅ 🌐 (لغة) → `IconGlobe` كرة أرضية بخطوط طول/عرض — مُنفَّذ بزر اختيار اللغة وعنواني درج اللغة (عميل/مالك)
    - ✅ 📱 (جوال/تطبيق) → `IconMobile` جوال بسيط بخط علوي/سفلي (أُعيد تصميمه بعد رفض التصميم الأول؛ بنفس شكل أيقونة ⌚ المستخدمة بمكان آخر) — مُنفَّذ بمؤشر إيراد التطبيق
    - ✅ 💬 (رسائل/شات) → `IconChat` فقاعة كلام كلاسيكية — مُنفَّذ بزر واتساب الصالون وحقل تسمية واتساب بإعدادات التواصل (مكانين متطابقين عبر replace_all)؛ تُرك بدون تحويل: PACKAGES (`icon:"💬"`) الزخرفية
    - ✅ 📞 (هاتف) → `IconCall` سمّاعة كلاسيكية — مُنفَّذ بكل مواضع عرض رقم الهاتف (صف معلومات الصالون، تفاصيل الحجز بقائمة الانتظار وقائمة الحجوزات، رأس صفحة المالك)
    - ✅ 💰 (كيس فلوس/إيراد) → `IconMoneyBag` دائرة بعلامة $ بخط متموّج ذهبي #c9952c (بعد رفض تصميمَي "كيس فلوس" و"محفظة" — اختار المستخدم تصميم الدائرة+$ المجرّد) — مُنفَّذ بعنوان قسم "الأرباح" وبطاقة KPI
    - ✅ 💵 (كاش/نقد) → `IconCash` — يعيد استخدام شكل `IconMoneyBag` نفسه بلون أخضر #27ae60 مختلف فقط (قرار صريح من المستخدم بعد 3 جولات تصميم مرفوضة: ورقة نقدية، أوراق مكدّسة، أشكال مجرّدة — كلها لم تُناسب) — مُنفَّذ بكل مواضع عرض الكاش (ملخص الحلاق، ملخص الإيراد، نموذج إضافة كاش ×2)
    - ✅ 🌙 (ليل/إعدادات إضاءة) → `IconMoon` هلال معبّأ — مُنفَّذ عبر اكتشاف نقطة ارتكاز جديدة لم تكن تمر بـ`NotifIcon` (مصفوفة منتقي نمط الإضاءة الأربعة كانت تعرض `{icon}` مباشرة)؛ حُلّت بسطر واحد فحلّ 🌙 تلقائياً (الأنماط الأخرى ⬛🔘☀️ تبقى نصاً احتياطياً لعدم وجود أيقونة معتمدة لها بعد)
    - ✅ ❓ (سؤال/فاق) → `IconQuestion` علامة استفهام خطية — حُلّت بالكامل تلقائياً عبر نقاط الارتكاز الموجودة (Row بالدرج + مصفوفتا تبويبات الإعدادات) دون أي تعديل سطر إضافي
    - ✅ 🚪 (تسجيل خروج) → `IconLogout` مستطيل باب + سهم خارج أحمر #e74c3c — مُنفَّذ بزر تسجيل الخروج وأيقونتي تأكيد الخروج (متطابقتان عبر replace_all)
    - ✅ 👥 (عملاء/مجموعة) → `IconGroup` شخصان خطيان — مُنفَّذ ببطاقة الصالون (عدد الحلاقين) وعنوان "تحليل العملاء" وبطاقة KPI
    - ✅ 🐦 (شعار X) → `IconXLogo` — بطلب صريح من المستخدم استُخدم مسار الشعار **الرسمي** لـX بدل تصميم تقريبي ("يتغير كامل نريدة نفس شعار اكس") — مُنفَّذ بزر تويتر/X بكرت الصالون وحقلي تسمية "تويتر / X" بإعدادات التواصل (متطابقان عبر replace_all)
    - ✅ ✈️ (تيليغرام) → `IconTelegram` ورقة طائرة مائلة بلون أزرق تيليغرام #2980d9 — مُنفَّذ بزر تيليغرام بكرت الصالون وحقلي تسمية "تيليغرام" بإعدادات التواصل (متطابقان عبر replace_all)
    - ✅ 📷 (كاميرا) → `IconCamera` عدسة دائرية بجسم مستطيل — مُنفَّذ بصورة الحلاق الاحتياطية (بدون صورة) وزر رفع صورة الحلاق الدائري (نُودي مباشرة بـ`color="#fff"` بدل `NotifIcon` لأن لون الخلفية var(--p) يطابق اللون الافتراضي للأيقونة فيختفي بصرياً)
    - ✅ ⚙️ (إعدادات، نمط iOS) → `IconGear` ترس حقيقي بـ18 سنّاً صغيرة كثيفة + 3 خطوط محورية داخلية من مركز مثقوب + خلفية رمادية مدوّرة #8e8e93 (صُمم خصيصاً ليطابق صورة أيقونة إعدادات iOS التي أرسلها المستخدم بعد رفض تصميمين تقريبيين سابقين: ترس مبسّط، ومفتاح ربط) — مُنفَّذ بعنوان قسم "إعدادات الحساب" (الموضع الوحيد المتبقي بالملف)
    - ملاحظة: ✨، 👑، 🎉 تم استثناؤها من هذه الدفعة بعد فحص شامل أكّد عدم وجود أي موضع عرض UI فعلي لها بكامل الملف (زخرفية/أصلية بالكامل فقط) — لا حاجة لأيقونة لها حالياً
    - **الدفعة الرابعة (٢٠ رمز بمناقشة واحدة، بصيغة الترقيم ١/٢/٣ بطلب المستخدم)**:
    - ✅ 🎨/🖼/🔤/📖/🔘/🏷/🎁 (تصميم/صورة/خط/كتاب/راديو/وسم/هدية) → `IconPalette`/`IconImagePic`/`IconFontSize`/`IconBook`/`IconRadioFilled`/`IconTag`/`IconGift` — **حُلّت السبعة بإضافة خريطة `NotifIcon` فقط، بلا أي تعديل JSX إضافي**، لأنها كانت مُمرَّرة مسبقاً عبر نقاط ارتكاز جاهزة (`Row`، مصفوفتا تبويبات الإعدادات، مصفوفة منتقي الإضاءة الليلية، ومصفوفة أنواع قوالب العرض الترويجي المكتشفة هذه الدفعة)
    - ✅ 📈 (ارتفاع/إيراد) → `IconTrendUp` خط صاعد أخضر #27ae60 — مُنفَّذ بعنوان لوحة "الإيراد — آخر 7 أيام"، ووُسّع regex الـ`LabelWithIcon` ليشملها (يحلّ تلقائياً تنبيه smartAlert عند ظهور هذا الرمز)
    - ✅ 🚀 (إرسال/انطلاق) → `IconRocket` ذهبي #c9952c افتراضياً — مُنفَّذ بزر "إرسال العرض" بـ`color="#fff"` **عبر استدعاء مباشر للمكوّن متجاوزاً `NotifIcon`** (لون الذهبي الافتراضي يتعارض مع خلفية الزر المتدرّجة ونصه الأبيض)؛ وُسّع regex الـ`LabelWithIcon` ليشمله أيضاً
    - ✅ 💡 (فكرة/تلميح) → `IconBulb` كهرمانية #f0a020 — مُنفَّذ بنص تلميح نموذج العرض الترويجي، ووُسّع regex الـ`LabelWithIcon` ليشملها
    - ✅ 💳 (بطاقة/دفع) → `IconCard` — مُنفَّذ بمؤشر "حجوزات" بملخص الحلاق وزر "ادفع وأرسل" المعطّل
    - ✅ 📧 (بريد إلكتروني) → `IconMail` — مُنفَّذ بحقل تسمية البريد الإلكتروني (إعدادات المالك + الإدارة، متطابقان عبر replace_all)
    - ✅ 🕐 (وقت/ساعة) → `IconClock` — مُنفَّذ بعنوان قسم ساعات العمل وزر "24h" السريع
    - ✅ 📌 (دبوس/تثبيت) → `IconThumbtack` — مُنفَّذ بحقل تسمية الحقل الاجتماعي المخصّص (مكانان متطابقان عبر replace_all)
    - ✅ 🔍 (بحث) → `IconSearch` ذهبي #c9952c — مُنفَّذ بزر البحث الدائري بشريط فلاتر الصفحة الرئيسية
    - ✅ 💾 (حفظ) → `IconSave` — مُنفَّذ بزر "حفظ المدد"
    - ✅ 📄 (ملف/تصدير) → `IconFileExport` ذهبي #c9952c — مُنفَّذ بزر "تصدير التقرير"
    - ✅ ✗ (إغلاق/إلغاء، نمط مختلف عن ✕ السابقة) → أُعيد استخدام `IconClose` الموجود بلون `var(--gold)` (بلا تصميم جديد، تطابق بصري كافٍ) — مُنفَّذ بقائمة تأكيد حذف الحساب (سطرين)
    - ✅ 🔒 (قفل/قريباً، نمط مختلف عن 🔐 السابقة) → أُعيد استخدام `IconLock` الموجود بلون `var(--text-muted)` (بلا تصميم جديد) — مُنفَّذ ببادج "قريباً" بزر "ادفع وأرسل" المعطّل
    - ✅ 🎟 (كود خصم) → `IconDiscountSeal` ختم مسنن دائري (12 سنّاً) بعلامة % بالداخل — صُمم بناءً على صورة مرجعية أرسلها المستخدم (بعد رفض جولتين تصميم سابقتين بشكل تذكرة بخط منقط مهما اختلف التفصيل)، بلون التطبيق `var(--p)` — مُنفَّذ بعنوان قسم "كود الخصم" ونص تنبيه "أدخل كود الخصم لتفعيل الإرسال"
    - **الدفعة الخامسة (١٤ رمز بمناقشة واحدة، بصيغة الترقيم ١/٢ بطلب المستخدم، بعد طلب صريح "نكمل الأيقونات بنفس الفكرة")**:
    - ✅ ☆ (نجمة مجوّفة، تقييم منخفض) → `IconStarOutline` خطية بلون `var(--gold)` — مُنفَّذ بفلتر "الأقل تقييماً" بشريط الترتيب
    - ✅ ⚖ (مقارنة) → `IconScale` ميزان خطي — مُنفَّذ بزر "مقارنة" بكرت الصالون (استدعاء مباشر للمكوّن، لا حاجة لخريطة NotifIcon لأنه موضع استخدام واحد)
    - ✅ ⬛ (بلا خلفية) → `IconNone` مربع بخط قطري — **حُلّت بإضافة خريطة `NotifIcon` فقط بلا أي تعديل JSX**، لأنها كانت مُمرَّرة مسبقاً عبر نقطة ارتكاز مصفوفة منتقي الإضاءة الليلية
    - ✅ ☀️ (وضع نهاري) → `IconSun` كهرمانية `#f0a020` — **حُلّت بإضافة خريطة `NotifIcon` فقط بلا أي تعديل JSX**، نفس نقطة الارتكاز أعلاه
    - ✅ ✨ (بريق/خلفية نجوم) → `IconSparkle` معبّأة بلون `var(--p)` — مُنفَّذ بمصفوفة اختيار الخلفية (`BACKGROUNDS`)
    - ✅ 🔲 (خلفية شبكة) → `IconGridLines` خطوط متقاطعة — مُنفَّذ بمصفوفة اختيار الخلفية
    - ✅ 🌊 (خلفية أمواج) → `IconWaves` خطين متموّجين أزرق `#3498db` — مُنفَّذ بمصفوفة اختيار الخلفية
    - ✅ 👑 (خلفية ملكية) → `IconCrown` تاج ذهبي `#d4a017` — مُنفَّذ بمصفوفة اختيار الخلفية
    - ✅ ✦ (خلفية رخام ذهبي) → `IconDiamond` ماسة بلون `var(--p)` — مُنفَّذ بمصفوفة اختيار الخلفية
    - ✅ 🔎 (بحث، نمط مختلف عن 🔍 السابقة) → أُعيد استخدام `IconSearch` الموجود (بلا تصميم جديد) — مُنفَّذ بحقل البحث بشريط الصفحة الرئيسية
    - ✅ ✉️ (بريد، نمط مختلف عن 📧 السابقة) → أُعيد استخدام `IconMail` الموجود — مُنفَّذ برابط بريد الصالون بكرت التواصل
    - ✅ 🟢/🔵 (نقاط حالة "جدد"/"عائدون" بتحليل العملاء) → أُعيد استخدام `IconRadioFilled` الموجود بلون أخضر `#27ae60`/أزرق `#3498db` مطابق لإطار كل صندوق — مُنفَّذ
    - ✅ ❤️ (مفضّلة بقائمة العميل، نمط مختلف عن ♥/♡ السابقة) → أُعيد استخدام `IconHeart` الموجود معبّأ بلون `#e74c3c` — **حُلّت بإضافة خريطة `NotifIcon` فقط بلا أي تعديل JSX**، لأنها كانت مُمرَّرة مسبقاً عبر نقطة ارتكاز مكوّن `Row`
    - 🔍 اكتُشفت نقطتا ارتكاز جديدتان لم تكونا تستخدمان `NotifIcon` رغم كونهما مواضع عرض فعلية: مصفوفة الخلفيات (`BACKGROUNDS`) ومصفوفة فلاتر الترتيب بالصفحة الرئيسية — كانتا تعرضان الرمز كنص خام مباشرة (`<span>{ic}</span>`). تحويلهما صحّح أيضاً بأثر رجعي ظهور 📍⭐💰 (المُنجزة سابقاً بمواضع أخرى) بهذين الموضعين بالتحديد، حيث كانت لا تزال تظهر كرموز خام فيهما فقط (ثغرة لم تُكتشف بالدفعات السابقة)
    - ⏸ 5 رموز رُفضت من المستخدم بهذه الجولة (مالها علاقة بالمفهوم أو لم تُعجب بصرياً): 🪙 (ترتيب الأرخص)، 🌅 (ملخص اليوم)، ✌️ (الباقة المزدوجة)، 🪨 (خلفية مرمر)، 🔌 (خلفية دائرة) — لم تُحسب كمُنجزة، أُجِّل تصميمها بطلب صريح من المستخدم لتُدمج مع الدفعة السادسة القادمة
    - ❌ باقي 38 رمز فريد بـApp.jsx (منها 5 مرفوضة مؤجلة أعلاه) + 78 رمز فريد بـadmin/ (صفر منها بدأ) لم تُبدأ بعد — الأكثر تكراراً بـApp.jsx حالياً: ← (24)، ✅/❌/🔔/⚠ (نص خام متبقٍّ بمواضع native غير قابلة للتحويل)، الأسهم الأخرى (→↩↔↻↑↓⇄)، الميداليات (🥇🥈🥉)
    - **إصلاح مستقل بإطار الدفعة السادسة (v1.3.12)**: 🔍 اكتُشفت نقطة ارتكاز ثالثة لم تكن تستخدم `NotifIcon`: مصفوفة `THEME_OPTIONS` (شبكة اختيار الثيمات الـ15 بصفحة الإعدادات) كانت تعرض `{t.emoji}` كنص خام. تحويلها صحّح بأثر رجعي ظهور ✨/👑/🌊 بهذا الموضع تحديداً، وكشف 12 رمزاً جديداً غير مُعيَّن بعد بهذا الموضع نفسه: 🌿💎🏺🌸🔴🩵🪸🌺🍷🌲🍃🔮 (لا يُضاف للعدّاد كرموز "منجزة" بعد — لا تزال تحتاج خريطة `NotifIcon` فعلية؛ مُدرجة ضمن مرشحي الدفعة السادسة قيد الاختيار)
    - **الدفعة السادسة (٢٢ رمز مرشح من أصل 38 المتبقية، مقسّمة لمجموعتين أ/ب)**: مجموعة أ (9 رموز/7 قرارات: 🌿💎🏺🌸🪸🌺🍷🌲🍃 + إعادة استخدام تلقائية لـ🔴🩵)، مجموعة ب (11 رمزاً/9 قرارات: 🔮🌟🗓🥇🥈🥉 + 5 مؤجلة من الدفعة السابقة بتصاميم جديدة كلياً: 🪙🌅✌️🪨🔌). الـ16 المستبعدة من هذه الجولة: 8 أسهم (←→↩↔↻↑↓⇄ — مدمجة كنص بتسميات الأزرار، تحتاج إعادة هيكلة JSX لا تعيين بسيط، ستُعالج لاحقاً بمنهجية مستقلة) + 8 زخرفية/native فقط بلا موضع عرض حقيقي (🎉🪒🎶🎵🧖💦🎺📡)
    - ✅ 🌿 (نبتة، زمردي) و🍃 (ورقة، ليموني) → `IconLeaf` بشكل ورقة بعرق واحد، بلون `#10b981`/`#65a30d` على التوالي — مُنفَّذة عبر `NotifIcon` وشبكة الثيمات
    - ✅ 💎 (جوهرة، ياقوتي) → `IconGemCut` قطع ألماسي بلون `#3b82f6`
    - ✅ 🏺 (إناء، برونزي) → `IconVaseHandles` إناء بمقبضين بلون `#8b5a2b`
    - ✅ 🌸 (زهرة، وردي) و🌺 (زهرة، فوشيا) → `IconBlossom` بشكل زهرة 5 بتلات، بلون `#ec4899`/`#d946ef` على التوالي
    - ✅ 🪸 (مرجان) → `IconCoral` تفرّع بنقاط أطراف، بلون `#f97316`
    - ✅ 🍷 (خمري) → `IconWineGlass` كوب نبيذ معبّأ بلون `#9f1239`
    - ✅ 🌲 (غابة) → `IconTree` شجرة مخروطية بجذع، بلون `#15803d`
    - ✅ 🔴 (قرمزي) و🩵 (فيروزي) → أُعيد استخدام `IconRadioFilled` الموجود بلون `#ef4444`/`#0d9488` — بلا تصميم جديد، بطلب تلقائي
    - ✅ 🔮 (بنفسجي، كرة سحرية) → `IconCrystalBall` كرة بنقاط بريق داخلية، بلون `#8b5cf6` — مُنفَّذة بشبكة الثيمات وبطاقة "توقع الأسبوع القادم" (كانت نص خام، الآن `NotifIcon`)
    - ✅ 🌟 (شارة "مميز"/VIP) → `IconStarRadiant` نجمة بإشعاع (تصميم جديد، لا إعادة استخدام) بلون `#d4a017` — وُسّع regex الـ`LabelWithIcon` ليشمله، يُحل تلقائياً بكل مواضع تصنيف العملاء
    - ✅ 🗓 (تبويب "الحجوزات") → `IconCalendarGrid` تقويم شبكي بحلقتي تعليق (تصميم جديد، لا إعادة استخدام IconCalendar) بلون `#c9952c` — **حُلّت بإضافة خريطة `NotifIcon` فقط بلا أي تعديل JSX**، كانت مُمرَّرة مسبقاً عبر `Row`
    - ✅ 🥇🥈🥉 (ميداليات الترتيب) → `IconMedal` شريط+حلقة معبأة+نجمة منقوشة بالداخل (شكل واحد، يقبل `color` للذهبي/الفضي/البرونزي) — حُوّلت 5 مواضع عرض: لوحة تحليل الحلاقين، أكثر الخدمات طلباً، وبلوحة العروض الترويجية `PromoPanel` (بطاقة اختيار الباقة، ملخص الطلب، سجل العروض المُرسلة)
    - ✅ 🪙 (ترتيب "الأرخص"، مؤجل سابقاً) → `IconCoinArrow` عملة بسهم متجه للأسفل بلون `#c9952c` — **حُلّت بإضافة خريطة `NotifIcon` فقط بلا أي تعديل JSX**، كانت مُمرَّرة مسبقاً عبر مصفوفة فلاتر الترتيب
    - ✅ ✌️ ("باقة مزدوجة"، مؤجل سابقاً) → `IconOverlapCircles` حلقتان متقاطعتان (مفهوم جديد كلياً، لا علاقة بإشارة اليد المرفوضة) — **حُلّت بإضافة خريطة `NotifIcon` فقط بلا أي تعديل JSX**، كانت مُمرَّرة مسبقاً عبر مصفوفة أنواع القوالب
    - ✅ 🪨 (خلفية "رخام"، مؤجل سابقاً) → `IconPebble` حصاة بيضوية بعروق رخامية (مفهوم جديد، لا علاقة بشكل الصخرة المرفوض) بلون `#aaa` — **حُلّت بإضافة خريطة `NotifIcon` فقط بلا أي تعديل JSX**، كانت مُمرَّرة مسبقاً عبر مصفوفة `BACKGROUNDS`
    - 🔍 اكتُشف عرضياً أثناء تحويل الميداليات أن `p.icon` (🔔🔥💬) بمصفوفة `PACKAGES` بلوحة `PromoPanel` كان أيضاً يُعرض كنص خام ببطاقة اختيار الباقة، رغم استثناء هذه المصفوفة سابقاً باعتبارها "زخرفية فقط" — تبيّن أنها موضع عرض فعلي مرئي للمستخدم، فحُوّلت لاستخدام `NotifIcon` (ثغرة لم تُكتشف بالدفعات السابقة، تُضاف لقائمة نقاط الارتكاز المكتشفة لاحقاً: `BACKGROUNDS`، فلاتر الترتيب، `THEME_OPTIONS`، والآن `PACKAGES.icon`)
    - ⏸ 2 رموز رُفض تصميمهما **للمرة الثانية** (بعد رفض أول مختلف بالدفعة السابقة): 🌅 (ملخص اليوم — "ماله علاقة، الايقونه شمس ماتجي" — الشمس لا تناسب معنى "ملخص") و🔌 (خلفية دوائر — "الايقونه خطوط" — الشكل ظهر كخطوط مجردة بلا هوية "دوائر" واضحة). أُرسلت مفاهيم بديلة جديدة كلياً (إيصال/أعمدة إحصائية لـ14، شريحة IC/عُقد دائرة بارزة لـ17)
    - ✅ 🌅 (ملخص اليوم، رُفض الشمس مرتين) → `IconReceipt` إيصال/فاتورة بثلاث سطور نص، بلون `#f0a020` — مُنفَّذ ببطاقة ملخص اليوم بالصفحة الرئيسية للصالون (كانت نص خام، الآن `NotifIcon`)
    - ✅ 🔌 (خلفية دوائر، رُفض الشكل الخطي مرتين) → `IconCircuitNodes` مسار دائرة بعُقد دائرية بارزة، بلون `#888` — **حُلّت بإضافة خريطة `NotifIcon` فقط بلا أي تعديل JSX**، كانت مُمرَّرة مسبقاً عبر مصفوفة `BACKGROUNDS`
    - 🏁 **بهذا تُغلق دفعة الـ38 رمزاً المتبقية بالكامل (٢٢ رمز نُفّذ + 8 أسهم مؤجلة + 8 زخرفية مستثناة = 38)**. App.jsx الآن: 90/106 رمز فريد منجز، الباقي 16 رمزاً فقط بلا أي عمل ممكن حالياً
48. ✅ عمود social بجدول salons مُضاف فعلياً للقاعدة الحية (شغّل المستخدم 20260622_add_salons_social_column.sql) — تم إعادة social لاستعلامي App.jsx (loadData) وadmin/src/app/api/salons/route.ts (FULL_SELECT) بعد التأكد من تشغيل الترحيل
49. ✅ cancellation_window موجود فعلياً بنموذج تعديل الصالون بالإدارة
50. ✅ تصفير/تغيير PIN المالك متاح من الإدارة (new_pin)
51. ✅ total_paid يُجلب فعلياً بـtoAppSalon
52-69. ✅ كل الألوان الثابتة بصفحات الإدارة المذكورة (bg-[#0d0d1a], bg-[#1a1a2e], bg-[#0f1117], #333, إلخ) لم يُعثر على أي أثر لها بالكود حالياً
70. ✅ كرت owner-login لا يحتوي bg-[#13131f]/border-[#2a2a3a] حالياً
71. ✅ جلسة المالك موقّعة بـHMAC (owner-session.ts) — لا يمكن تزييف الكوكي بعد الآن
- كود ميت إضافي (DEMO_SALONS, Splash, toAppBooking, googleCalLink, HomeReviewsSection, LocFilter): ✅ كل الستة محذوفة فعلياً (صفر تطابق ببحث شامل)

ملاحظة (لم تُعدّ ولم تُعدّل ضمن الـ71 — ضمن نفس عمل #27): تأكدت بقراءة الكود أن قبول/رفض الصالون (admin/src/app/(admin)/salons/page.tsx) وتجميد/حظر الصالون موجودان فعلياً ويعملان end-to-end عبر /api/salons/[id] بمفتاح service_role — لا يوجد أي عطل هنا، لم يكن يحتاج إصلاحاً. أُضيف فوقهما خيار جديد "القبول التلقائي للصوالين الجديدة" (مفتاح admin_config.auto_approve_salons، متوقف افتراضيًا = false كما طُلب) يُقرأ من /api/register-salon قبل إدراج كل صالون جديد.

⚠️ ثغرة منفصلة اكتُشفت بالصدفة أثناء هذا العمل، لم تُصلَح بالكامل (خارج الطلب الحالي): جدول admin_config (يُستخدم لـpinned وweek_salon بصفحة الصالونات) سياسة RLS عليه service_role فقط — لكن صفحتي الصالونات/العملاء بالإدارة تقرآن/تكتبان فيه (وفي أعمدة أخرى بجدول customers) مباشرة بمفتاح anon من المتصفح (admin/src/lib/supabase-browser.ts)، فمن المرجّح أن قراءة/كتابة "مثبّت" و"صالون الأسبوع" تفشل بصمت حاليًا على القاعدة الحية (RLS يرجع 0 صفوف بدون أي خطأ ظاهر). لم أصلحها لأنها خارج هذا الطلب — أعلمكم بها فقط.

