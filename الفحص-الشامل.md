# الفحص الشامل — النقاط المؤجلة

> النقاط المنجزة (84 نقطة) انتقلت إلى `ملف_الاختبارات.md`
> آخر تحديث: 2026-07-14

---

| # | النقطة | الشرط / السبب |
|---|--------|--------------|
| 6 | تقسيم App.jsx إلى ملفات .md (نظام السور) — كل ميزة في ملف مستقل | مؤجل لآخر شيء — يُبدأ بعد إنهاء باقي النقاط |
| 27 | تنبيهات استهلاك Supabase عند 60% | يُفعَّل عند الترقية للـ Pro plan (Settings → Billing → Usage alerts) |
| ~~28~~ | ~~بيئة Staging — Supabase مشروع ثانٍ مجاناً~~ | ✅ **منجز 2026-07-06** — مشروع "DAWRAK-TEST" أُنشئ (نفس منطقة الإنتاج + RLS تلقائي)، والإنتاج أُعيد تسميته لـ"DAWRAK" |
| 36 | PITR (Point-in-Time Recovery) في Supabase | يُفعَّل عند الترقية إلى Supabase Pro + بدء الحجوزات التجارية |
| 47 | Stress Test — p95 < 800ms عند 1000 مستخدم | نقطة 28 (Staging) صارت جاهزة الآن — تقدر تُنفَّذ متى ما رغبت |
| 48 | إزالة Re-renders غير الضرورية (React DevTools Profiler) | يحتاج أحمد يفتح React DevTools Profiler في المتصفح ويسجّل الحركات |
| 71 | التحقق من معايير App Store وGoogle Play | يُفعَّل عند تحضير Native App Wrapper (Capacitor أو React Native) |
| 86 | تشغيل Migration Script (Auth الصالونات) يدوياً | ✅ **تحديث 2026-07-14/15:** صالون "الوادي" صار له `owner_email` مضبوط، و`auth_uid` تعبّى تلقائياً فعلياً عند أول دخول بعد إصلاح `owner-auth.js` (self-healing، بدل تشغيل السكربت يدوياً) — مؤكد حياً. راجع Tier 2 مرحلة 1 بـ`تقرير-أمان-RLS.md` |
| 92 | مراجعة أمنية احترافية مستقلة (Pentest) من مختص خارجي | **إلزامي قبل أي إطلاق تجاري حقيقي بعملاء وأموال فعليين** — مراجعة الكود (اللي نسويها بالجلسات) لا تعوّض اختبار اختراق حي على التطبيق المنشور (IDOR، XSS، تجاوز rate limiting، أمان الجلسة/الكوكيز، إساءة استخدام منطق العمل، تسريب أسرار بكود المتصفح). تفاصيل كاملة وأمثلة بـ`تقرير-أمان-RLS.md` (قسم 2026-07-14) |
| 93 | مشروع Tier 2 — نظام هوية حقيقي (`auth.uid()`) لكل من الصالونات والعملاء + إعادة كتابة سياسات RLS + إصلاح `loadData()` (تجيب كل العملاء/الحجوزات بلا فلتر لأي زائر) | مشروع كامل، مو ترقيع SQL — يحتاج جلسة مخصّصة منفصلة. **تحديث 2026-07-16:** تحقّق حي (F12/Network) أكّد الثغرة حقيقية بالكود لكن الضرر الفعلي **صفر حالياً** (لا يوجد عميل حقيقي ثانٍ). الأولوية: **يجب يُصلح قبل انضمام أي عميل حقيقي ثانٍ**، مو طارئ فوري. تفاصيل الحل الكامل بـ`تقرير-أمان-RLS.md` (أقسام "Tier 2" و2026-07-16) وبـ`أمان-التطبيق-وقاعدة-البيانات.md` |
| 94 | شراء نطاق حقيقي (domain) + توثيقه عند Resend لحل مشكلة وصول رسائل استعادة الرمز السري لـSpam (وفقدان التعبئة التلقائية للكود) | **قرار أحمد (2026-07-15):** مؤجَّل لجلسة قادمة بذهن صافٍ — اختيار اسم النطاق وشراؤه قرار يستاهل تفكير، مو قرار وقت متأخر. حالياً التطبيق على رابط Vercel المجاني (`dork-app.vercel.app`) بدون نطاق مملوك، فما يمكن توثيق بريد (SPF/DKIM/DMARC) بدونه. **غير عاجل وظيفياً** — ميزة استعادة الرمز السري تعمل 100%، بس المستخدم يحتاج يفتح مجلد Spam مرة. **لغز مرتبط غير محلول:** فيه دليل (مفتاح Resend قديم "dork-app" مُستخدَم فعلياً قبل شهرين) على وجود إعداد SMTP سابق شغّال بجودة أعلى (تعبئة تلقائية + وصول لصندوق الوارد)، لكن تفتيش تاريخ git الكامل وحساب Resend الحالي (Domains: "No domains yet") ما لقى أي أثر لتفاصيله — شراء نطاق جديد يحل المشكلة بغض النظر عن حل هذا اللغز. التفاصيل الكاملة بـ`تقرير-أمان-RLS.md` (قسم 2026-07-15) |
| 95 | فجوة GRANT لـ`authenticated` على 5 جداول (`promotions`, `messages`, `notifications`, `promo_codes`, `waiting_list`) | ✅ **مُصلح بالكامل 2026-07-16** — `anon` كان عنده وصول كامل و`authenticated` بلا أي GRANT، فأي مستخدم مسجّل دخول (بعد Tier 2 مرحلة 1) يواجه 403 صامت. اكتُشف بالصدفة (عطل العروض الترويجية) ثم فحص شامل استباقي كشف الباقي. مؤكَّد على DAWRAK (صفر فجوات). ⏳ متبقٍ: تطبيق نفس الإصلاح على DAWRAK-TEST + اختبار حي للميزات الأربع. أضاف بند 13 لـCLAUDE.md — راجع `تقرير-أمان-RLS.md` (قسم 2026-07-16) |
