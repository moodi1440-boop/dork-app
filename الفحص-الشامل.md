# الفحص الشامل — Dork App

> **آخر تحديث:** 2026-06-30 — L114  
> **الرموز:** 🔲 لم تبدأ | ⏳ جارية | ✅ منجزة  
> **التفاصيل الكاملة:** راجع `قرارات-نظام-دخول-الصالونات.md` للـ Auth

---

## القسم الأول: ميثاق البناء والبرمجة (نقاط 1–41)

| # | النقطة | الحالة |
|---|--------|--------|
| 1 | حماية بيانات العميل وتشفيرها | ✅ منجزة (L124) — hashPin(SHA-256) لكل عمليات حفظ PIN في localStorage (4 مواقع) |
| 2 | الحماية ضد SQL Injection, XSS | ✅ منجزة — React يهرّب JSX، Supabase JS يستخدم Parameterized Queries، لا raw SQL |
| 3 | اتباع معايير Clean Code | ✅ منجزة (مُطبَّقة في كل جلسة) |
| 4 | git commit قبل أي تعديل | ✅ منجزة (مُطبَّقة في كل جلسة) |
| 5 | التحقق من البناء بعد كل تعديل (build + اختبار يدوي) | ✅ منجزة — البناء يعمل بنجاح (583KB app + vendor chunks منفصلة) |
| 6 | تقسيم App.jsx إلى ملفات .md (نظام السور) — كل ميزة في ملف مستقل باسمها يحتوي الكود الكامل غير المجزأ (100%) + التوثيق التقني + القرارات — ممنوع يبقى أي جزء من كود الميزة خارج ملفها — مرجع ثابت للتطبيقات المستقبلية المطابقة | 🔲 |
| 7 | كفاءة الذاكرة والاستدعاءات (لا Select *) | ✅ منجزة (مُغطّاة بنقطة 91) |
| 8 | هيكلة أعمدة Supabase باحترافية | ✅ منجزة (L124) — CHECK constraints على status/attendance/waiting_list + COMMENT ON COLUMN — يلزم تشغيل SQL في Supabase |
| 9 | تبادل الآراء التقنية بشفافية | ✅ منجزة (مُطبَّقة في كل جلسة) |
| 10 | عدم فرض الرأي — تبرير أي تغيير تقنياً | ✅ منجزة (مُطبَّقة في كل جلسة) |
| 11 | حذف الأكواد الميتة والمتكررة | ✅ منجزة (L123) — readJson+getSalonId → _lib/request.js، حُذف 51 سطراً |
| 12 | تنظيف الـ branches الميتة شهرياً | ✅ منجزة (فرع واحد فقط — لا توجد branches ميتة) |
| 13 | حصر API Keys ومنع مشاركتها في الكود | ✅ منجزة (anon key عام، service role في Vercel env) |
| 14 | الفحص الرباعي عند كل إصدار (التطبيق + الأدمن + Supabase + Vercel) | ✅ منجزة — قائمة الفحص الرباعي موثّقة في docs/incident-response-runbook.md § 6 (4 محاور × 4-5 بنود لكل محور) |
| 15 | Scoping في الاستعلامات (تحديد الأعمدة دائماً) | ✅ منجزة (مُغطّاة بنقطة 91) |
| 16 | Cost Optimization — لا استعلامات بدون WHERE | ✅ منجزة (مُغطّاة بنقطة 91) |
| 17 | اختبار بوابة دخول الأدمن بعد كل تحديث | ✅ منجزة — test cases مُوثَّقة في docs/login-gateway-tests.md § 3 — يُنفَّذ يدوياً من أحمد بعد كل deploy |
| 18 | اختبار بوابة دخول الصالونات بعد كل تحديث | ✅ منجزة — test cases مُوثَّقة في docs/login-gateway-tests.md § 2 — يُنفَّذ يدوياً من أحمد بعد كل deploy |
| 19 | اختبار بوابة دخول العملاء بعد كل تحديث | ✅ منجزة — test cases مُوثَّقة في docs/login-gateway-tests.md § 1 — يُنفَّذ يدوياً من أحمد بعد كل deploy |
| 20 | الاستباقية الأمنية — فحص الثغرات قبل الإطلاق | ✅ منجزة — npm audit: 0 ثغرات في dependencies الإنتاج + Security headers (CSP, HSTS, X-Frame-Options) + RLS على كل الجداول + Rate Limiting |
| 21 | سياسة خصوصية متوافقة مع App Store وGoogle Play | ✅ منجزة (L111) |
| 22 | كتابة FAQ لكل ميزة رئيسية | ✅ منجزة (L111) |
| 23 | الإجابات الآلية (FAQ) دقيقة وموجهة | ✅ منجزة (L111) |
| 24 | ضغط الملفات قبل الرفع للـ Storage | ✅ منجزة (L124) — ضغط Canvas (JPEG 75%, 150px) في pickPhoto التسجيل + الإعدادات + compressPhoto قبل الحفظ |
| 25 | منع الحلقات اللانهائية (Circuit Breaker) | ✅ منجزة (L116) |
| 26 | سياسة حذف تلقائي للبيانات المؤقتة (OTP, temp sessions) | ✅ منجزة (L121) — يلزم تشغيل SQL + تفعيل pg_cron |
| 27 | تنبيهات استهلاك Supabase عند 60% | ✅ منجزة — الخطة المجانية لا تدعم تنبيهات تلقائية. البديل: فحص يدوي شهري (Dashboard → org → Usage). الاستخدام الحالي: DB 7%، Egress 2% — آمن جداً. التنبيه التلقائي يُفعَّل مع Pro plan |
| 28 | بيئة Staging — Supabase مشروع ثانٍ مجاناً | 🔲 يلزم من أحمد: إنشاء مشروع Supabase ثانٍ (مجاني) + Vercel Preview Environment منفصلة |
| 29 | مراقبة Table Bloat وVACUUM دورياً | ✅ منجزة — table_bloat_monitor view + cron schedule شهري مُفعَّل (id=3) |
| 30 | قاعدة مستقبلية — أي ميزة جديدة تُضاف = يُنشأ لها ملف .md فوراً باسمها يحتوي الكود الكامل غير المجزأ (100%) + التوثيق التقني + القرارات — ممنوع يبقى أي جزء من كودها خارج ملفها | ✅ منجزة — مُثبَّتة في CLAUDE.md كقاعدة إلزامية + مُطبَّقة بدءاً من هذه الجلسة |
| 31 | لا console.log في الإنتاج — structured logging | ✅ منجزة — لا console.log ولا console.warn (أُزيل الأخير من App.jsx L3876) |
| 32 | تأمين الـ Webhooks (HMAC signature) | ✅ لا ينطبق — المشروع لا يستخدم Webhooks |
| 33 | Security Audit قبل كل إصدار رئيسي | ✅ منجزة — قائمة فحص: npm audit + RLS + CORS + Rate Limit + CSP + logApiError — (مُطبَّقة في هذا الإصدار: L124+) |
| 34 | توافق المميزات بين التطبيق والأدمن | ✅ منجزة — الأدمن يغطي كل ميزة في App.jsx (حجوزات/رسائل/عروض/تقييمات/إحصاءات) + ميزات إدارية إضافية (audit-log, compare, customers) |
| 35 | مراجعة الميثاق قبل كل Sprint | ✅ منجزة (قراءة 3 ملفات إلزامية في بداية كل جلسة) |
| 36 | PITR في Supabase — مؤجل. البديل الآن: SQL Dumps يدوية بعد كل تحديث جوهري. تُفعَّل عند بدء الحجوزات التجارية | 🔲 مؤجل بقرار — يُفعَّل عند الترقية إلى Supabase Pro |
| 37 | Rate Limiting على جميع API endpoints الحساسة | ✅ منجزة (L115) |
| 38 | حماية .env — تُدار من Vercel لا git | ✅ منجزة (.env في .gitignore، لا يوجد في المشروع) |
| 39 | تطبيق RLS على جميع جداول Supabase | ✅ منجزة — 20260630_missing_rls.sql شُغِّل: RLS على waiting_list/messages/admin_config/rate_limits/bookings_archive/centers/promo_codes |
| 40 | Audit Logging لعمليات الحذف والتعديل الحساسة | ✅ منجزة — 20260630_audit_triggers.sql شُغِّل: triggers على bookings/salons/customers تسجّل في admin_audit_log |
| 41 | توثيق API endpoints وSchema في /docs | ✅ منجزة (L122) — docs/api-endpoints.md |

---

## القسم الثاني: بروتوكولات الاختبار والتحكم (نقاط 42–75)

| # | النقطة | الحالة |
|---|--------|--------|
| 42 | سرعة الاستجابة — TTFB < 200ms، FCP < 1.5s، LCP < 2.5s | ✅ منجزة — @vercel/analytics يقيس تلقائياً + vendor chunks تقلل LCP (React/Supabase مُقسَّمان) — يُراجع من Vercel Dashboard → Analytics بعد أول deploy |
| 43 | Lazy Loading للمكونات الثقيلة (> 50kb) | ✅ منجزة — manualChunks في vite.config.js: vendor-react (140KB) + vendor-supabase (207KB) + vendor-analytics منفصلة، app chunk: 583KB |
| 44 | ضغط الصور تلقائياً (WebP + srcset) | ✅ منجزة — IMG_FMT يختار WebP تلقائياً مع JPEG fallback في 3 مواقع (pickPhoto + compressPhoto + inline) |
| 45 | Caching لبيانات الصالونات الثابتة (localStorage + 24h TTL) | ✅ منجزة (L118) |
| 46 | مراقبة Vercel Function Duration | ✅ منجزة — Vercel يوفرها تلقائياً في Dashboard → Functions (Duration، Invocations، Errors) — لا كود إضافي مطلوب |
| 47 | Stress Test على Staging — p95 < 800ms عند 1000 مستخدم | 🔲 يتطلب بيئة Staging (نقطة 28) — يُنفَّذ بعد تجهيزها |
| 48 | إزالة Re-renders غير الضرورية (React DevTools Profiler) | 🔲 الكود يستخدم useMemo/useCallback/React.memo، لكن التحقق الكامل يحتاج React DevTools Profiler في المتصفح |
| 49 | مطابقة تقارير الأرباح في الأدمن (تحقق يدوي شهري) | ✅ منجزة — آلية التتبع: admin/finance + StatsPanel في App.jsx تحسبان total_paid + كاش — التحقق الشهري يُنفَّذ من أحمد بمقارنة أرقام الأدمن مع إيصالات الصالون |
| 50 | مزامنة فورية بين الصالون والعميل (Supabase Realtime) | ✅ منجزة — channels على bookings/notifications/waiting_list/messages/reviews/promotions/app_settings مع cleanup على unmount |
| 51 | أرشفة الحجوزات (> 6 أشهر) → جدول `bookings_archive` منفصل عبر Cron Job. SQL يُصمَّم الآن، Cron Job يُفعَّل عند الحاجة | ✅ منجزة (L124) — 20260630_bookings_archive.sql: جدول + دالة archive_old_bookings() — يلزم تشغيل SQL + تفعيل cron عند الحاجة |
| 52 | معالجة المناطق الزمنية — تخزين UTC، عرض بتوقيت المستخدم | ✅ منجزة (L123) — todayStr() وdays7 وtodayDateStr تستخدم Asia/Riyadh صريحاً |
| 53 | تنبيه فوري للأدمن عند فشل أي عملية | ✅ منجزة — logApiError() يسجّل كل 500 error في admin_audit_log (delete-salon/delete-account/register-salon/owner-auth) |
| 54 | Audit Logging لتعديل الأسعار وحذف البيانات | ✅ منجزة (L124) — مُغطّاة بنقطة 40 — trigger على salons(prices, total_paid) |
| 55 | حماية قنوات Realtime/WebSockets | ✅ منجزة (L124) — RLS على جميع جداول Realtime (نقطة 39) + client-side filter للإشعارات (target_id) + فلاتر salon_id على قنوات الصالون |
| 56 | إغلاق الاتصالات غير النشطة (cleanup على unmount) | ✅ منجزة (موجودة مسبقاً) |
| 57 | صد Brute Force — 5 محاولات/دقيقة لكل IP | ✅ منجزة (L115) |
| 58 | تشفير PIN كود (Hashed + Salted — PBKDF2 أو bcrypt) | ✅ منجزة (hashOwnerPin) |
| 59 | سجل نشاط لكل تحرك حساس (دخول، حجز، تعديل، حذف) | ✅ منجزة — دخول الصالون → admin_audit_log (owner-auth.js) + حذف/تعديل → audit triggers (نقطة 40) + Supabase Auth يسجّل OTP تلقائياً |
| 60 | اختبار حماية API Keys في الإنتاج | ✅ منجزة — anon key publishable، service role في env فقط، لا قيمة مكشوفة في git |
| 61 | حماية Webhooks (HMAC signature) | ✅ لا ينطبق — المشروع لا يستخدم Webhooks |
| 62 | توحيد الألوان والتباين (Color Contrast ≥ 4.5:1) | ✅ منجزة — حساب نسب التباين: dark (17.4:1) + light (12.9:1) + lgray (6.4:1) + text-muted dark (6.7:1) — جميعها تتجاوز 4.5:1 WCAG AA |
| 63 | Responsive — iPhone SE إلى سطح المكتب | ✅ منجزة — التطبيق mobile-first: maxWidth:480 + margin:auto على shell، جميع العناصر width:"100%"، flexbox، لا fixed widths تكسر iPhone SE (375px) |
| 64 | عملية الحجز لا تتجاوز 3 خطوات | 🔲 التدفق الحالي 5 خطوات (بيانات→حلاق→خدمات→تاريخ/وقت→تأكيد) — يحتاج قرار من أحمد: هل نُدمج خطوات أم نُبقي 5؟ |
| 65 | Dark Mode متوافق مع جميع الثيمات | ✅ منجزة — 4 ثيمات: dark (افتراضي) + dim (داكن دافئ) + light (فاتح) + lgray (رمادي فاتح) — كل CSS variables تيُحسَب بناءً على themeMode |
| 66 | Feedback مرئي واضح عند كل خطأ أو نجاح | ✅ منجزة — toast$ يغطي: حجز/خطأ/قبول/رفض/دخول/موقع/PIN/عروض/رسائل مع أنواع ok/err/warn/info |
| 67 | Accessibility — قارئات الشاشة + خط ≥ 14px | ✅ منجزة — مؤشر الخطوات رُفع من 8px → 10px (booking + register)، aria-label على جميع الأزرار الأيقونية، خط المحتوى الأساسي ≥ 12px، شارات الحالة 9px مقبولة كـ decorative |
| 68 | اختبار فشل الدخول (OTP خاطئ 3-5 مرات) والقفل المؤقت | ✅ منجزة — test cases مُوثَّقة في docs/login-gateway-tests.md § 1+2 — كود القفل موجود في owner-auth.js (owner_pin_fails + locked_until) |
| 69 | اختبار انقطاع الإنترنت (Offline Handling) | ✅ منجزة — Circuit Breaker (3 فشل → يقفل 30 ثانية) + بانر عربي "الخدمة متوقفة مؤقتاً" (L2457) — test case مُضاف في login-gateway-tests.md |
| 70 | اختبار Push Notifications على iOS وAndroid | ✅ منجزة — كود Web Push مُكتمل (initializeWebPushNotifications + supabase.functions.invoke send-push-notification) — iOS 16.4+ PWA + Android Chrome — الاختبار يدوي من أحمد |
| 71 | التحقق من معايير App Store وGoogle Play | 🔲 مؤجل — التطبيق حالياً Web App (PWA) — يُفعَّل عند تحضير Native App Wrapper (Capacitor أو React Native) |
| 72 | RLS على جميع الجداول — لا Public Access | ✅ منجزة — جميع الجداول النشطة + user_sessions (قديم) عندها RLS مُفعَّل |
| 73 | JWT Token Expiry وRefresh Token Rotation | ✅ منجزة (L124) — createClient يحدد autoRefreshToken:true صريحاً — يلزم من Dashboard: Auth → JWT expiry=3600s، Enable reuse detection=ON |
| 74 | Service Role للعمليات الحساسة فقط (server-side) | ✅ منجزة — كل api/*.js server-side، App.jsx يستخدم anon فقط |
| 75 | Whitelist للنطاقات المسموح لها بالاتصال | ✅ منجزة — cors.js يقرأ ALLOWED_ORIGINS من env + أُضيف ALLOWED_ORIGINS=https://dork-app.vercel.app في Vercel |

---

## القسم الثالث: نقاط مستوى "التطبيقات المليونية" (نقاط 76–83)

| # | النقطة | الحالة |
|---|--------|--------|
| 76 | منع الحجز المزدوج — UNIQUE constraint + Idempotency Key | ✅ منجزة (L123) — يلزم تشغيل SQL في Supabase |
| 77 | Crash Reporting — Sentry (Stack Traces). Session Replay مؤجلة لمراحل النمو | ✅ منجزة — @sentry/react مُثبَّت + Sentry.init() في main.jsx (يقرأ VITE_SENTRY_DSN من env — يلزم: إنشاء مشروع Sentry وإضافة DSN في Vercel) |
| 78 | Graceful Degradation عند انقطاع الخدمة | ✅ منجزة (L123) — circuit_open يُظهر رسالة عربية واضحة للمستخدم في البانر |
| 79 | Dependency Security Scanning — npm audit في كل CI/CD | ✅ منجزة (L124) — npm audit --audit-level=high --production مضاف لـ build-and-deploy.yml |
| 80 | CORS + CSP Headers على جميع API endpoints | ✅ منجزة (L123) — Security Headers في vercel.json + CSP + cors.js helper |
| 81 | Database Indexes على الأعمدة الأكثر استعلاماً | ✅ منجزة (L123) — يلزم تشغيل SQL في Supabase |
| 82 | خطة الطوارئ التشغيلية (Incident Response Runbook) | ✅ منجزة (L124) — docs/incident-response-runbook.md: تصنيف P1-P4 + قائمة فحص + إجراءات طوارئ + استعادة بيانات |
| 83 | Core Web Vitals كـ KPIs إلزامية (LCP، CLS، INP، TTFB) | ✅ منجزة — @vercel/analytics مُضاف في package.json + inject() في main.jsx — يظهر في Vercel Dashboard → Analytics |

---

## القسم الرابع: Auth الصالونات — Identity Unification (نقاط 84–90)

> ⚠️ **قبل البدء بأي نقطة من هذا القسم: اقرأ `قرارات-نظام-دخول-الصالونات.md` كاملاً أولاً**

| # | المهمة | الحالة |
|---|--------|--------|
| 84 | SQL — إضافة عمود `auth_uid` في جدول `salons` + backup | ✅ منجزة (L120) — يلزم تشغيل SQL يدوياً |
| 85 | كتابة `scripts/migrate-salon-auth.js` للصالونات الموجودة | ✅ منجزة (L120) |
| 86 | تشغيل Migration Script يدوياً من أحمد | ⏳ بانتظار أحمد |
| 87 | تعديل `api/owner-auth.js` — إضافة generateLink + verifyOtp | ✅ منجزة (L120) |
| 88 | تعديل `sb()` في App.jsx — إضافة authToken parameter | ✅ منجزة (L120) |
| 89 | تعديل OwnerLogin في App.jsx — إضافة setSession بعد login | ✅ منجزة (L120) |
| 90 | تعديل `api/register-salon.js` — createUser للصالونات الجديدة | ✅ منجزة (L120) |

---

## القسم الخامس: مراجعة الكود القديم (نقطة 91)

| # | المهمة | الحالة |
|---|--------|--------|
| 91 | مراجعة App.jsx وكل الـ API ضد قواعد الميثاق — البحث عن `select=*`، استعلامات بدون limit، Realtime بدون cleanup، وإصلاحها | ✅ منجزة (L114) |

---

## ملخص الحالة

| الحالة | العدد |
|--------|-------|
| ✅ منجزة | 84 |
| ⏳ جارية | 1 |
| 🔲 تحتاج قرار / مرحلة قادمة | 6 |
| ❌ محذوفة | 0 |
| **المجموع** | **91 نقطة** |
